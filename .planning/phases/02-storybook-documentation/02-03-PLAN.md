---
phase: 02-storybook-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - stories/spacing.stories.js
  - stories/radius.stories.js
  - stories/border.stories.js
autonomous: true
requirements:
  - REQ-STORYBOOK-STORIES-8

must_haves:
  truths:
    - "stories/spacing.stories.js exports SpacingRamp (PascalCase)"
    - "Spacing bars render horizontal bars whose pixel width equals the token value"
    - "stories/radius.stories.js exports RadiusScale (PascalCase)"
    - "Radius boxes show border-radius applied from each of the 7 radius tokens"
    - "stories/border.stories.js exports BorderWidths (PascalCase)"
    - "Border story shows 4 lines: none/hairline/strong/focus with correct widths and Electric Green for focus"
    - "All three stories import from ../generated/tokens.js"
  artifacts:
    - path: "stories/spacing.stories.js"
      provides: "Spacing scale visual bars"
      contains: "space050"
    - path: "stories/radius.stories.js"
      provides: "Radius scale visual boxes"
      contains: "radiusXs"
    - path: "stories/border.stories.js"
      provides: "Border width stripe demo"
      contains: "borderWidthFocus"
  key_links:
    - from: "stories/spacing.stories.js"
      to: "generated/tokens.js"
      via: "import * as tokens from '../generated/tokens.js'"
      pattern: "from '\\.\\./generated/tokens\\.js'"
---

<objective>
Author three visual token documentation stories: Spacing (bar widths proportional to token values), Radius (boxes with border-radius applied), and Border (stripe demo showing 4 border-width tokens). These three stories can be authored in parallel with Plan 02-02 since they touch different story files.

Purpose: Spacing, radius, and border tokens are foundational layout constraints — designers and engineers need visual reference for every value.

Output: stories/spacing.stories.js, stories/radius.stories.js, stories/border.stories.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-storybook-documentation/02-CONTEXT.md
@.planning/phases/02-storybook-documentation/02-RESEARCH.md

<interfaces>
From generated/tokens.js (spacing — values are JS numbers in dp/pt):
  space050 = 2, space100 = 4, space200 = 8, space300 = 12, space400 = 16,
  space500 = 20, space600 = 24, space800 = 32, space1000 = 40, space1200 = 48, space1600 = 64

From generated/tokens.js (radius — values are JS numbers):
  radiusXs = 8, radiusSm = 12, radiusMd = 16, radiusLg = 20, radiusXl = 28,
  radius2xl = 36, radiusFull = 999
  (radiusIcon = 22.37 — app icon squircle, do NOT include in radius story — it is a special constant)

From generated/tokens.js (border widths — values are JS numbers):
  borderWidthNone = 0, borderWidthHairline = 1, borderWidthStrong = 1.5, borderWidthFocus = 2
  colorBorderFocus — Electric Green hex (color of focus ring border, use for focus stripe)
  colorActionPrimary — Electric Green (same value as colorBorderFocus; use colorBorderFocus for clarity)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write stories/spacing.stories.js</name>
  <files>stories/spacing.stories.js</files>

  <read_first>
  - generated/tokens.js — confirm exact spacing token names and their numeric values (lines ~82-92)
  - .planning/phases/02-storybook-documentation/02-CONTEXT.md — D-07 (spacing bars spec: bar width = token value in px, token name + value below each)
  </read_first>

  <action>
  Create stories/spacing.stories.js as a plain ES module.

  Structure:
  - Import: import * as tokens from '../generated/tokens.js'
  - Default export: { title: 'Foundation/Spacing' }
  - One named export: SpacingRamp (PascalCase)

  Define a bar helper (not exported). Takes (name, value) and returns an HTML row containing:
  - A horizontal colored bar: height 24px, width equal to the token value in px (e.g. style="width:${value}px"), background-color #c6ff2d (Electric Green), border-radius 4px
  - Below the bar: token name in Inter 11px 600 #0f0f0f, and value in JetBrains Mono 11px #808080 formatted as value+"dp"

  The SpacingRamp story renders all 11 spacing tokens in ascending order of value:
  space050 (2), space100 (4), space200 (8), space300 (12), space400 (16), space500 (20), space600 (24), space800 (32), space1000 (40), space1200 (48), space1600 (64).

  Container: padding:32px, background:#ffffff, max-width:480px. Each bar row has margin-bottom:16px. Because space050 bar (2px) is very narrow, render all bars left-aligned in a flex column.

  The bar color uses the Electric Green value directly as a hardcoded CSS literal #c6ff2d (not from token — it is a visual aid bar, not a token swatch; this is acceptable since the bar color is decorative, not demonstrating a color token). Alternatively, use tokens.colorActionPrimary if preferred — either is acceptable.
  </action>

  <verify>
    <automated>node -e "import('./stories/spacing.stories.js').then(m => { if(!m.SpacingRamp) throw new Error('missing SpacingRamp'); const html = m.SpacingRamp(); if(typeof html !== 'string') throw new Error('must return string'); ['space050','space400','space1600'].forEach(t => { if(!html.includes(t)) throw new Error('missing: '+t); }); if(!html.includes('width:2px')&&!html.includes('width: 2px')) throw new Error('space050 bar width missing'); console.log('spacing.stories.js OK'); }).catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>

  <done>stories/spacing.stories.js exists, exports SpacingRamp, renders 11 horizontal bars (width = token value px), each labeled with token name and value.</done>
</task>

<task type="auto">
  <name>Task 2: Write stories/radius.stories.js and stories/border.stories.js</name>
  <files>stories/radius.stories.js, stories/border.stories.js</files>

  <read_first>
  - generated/tokens.js — confirm radius token names/values (lines ~74-81) and border token names/values (lines ~5-8) and colorBorderFocus value
  - .planning/phases/02-storybook-documentation/02-CONTEXT.md — D-07 (radius boxes spec), D-09 (border stripe spec)
  </read_first>

  <action>
  Create both files as plain ES modules.

  File 1 — stories/radius.stories.js:
  - Import: import * as tokens from '../generated/tokens.js'
  - Default export: { title: 'Foundation/Radius' }
  - One named export: RadiusScale (PascalCase)

  Define a box helper (not exported). Takes (name, value) and returns an HTML card containing:
  - A 64x64px square box, background #ebebeb, border-radius set to min(value, 32)+"px" (cap at 32px display so radiusFull=999 renders as a circle rather than overflowing the 64px box — but show the actual token value 999 in the label). Add border:1px solid #c9c9c9.
  - Below the box: token name in Inter 11px 600 #0f0f0f, and value in JetBrains Mono 11px #808080 formatted as value+"dp"

  The RadiusScale story renders 7 radius tokens (exclude radiusIcon — it is a special-purpose squircle constant, not a general UI radius) in order: radiusXs (8), radiusSm (12), radiusMd (16), radiusLg (20), radiusXl (28), radius2xl (36), radiusFull (999).
  Layout: flex-wrap:wrap, gap:24px, padding:32px, background:#ffffff.

  File 2 — stories/border.stories.js:
  - Import: import * as tokens from '../generated/tokens.js'
  - Default export: { title: 'Foundation/Border' }
  - One named export: BorderWidths (PascalCase)

  The BorderWidths story renders 4 stripe rows, each showing a horizontal border line:
  - borderWidthNone (0px): a div with no visible border, just a label row with "0dp — none (no border)" in Inter 13px #808080 and a note "(not visible)"
  - borderWidthHairline (1px): a horizontal line using border-bottom:${tokens.borderWidthHairline}px solid #0f0f0f, below: label "borderWidthHairline — 1dp — dividers, outlined chips"
  - borderWidthStrong (1.5px): border-bottom:${tokens.borderWidthStrong}px solid #0f0f0f, label "borderWidthStrong — 1.5dp — input focus, selected card"
  - borderWidthFocus (2px): border-bottom:${tokens.borderWidthFocus}px solid ${tokens.colorBorderFocus} on a dark (#0f0f0f) background swatch to make Electric Green visible, label "borderWidthFocus — 2dp — focus ring, always Electric Green"

  Container: padding:32px, background:#ffffff, max-width:480px. Each stripe row has margin-bottom:32px.
  </action>

  <verify>
    <automated>node -e "Promise.all([import('./stories/radius.stories.js'),import('./stories/border.stories.js')]).then(([r,b]) => { if(!r.RadiusScale) throw new Error('missing RadiusScale'); if(!b.BorderWidths) throw new Error('missing BorderWidths'); const rh=r.RadiusScale(); const bh=b.BorderWidths(); if(!rh.includes('radiusXs')) throw new Error('missing radiusXs'); if(!rh.includes('radiusFull')) throw new Error('missing radiusFull'); if(!bh.includes('borderWidthFocus')) throw new Error('missing borderWidthFocus'); if(!bh.includes('colorBorderFocus')&&!bh.includes('#c6ff2d')) throw new Error('focus ring color missing'); console.log('radius+border stories OK'); }).catch(e=>{ console.error(e.message); process.exit(1); })"</automated>
  </verify>

  <done>stories/radius.stories.js exports RadiusScale with 7 radius boxes (radiusXs through radiusFull). stories/border.stories.js exports BorderWidths with 4 stripe rows including Electric Green focus ring on dark background.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated/tokens.js → story HTML | Numeric token values interpolated into CSS width/border-radius/border-width properties |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | CSS value interpolation (width:${value}px) | accept | Values are numeric primitives from controlled build output; no user input; no injection surface |
</threat_model>

<verification>
After both tasks, run from project root (with generated/tokens.js already built):
1. node -e "import('./stories/spacing.stories.js').then(m=>console.log('spacing ok:', typeof m.SpacingRamp()))"
2. node -e "import('./stories/radius.stories.js').then(m=>console.log('radius ok:', typeof m.RadiusScale()))"
3. node -e "import('./stories/border.stories.js').then(m=>console.log('border ok:', typeof m.BorderWidths()))"
All should print "ok: string".
</verification>

<success_criteria>
- stories/spacing.stories.js: exports SpacingRamp, 11 bars with width = token value px, labeled with token name and value
- stories/radius.stories.js: exports RadiusScale, 7 boxes with border-radius applied from tokens, radiusFull shows as circle (capped at 32px display radius) with 999 in label
- stories/border.stories.js: exports BorderWidths, 4 stripes showing none/hairline/strong/focus, focus stripe uses colorBorderFocus (Electric Green) on dark background
- All three files use import * as tokens from '../generated/tokens.js'
</success_criteria>

<output>
Create `.planning/phases/02-storybook-documentation/02-03-SUMMARY.md` when done
</output>
