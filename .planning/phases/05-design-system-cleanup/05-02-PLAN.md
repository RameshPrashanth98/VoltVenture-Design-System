---
phase: 05-design-system-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package-lock.json
autonomous: true
requirements:
  - D-03
  - D-04

must_haves:
  truths:
    - "package-lock.json contains no entries starting with apps/showcase"
    - "package-lock.json retains the 5 root devDependencies entries (style-dictionary, @storybook/html-vite, storybook, vite, wcag-contrast)"
    - "node_modules/ contains only root devDep packages — 589 extraneous showcase/Expo/React Native packages removed"
    - "npm run build:tokens exits 0 after lockfile regeneration"
    - "npm test exits 0 (42 pass) after lockfile regeneration"
  artifacts:
    - path: "package-lock.json"
      provides: "Clean lockfile with only 5 root devDependencies — no showcase workspace entries"
      contains: "\"lockfileVersion\""
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install reads workspaces glob"
      pattern: "workspaces.*apps"
---

<objective>
Run `npm install` from repo root to regenerate package-lock.json after apps/showcase/ deletion, then stage and commit the clean lockfile. Also run build:tokens and npm test as a quick health check.

Purpose: Removes the 589 extraneous Expo/React Native/showcase packages from the lockfile and node_modules/. After this plan, the only installed packages are the 5 root devDependencies. The GitHub Actions CI (npm ci) will be significantly faster.

Output: One git commit with the regenerated package-lock.json. Build and test pipelines confirmed passing.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/05-design-system-cleanup/05-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/05-design-system-cleanup/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate lockfile via npm install and verify health</name>
  <files>package-lock.json</files>
  <read_first>
    Confirm Plan 01 completed: run `git log --oneline -1` and verify the cleanup commit exists. Confirm `git status` shows package-lock.json as ` M` (unstaged modification) — this is the pre-npm-install dirty lockfile still with showcase entries.
  </read_first>
  <action>
    From the repo root (D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/):

    1. Run npm install:
       npm install
       This removes 589 extraneous packages (all React Native, Expo, showcase transitive deps) and the two showcase workspace entries (apps/showcase and apps/showcase/node_modules/react-native-safe-area-context). Expected output includes lines like "removed 589 packages". Any "could not resolve workspace" warnings for the now-absent apps/showcase are benign — ignore them.

    2. Verify the lockfile is clean — run this inline node check:
       node -e "const l=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const keys=Object.keys(l.packages).filter(k=>k.startsWith('apps/showcase')); if(keys.length){console.error('ERROR: showcase entries remain:',keys);process.exit(1);}console.log('Lockfile clean — no showcase entries. Package count:',Object.keys(l.packages).length);"
       Expected output: "Lockfile clean — no showcase entries." If the check errors, do NOT commit — inspect what remains and report.

    3. Run the quick health checks to confirm the design system pipelines are unaffected:
       npm run build:tokens
       npm test
       Both must exit 0. If either fails, stop and report the error before committing.

    4. Stage only the lockfile — do NOT stage anything else:
       git add package-lock.json

    5. Confirm staged diff: `git diff --cached --name-only` must show only package-lock.json.

    6. Commit with this exact message (no Co-Authored-By line per project convention):
       chore(deps): regenerate lockfile after showcase removal

    Do NOT run git add -A, git add ., or stage any other file. The three untracked files (.claire/, voltventure-foundations (1).html, voltventure_wireframes.pen) must remain untracked.
  </action>
  <verify>
    <automated>git log --oneline -2 && node -e "const l=JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const keys=Object.keys(l.packages).filter(k=>k.startsWith('apps/showcase')); console.log(keys.length===0?'PASS: lockfile clean':'FAIL: showcase entries remain: '+keys.join(', '));"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -2` shows both the Plan 01 cleanup commit and the new lockfile commit as the two most recent commits
    - The inline node lockfile check outputs "PASS: lockfile clean" (zero keys starting with "apps/showcase")
    - `npm run build:tokens` exits 0 (run before committing — confirmed in step 3)
    - `npm test` exits 0 with 42 pass, 0 fail (run before committing — confirmed in step 3)
    - `git status` shows working tree clean (no staged changes, package-lock.json no longer dirty)
    - The three untracked files (.claire/, voltventure-foundations (1).html, voltventure_wireframes.pen) still show as `??` — not committed
  </acceptance_criteria>
  <done>Lockfile committed clean. Design system build and test pipelines confirmed passing. node_modules/ contains only the 5 root devDeps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → node_modules | npm reads package.json and workspace glob; showcase workspace is absent from disk |
| git staging area → commit | Only package-lock.json must be staged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | git staging (lockfile commit) | mitigate | Stage only `package-lock.json` explicitly; confirm with `git diff --cached --name-only` before committing |
| T-05-04 | Denial of Service | npm install removes needed packages | accept | All 5 root devDeps retained by package.json declaration; npm install re-installs them if missing |
</threat_model>

<verification>
After this plan completes:
- `git log --oneline -3` shows Plan 01 commit and Plan 02 commit in order
- Lockfile check node script exits 0 with "PASS: lockfile clean"
- `npm run build:tokens && npm test` both exit 0
</verification>

<success_criteria>
The lockfile is regenerated and committed. Only the 5 root devDeps are installed. The design system token pipeline and unit tests pass. The repo is now clean of all showcase-related npm artifacts.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-cleanup/05-02-SUMMARY.md` when done.
</output>
