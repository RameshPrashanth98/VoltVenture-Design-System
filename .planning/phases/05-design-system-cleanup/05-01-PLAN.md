---
phase: 05-design-system-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/.gitkeep
autonomous: true
requirements:
  - D-01
  - D-02

must_haves:
  truths:
    - "apps/showcase/ no longer exists in the git index (all 29 files staged as deleted)"
    - "apps/ directory is preserved via apps/.gitkeep so the workspace glob retains meaning"
    - "No untracked files (.claire/, voltventure-foundations (1).html, voltventure_wireframes.pen) appear in the commit"
  artifacts:
    - path: "apps/.gitkeep"
      provides: "Placeholder that keeps apps/ directory tracked by git after showcase removal"
      contains: ""
  key_links:
    - from: "package.json"
      to: "apps/"
      via: "workspaces: [\"apps/*\"]"
      pattern: "workspaces.*apps"
---

<objective>
Stage the 29 apps/showcase/ file deletions (already deleted on disk as unstaged D entries), add apps/.gitkeep to preserve the apps/ directory per D-02, and commit both changes atomically.

Purpose: Clears the showcase app from git history cleanly, without touching untracked files that must never be committed (.claire/, voltventure-foundations (1).html, voltventure_wireframes.pen). Preserves the apps/ monorepo directory so workspaces: ["apps/*"] retains its intended meaning.

Output: One git commit removing 29 files and adding 1 .gitkeep file. The package-lock.json is intentionally NOT staged here — it is regenerated in Plan 02 after this commit.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/05-design-system-cleanup/05-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage showcase deletions and add .gitkeep, then commit</name>
  <files>apps/.gitkeep</files>
  <read_first>
    Run `git status` from repo root before touching anything. Confirm the 29 apps/showcase/ entries show as ` D` (unstaged deletions) and that .claire/, "voltventure-foundations (1).html", and voltventure_wireframes.pen show as `??` (untracked — must NOT be staged).
  </read_first>
  <action>
    From the repo root (D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/):

    1. Create the apps/.gitkeep file using the Write tool (empty file, zero bytes). This preserves the apps/ directory in git per D-02.

    2. Stage the showcase deletions using the exact quoted path to avoid shell glob expansion on the bracket filename [item].tsx:
       git add "apps/showcase/"

    3. Stage the new .gitkeep:
       git add "apps/.gitkeep"

    4. Verify staged files before committing — run `git diff --cached --stat`. Confirm:
       - Exactly 29 files shown as deleted (all under apps/showcase/)
       - Exactly 1 file shown as added (apps/.gitkeep)
       - Total: 30 file changes. If fewer than 29 deletions appear, the bracket filename may not have staged — abort and use git add with explicit quoting.
       - Confirm .claire/, voltventure-foundations (1).html, voltventure_wireframes.pen do NOT appear in the staged list.

    5. Commit with this exact message (no Co-Authored-By line per project convention):
       chore(cleanup): remove apps/showcase — showcase moves to separate repo

    Do NOT use git add -A or git add . at any point. Do NOT stage package-lock.json in this task — it will be handled in Plan 02 after npm install.
  </action>
  <verify>
    <automated>git log --oneline -1 && git show --stat HEAD | grep -E "apps/showcase|apps/.gitkeep"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows the commit message "chore(cleanup): remove apps/showcase — showcase moves to separate repo"
    - `git show --stat HEAD` lists exactly 30 file changes: 29 deletions under apps/showcase/ and 1 insertion of apps/.gitkeep
    - `git status` shows package-lock.json as ` M` (still unstaged) — it was NOT included in this commit
    - `git status` shows no staged changes (working tree clean except package-lock.json unstaged modification and the three untracked files)
    - Running `ls apps/` from repo root shows only `.gitkeep` — the directory exists and showcase is gone
  </acceptance_criteria>
  <done>Commit exists on main with 29 showcase deletions + 1 .gitkeep. package-lock.json is still dirty (unstaged) — left for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git staging area → commit | Only explicitly named paths must enter; untracked files must stay out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | git staging | mitigate | Stage only explicit paths (apps/showcase/ and apps/.gitkeep); verify with git diff --cached --stat before committing |
| T-05-02 | Information Disclosure | untracked files (.claire/, wireframes) | mitigate | Never use git add -A or git add .; acceptance criteria explicitly checks these do not appear in staged changes |
</threat_model>

<verification>
After this plan completes:
- `git log --oneline -1` shows the cleanup commit
- `git show --stat HEAD` shows exactly 30 file changes (29 D + 1 A)
- `git status` shows package-lock.json unstaged modified only
- `ls "apps/"` shows only .gitkeep
</verification>

<success_criteria>
The showcase app code is removed from git. The apps/ directory is preserved. The lockfile is not yet cleaned — that is Plan 02's job.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-cleanup/05-01-SUMMARY.md` when done.
</output>
