---
context: phase
phase: 05-design-system-cleanup
task: null
total_tasks: 3
status: in_progress
last_updated: 2026-08-05T00:00:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints require an understanding check before resuming._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| No Claude attribution | Never add `Co-Authored-By: Claude` to commit messages in this repo | advisory | Always write commit messages without attribution lines |
| git add -A / git add . | Working tree has untracked files (.claire/, .html, .pen) that must NEVER be committed. Bulk staging includes them. | blocking | Stage specific paths only: `git add "apps/showcase/"`, `git add apps/.gitkeep`, `git add package-lock.json`, etc. |
| Researcher lib/ mutation | The gsd-phase-researcher agent unexpectedly modified lib/voltventure_theme.ts (removed onPrimary override) and lib/voltventure_tokens.ts (changed export const → const) during research this session. Both were reverted. | advisory | After any researcher agent completes, run `git diff lib/` before staging anything. |
| Bracket filename glob | apps/showcase/app/[item].tsx has brackets that Windows bash expands as glob without quotes | advisory | Always quote the path: `git add "apps/showcase/"` |

<current_state>
Phase 5 Design System Cleanup. plan-phase COMPLETE (2026-08-05). 3 plans in 3 waves verified and committed. Ready to execute.

Working tree:
- 29 `apps/showcase/` files: DELETED (unstaged, `D` markers in git status)
- `package-lock.json`: MODIFIED (showcase deps still present; needs npm install to clean)
- Untracked (do NOT commit): `.claire/`, `voltventure-foundations (1).html`, `voltventure_wireframes.pen`

Plan structure:
- Wave 1 (05-01): Stage 29 showcase deletions + create apps/.gitkeep, commit
- Wave 2 (05-02): npm install → regenerate package-lock.json clean, commit
- Wave 3 (05-03): Update README.md (9 locations), STATE.md, ROADMAP.md → done-bar (build:tokens + npm test + build-storybook) → push

KEY: apps/ needs `apps/.gitkeep` — deleting its only child removes apps/ from git, violating D-02 (keep apps/ for future packages).
</current_state>

<completed_work>

This session:
- discuss-phase complete (commit babbe61) — prior session
- plan-phase complete — 3 plans, 3 waves, verification PASSED (commit 50d5775)
- RESEARCH.md complete — 9 README locations identified, .gitkeep gap discovered (commit 506f85b)
- STATE.md updated — Phase 5 planned, Phase 4 ABANDONED reflected (commit 3681168)
- Reverted unintended lib/ modifications from researcher agent (lib/ unchanged)
</completed_work>

<remaining_work>

- 05-01 (Wave 1): Stage 29 showcase file deletions + create apps/.gitkeep + commit
- 05-02 (Wave 2): Run npm install from root → stage + commit package-lock.json
- 05-03 (Wave 3): Update README.md (9 showcase references removed) + update STATE.md + update ROADMAP.md + run done-bar + push to origin/main
</remaining_work>

<decisions_made>

- apps/.gitkeep needed (D-02) — deleting apps/showcase/ makes apps/ vanish from git; .gitkeep preserves workspace slot
- Stage via `git add "apps/showcase/"` (quoted) — bracket filename causes Windows shell glob issues
- Researcher agents can mutate lib/ — always check `git diff lib/` after agent completes
- Phase 4 = ABANDONED, not COMPLETE — no SUMMARY.md needed; already marked in ROADMAP.md
- workspaces: ["apps/*"] retained in root package.json (D-03) — never touched in this phase
- Frontend in separate repo (D-08); this repo = token pipeline + Storybook only
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-design-system-cleanup/05-CONTEXT.md` — all locked decisions (D-01 through D-08)
2. `.planning/phases/05-design-system-cleanup/05-01-PLAN.md` — Wave 1: staging, .gitkeep, commit
3. `.planning/phases/05-design-system-cleanup/05-02-PLAN.md` — Wave 2: npm install, lockfile verify
4. `.planning/phases/05-design-system-cleanup/05-03-PLAN.md` — Wave 3: README update, done-bar, push

## Critical Anti-Patterns (do NOT repeat these)
- **ANTI-PATTERN: git add -A** — three untracked files must never be committed; stage specific paths only
- **ANTI-PATTERN: touching lib/** — lib/ files are not modified in Phase 5; revert any agent that changes them

## Infrastructure State
- Git branch: `main`, HEAD at `3681168`
- origin/main: behind (push happens in 05-03 Wave 3)
- Working tree: 29 unstaged showcase deletions + modified package-lock.json
- Storybook: may be running on localhost:6006 (started this session via `npm run storybook`)
- No blocking processes

<context>
plan-phase 5 just completed this session. All 3 plans written, verified, and committed. The execution is short — the bulk of the work (file deletions) already happened in a prior session. This phase needs: staging → lockfile cleanup → doc updates. Should execute quickly.
</context>

<next_action>
Start with: `/gsd:execute-phase 5`
</next_action>
