---
phase: 05-design-system-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - D-05
  - D-06

must_haves:
  truths:
    - "README.md contains no references to apps/showcase, the Expo SDK badge, the App Screens section, the Component Showcase App section, or the Quick Start showcase subsection"
    - "README.md nav bar has no broken anchor link to #app-screens (the link is removed)"
    - "README.md correctly describes the repo as token pipeline + Storybook docs only"
    - "ROADMAP.md Phase 4 status shows ABANDONED with reason"
    - "ROADMAP.md Phase 5 status shows COMPLETE"
    - "STATE.md reflects Phase 5 as complete"
    - "npm run build-storybook exits 0 (Storybook stories unaffected per D-05)"
    - "npm run build:tokens exits 0"
    - "npm test exits 0 (42 pass)"
  artifacts:
    - path: "README.md"
      provides: "Updated repository README reflecting design system scope only"
      min_lines: 150
    - path: ".planning/STATE.md"
      provides: "Phase 5 marked complete"
      contains: "Phase 5"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 ABANDONED, Phase 5 COMPLETE recorded"
      contains: "ABANDONED"
  key_links:
    - from: "README.md"
      to: "nav bar anchors"
      via: "markdown heading links"
      pattern: "\\[.*\\]\\(#.*\\)"
---

<objective>
Update README.md to remove all nine showcase-app references, update ROADMAP.md to mark Phase 4 ABANDONED and Phase 5 COMPLETE, update STATE.md to reflect Phase 5 complete status, run the full done-bar verification (build:tokens + test + build-storybook), and push to origin/main.

Purpose: The public-facing README and planning docs must reflect the repo's actual scope — token pipeline + Storybook documentation only. The showcase app is gone; the README must not advertise it. Phase 4 closure and Phase 5 completion are formally recorded.

Output: Three file edits committed. Push to origin/main. Phase 5 complete.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/05-design-system-cleanup/05-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/README.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/05-design-system-cleanup/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md — remove all nine showcase references</name>
  <files>README.md</files>
  <read_first>
    Read README.md in full before editing. The file is 330 lines. All nine removal locations are identified below with exact line ranges and content descriptions from the RESEARCH.md audit.
  </read_first>
  <action>
    Read README.md fully, then rewrite it using the Write tool with the following removals. Do NOT use fenced code blocks in this action — make each change precisely:

    REMOVAL 1 — Expo SDK badge (line 13):
    Remove the entire badge line: `[![Expo SDK](https://img.shields.io/badge/Expo_SDK-57-C6FF2D?style=flat-square&labelColor=0F0F0F)](https://expo.dev)`
    Keep the other four badges (Style Dictionary, React Native Paper, Storybook, WCAG AA, License) on separate lines.

    REMOVAL 2 — Nav bar (line 16):
    Remove `[App Screens](#app-screens) · ` from the nav link line.
    The nav bar after edit should read:
    `[Overview](#overview) · [Design System](#design-system-architecture) · [Token Pipeline](#token-pipeline) · [Quick Start](#quick-start) · [Structure](#project-structure)`

    REMOVAL 3 — App Screens section (lines 46–111):
    Remove the entire "## App Screens" section including the heading, the intro paragraph, both tables (Gateway and Core Experience), and the two images within it. The section ends just before "## Design System Architecture".

    REMOVAL 4 — Component Showcase App section (lines 184–196):
    Remove the entire "## Component Showcase App" section including the heading, the intro paragraph, "### 11 Components" heading + component list, and "### 9 Screens" heading + screen list. The section ends just before "## Quick Start".

    REMOVAL 5 — Quick Start > Showcase App subsection (lines 227–234):
    Remove the "### Showcase App (React Native)" subsection including its heading and the bash block (cd apps/showcase; npx expo start; Press i/a/QR instructions). Keep "### Install & Build Tokens" and "### Storybook (Token Documentation)" subsections intact.

    REMOVAL 6 — Run Tests > TypeScript check (lines 242–244):
    Remove the two-line block under "### Run Tests":
    `# TypeScript check (showcase app)`
    `cd apps/showcase && npx tsc --noEmit`
    Keep the SD transform unit tests block (`npm test`).

    REMOVAL 7 — Project Structure > apps/showcase subtree (lines 268–275):
    In the project structure code block, replace the apps/showcase/ subtree:
    FROM (the indented block):
    ```
    ├── apps/
    │   └── showcase/           # Expo app — Preview + Code tabs
    │       ├── app/
    │       │   ├── _layout.tsx     # PaperProvider + font loading
    │       │   ├── index.tsx       # SectionList home (11+9 items)
    │       │   └── [item].tsx      # Preview + Code detail tabs
    │       ├── components/     # 11 RN Paper component implementations
    │       ├── screens/        # 9 RN Paper screen implementations
    │       └── registry.ts     # All 20 entries — name, preview, source
    ```
    TO (single empty placeholder line):
    `├── apps/                   # Reserved for future packages`

    REMOVAL 8 — Design Decisions table row (line 291):
    Remove the `workspace:* protocol` row from the Design Decisions table:
    `| **workspace:* protocol** | Metro resolves the design system package as a local symlink without file: path fragility |`

    REMOVAL 9 — Roadmap table row (line 313):
    Remove the showcase app row from the Roadmap table:
    `| React Native Paper showcase app (20 items) | Complete |`
    Add a new row in its place showing abandonment:
    `| React Native Paper showcase app | Abandoned — moved to separate repository |`

    ADDITION — Update the repo description paragraph in the Overview section (the blockquote starting with "> **This repository**"):
    After editing, the blockquote must read:
    > **This repository** is the standalone design system package: a Style Dictionary v4 token pipeline that compiles W3C Design Token JSON into typed TypeScript constants and a React Native Paper MD3 theme. Storybook provides visual documentation for all token categories and component/screen designs. The React Native app that consumes this design system lives in a separate repository.

    After all edits, verify no `#app-screens` anchor remains in the nav bar, no `apps/showcase` path appears in the file (use grep to check), and the file renders valid markdown (all tables have consistent column counts, all code blocks are closed).
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('README.md','utf8'); const checks=[['Expo_SDK badge removed',!r.includes('Expo_SDK-57')],['App Screens nav removed',!r.includes('#app-screens')],['App Screens section removed',!r.includes('## App Screens')],['Showcase App section removed',!r.includes('## Component Showcase App')],['Quick Start showcase removed',!r.includes('### Showcase App (React Native)')],['apps/showcase path removed',!r.includes('apps/showcase')]]; let pass=true; checks.forEach(([name,ok])=>{if(!ok){console.error('FAIL:',name);pass=false;}else{console.log('PASS:',name);}}); if(!pass)process.exit(1); console.log('All README checks passed.');"</automated>
  </verify>
  <acceptance_criteria>
    - The node verification script exits 0 with "All README checks passed."
    - `grep -c "apps/showcase" README.md` outputs 0
    - `grep -c "#app-screens" README.md` outputs 0
    - `grep -c "Expo_SDK-57" README.md` outputs 0
    - The "## Design System Architecture", "## Token Pipeline", "## Quick Start", "## Project Structure", "## Accessibility", "## Roadmap" sections still exist
    - The nav bar links resolve to headings that exist in the file (no dangling anchors)
    - The Project Structure code block shows `├── apps/                   # Reserved for future packages` (not the showcase subtree)
    - The Roadmap table row for showcase shows "Abandoned — moved to separate repository"
    - The Overview blockquote mentions Storybook as documentation and references a separate repository for the RN app
  </acceptance_criteria>
  <done>README.md reflects design system scope only. No showcase references remain. Storybook and token pipeline content intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning docs, run done-bar, commit and push</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    Read .planning/STATE.md and .planning/ROADMAP.md before editing. STATE.md currently shows Phase 4 as the active phase with status "executing". ROADMAP.md already has Phase 4 marked ABANDONED (per the prior session commit babbe61) and Phase 5 as "Status: Planned". Verify the current text before overwriting.
  </read_first>
  <action>
    1. UPDATE STATE.md:
       - Change the `status:` frontmatter field to `complete`
       - Change the `last_updated:` field to today's date: 2026-08-05
       - Update `## Current Position` to:
         Phase: Phase 5 — Design System Cleanup
         Plan: 05-03 complete
         Status: Phase 5 COMPLETE — showcase removed, lockfile clean, README updated
       - Update the progress bar for Phase 5 (add a new line):
         execute-5   [██████████] 100% — Phase 5 COMPLETE (2026-08-05)
       - Add to `## Recent Decisions`:
         - Phase 5 COMPLETE (2026-08-05) — apps/showcase removed (29 files), apps/.gitkeep added, lockfile regenerated (589 extraneous packages removed), README.md pruned of all showcase references (9 locations), Phase 4 closed as ABANDONED
       - Clear `## Pending Todos` and `## Blockers / Concerns` — both should say "None."
       - Update `## Session Continuity`:
         Last session: 2026-08-05
         Stopped at: Phase 5 complete — design system cleanup done
         Next action: None — milestone v0.1 complete. Next phase: future milestone (dark mode, status colors, or npm publish).

    2. UPDATE ROADMAP.md:
       - Locate the Phase 5 entry. Change its status line from:
         **Status:** Planned — context gathered 2026-08-04
         TO:
         **Status:** COMPLETE (2026-08-05)
       - Replace "**Plans:** TBD (plan-phase next)" with:
         **Plans:** 3 plans
       - Add the plan list under Phase 5 after the Plans count line:
         Plans:
         - [x] 05-01-PLAN.md — Stage showcase deletions + add apps/.gitkeep
         - [x] 05-02-PLAN.md — Regenerate lockfile via npm install
         - [x] 05-03-PLAN.md — Update README + planning docs + done-bar + push
       - Update the last-updated footer line to:
         *Last updated: 2026-08-05 — Phase 5 complete; design system cleanup done*

    3. RUN DONE-BAR — execute all three verification commands and confirm each exits 0:
       npm run build:tokens
       npm test
       npm run build-storybook
       All three must pass before staging. If any fails, stop and report the error.

    4. STAGE specific files only — do NOT use git add -A or git add .:
       git add README.md
       git add ".planning/STATE.md"
       git add ".planning/ROADMAP.md"
       Verify: `git diff --cached --name-only` shows exactly these three files.

    5. COMMIT with this exact message (no Co-Authored-By line per project convention):
       docs(cleanup): update README and planning docs — Phase 5 complete

    6. PUSH to origin/main:
       git push origin main
       Confirm push succeeds (no error output, shows the remote tracking branch updated).
  </action>
  <verify>
    <automated>git log --oneline -3 && npm run build:tokens && npm test && npm run build-storybook</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -3` shows three commits: Plan 01 cleanup, Plan 02 lockfile, Plan 03 docs (most recent)
    - `npm run build:tokens` exits 0 with 4 output files produced
    - `npm test` exits 0 with 42 pass, 0 fail
    - `npm run build-storybook` exits 0 with "Storybook build completed successfully"
    - `git push origin main` completes without error
    - STATE.md frontmatter shows `status: complete`
    - ROADMAP.md Phase 5 entry shows `**Status:** COMPLETE (2026-08-05)`
    - ROADMAP.md Phase 5 shows all three plan checkboxes checked ([x])
    - `git status` shows working tree clean (only the three untracked files remain as `??`)
  </acceptance_criteria>
  <done>Phase 5 complete. STATE.md, ROADMAP.md updated. All three verification commands pass. Changes pushed to origin/main. The repo is a clean design system package with no showcase artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git staging area → commit | Only the three named files (README.md, STATE.md, ROADMAP.md) must be staged |
| local → origin/main | Push completes the phase; no force push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | README.md edit | mitigate | Run node verification script after edit; grep for showcase/Expo references before committing |
| T-05-06 | Tampering | git staging | mitigate | Stage only README.md, STATE.md, ROADMAP.md by explicit path; verify with git diff --cached --name-only |
| T-05-07 | Denial of Service | build-storybook regression | mitigate | Run full done-bar before staging; if any command fails, stop and report rather than committing |
</threat_model>

<verification>
Final phase done-bar (all must pass):
- `npm run build:tokens` exits 0
- `npm test` exits 0 (42 pass, 0 fail)
- `npm run build-storybook` exits 0
- `git log --oneline -3` shows three Phase 5 commits
- `git push origin main` succeeds
- `grep -c "apps/showcase" README.md` returns 0
- ROADMAP.md Phase 5 shows COMPLETE
- STATE.md status: complete
</verification>

<success_criteria>
Phase 5 is complete. The VoltVenture design system repository is a clean, self-contained design system package: token pipeline (Style Dictionary v4), generated TypeScript constants, React Native Paper MD3 theme, and Storybook documentation (28 stories across 8 token categories + 11 components + 9 screens). No showcase app artifacts remain. The repo is pushed to GitHub.
</success_criteria>

<output>
Create `.planning/phases/05-design-system-cleanup/05-03-SUMMARY.md` when done.
</output>
