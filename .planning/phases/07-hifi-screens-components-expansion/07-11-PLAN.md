---
phase: 7
plan: "07-11"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - stories/components/qr-viewfinder.stories.js
  - stories/components/hub-card.stories.js
  - stories/components/route-card.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "QR Viewfinder: colorGrey900 viewport, 210×210px frame, 4-corner accents (32×4 H + 4×32 V, colorActionPrimary), animated scan line"
    - "Hub Card: 72×72px cafe photo (#e8e8e8, radiusMd), info text column, colorGrey300 chevron, press feedback"
    - "Route Card: #e8e8e8 hero image (~180px), overlaid Range badge (#FFFFFFEE bg) + Time badge (#0F0F0FDD bg), Start Route green button"
    - "All three components: Default, Interactive (phone-framed), SourceCode exports"
    - "QR Viewfinder scan line CSS keyframe injected via document.head.appendChild"
  artifacts:
    - path: "stories/components/qr-viewfinder.stories.js"
      provides: "QR Viewfinder component (C-04)"
      exports: ["Default", "Interactive", "SourceCode"]
    - path: "stories/components/hub-card.stories.js"
      provides: "Hub Card component (C-07)"
      exports: ["Default", "Interactive", "SourceCode"]
    - path: "stories/components/route-card.stories.js"
      provides: "Route Card component (C-08)"
      exports: ["Default", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/components/route-card.stories.js"
      to: "generated/tokens.js"
      via: "colorActionPrimary|radiusLg"
      pattern: "#FFFFFFEE|#0F0F0FDD"
---

<objective>
Create qr-viewfinder.stories.js (C-04), hub-card.stories.js (C-07), and route-card.stories.js (C-08) as new component stories.

Purpose: QR Viewfinder is needed for QR Unlock Scan screen. Hub Card is needed for Discover VIP Hubs screen. Route Card is needed for Curated Routes screen. All three are Wave 2 dependencies for Wave 3/5 screens.
Output: Three new component story files in stories/components/.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- QR corner accent dimensions: H=32×4px, V=4×32px (different from ID Scan's 28×3) -->
<!-- rgba(198,255,45,0.80) = #C6FF2DCC — QR scan line (identical to qr-unlock-scan screen) -->
<!-- Hub Card: bottom divider colorGrey100, press bg colorGrey050 on pointerdown -->
<!-- Route Card hero badge backgrounds: #FFFFFFEE = rgba(255,255,255,0.93), #0F0F0FDD = rgba(15,15,15,0.87) -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Create qr-viewfinder.stories.js — C-04 QR viewfinder with animated scan line</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/qr-unlock-scan.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Create stories/components/qr-viewfinder.stories.js. export default { title: 'Components/QrViewfinder' }.

    EXPORT: Default (HTML string).
    Outer viewport: width:393px, height:260px, background:tokens.colorGrey900 (#1A1A1A), position:relative, overflow:hidden, display:flex, align-items:center, justify-content:center.
    QR Frame: position:absolute, width:210px, height:210px, border:1px solid rgba(255,255,255,0.20). Centered via top:50%, left:50%, transform:translate(-50%,-50%).
    4 corner accent groups (position:absolute, background:tokens.colorActionPrimary):
      Per corner: H bar = 32×4px, V bar = 4×32px. Apply to all 4 corners of the QR frame boundary.
    Scan Line: position:absolute, width:178px, height:2px, background:rgba(198,255,45,0.80), centered within QR frame.
    Instructions Banner: position:absolute, bottom:0, width:393px, height:40px, background:rgba(0,0,0,0.53), display:flex, align-items:center, justify-content:center.
      Text "Hold QR code steady" — white typeLabelSm.

    EXPORT: Interactive (returns DOM element).
    makePhoneFrame() inline. Inner screen background override to #0f0f0f (dark phone frame for this component since it's camera content).
    Build viewport using createElement. Copy corner accent logic.
    Inject CSS keyframe:
      @keyframes qrScanBounce { 0%,100%{top:calc(50% - 90px)} 50%{top:calc(50% + 80px)} }
      Apply to scan line: animation:qrScanBounce 1.8s ease-in-out infinite.
    Also inject: @keyframes cornerPulse { 0%,100%{opacity:1} 50%{opacity:0.6} }
      Apply to all corner accent elements: animation:cornerPulse 2s ease-in-out infinite.
    Return frame (DOM element).

    EXPORT: SourceCode. _esc()/_blk(). RN Paper JSX.
  </action>
  <acceptance_criteria>
    - stories/components/qr-viewfinder.stories.js exists
    - `node --input-type=module --eval "import './stories/components/qr-viewfinder.stories.js'" 2>&1` exits 0
    - Exports Default, Interactive, SourceCode (all PascalCase)
    - File contains 'rgba(198,255,45,0.80)' for scan line
    - File contains '210' (QR frame dimension)
    - File contains '32' and '4' (corner accent sizes)
    - File contains keyframe animation for scan line bounce
  </acceptance_criteria>
</task>

<task id="T-02">
  <title>Create hub-card.stories.js — C-07 cafe hub list row</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Create stories/components/hub-card.stories.js. export default { title: 'Components/HubCard' }.

    EXPORT: Default (HTML string).
    Container: display:flex, align-items:center, padding:12px 16px, gap:12px, background:tokens.colorSurfaceBase, borderBottom:1px solid tokens.colorGrey100, box-sizing:border-box, cursor:pointer.
    Cafe Photo: 72×72px div, background:#e8e8e8, borderRadius:tokens.radiusMd, flex-shrink:0. (Image placeholder.)
    Hub Info (flex:1):
      Hub Name: "The Grind" — typeBodyMd, colorTextPrimary, fontWeight:600.
      Distance + type: "☕ VIP Hub · 0.4 km away" — typeLabelSm, colorTextSecondary.
      Slots: "6 charging slots available" — typeLabelSm, colorTextAccent (colorGreen700 = #7D9220).
    Chevron: "›" colorGrey300, fontSize:20px, flex-shrink:0.

    EXPORT: Interactive (returns DOM element).
    makePhoneFrame() inline. Inner screen #ffffff.
    Show 4 hub card rows stacked (simulating the Discover VIP Hubs list):
      Row 1: "The Grind", "☕ VIP Hub · 0.4 km"
      Row 2: "Sunrise Cafe", "☕ VIP Hub · 0.8 km"
      Row 3: "Urban Bean", "☕ VIP Hub · 1.2 km"
      Row 4: "Lotus Coffee", "☕ VIP Hub · 1.6 km"
    Each row: pointerdown → backgroundColor = tokens.colorGrey050; pointerup/leave → colorSurfaceBase.
    Return frame.

    EXPORT: SourceCode. _esc()/_blk(). RN Paper JSX. Literals: '#e8e8e8' (photo placeholder), '#F5F5F5' (colorGrey100 divider).
  </action>
  <acceptance_criteria>
    - stories/components/hub-card.stories.js exists
    - `node --input-type=module --eval "import './stories/components/hub-card.stories.js'" 2>&1` exits 0
    - Exports Default, Interactive, SourceCode (all PascalCase)
    - File contains '72' (cafe photo size)
    - File contains '#e8e8e8' (photo placeholder)
    - File contains 'colorGrey050' or press feedback logic
    - File contains at least 4 hub name strings in Interactive
  </acceptance_criteria>
</task>

<task id="T-03">
  <title>Create route-card.stories.js — C-08 route hero card with overlaid badges</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Create stories/components/route-card.stories.js. export default { title: 'Components/RouteCard' }.

    EXPORT: Default (HTML string).
    Card container: background:tokens.colorSurfaceBase, borderRadius:tokens.radiusLg, overflow:hidden, box-sizing:border-box, marginBottom:16px.

    Hero Image (position:relative, width:100%, height:180px, background:#e8e8e8, flex-shrink:0):
      Photo Gradient overlay: position:absolute, bottom:0, width:100%, height:80px, background:linear-gradient(to top, rgba(0,0,0,0.50), transparent).
      Range Badge (position:absolute, top:12px, left:12px): background:rgba(255,255,255,0.93), borderRadius:tokens.radiusFull, padding:4px 10px, "⚡ 100 km range" — typeLabelSm, colorTextPrimary, fontWeight:600.
      Time Badge (position:absolute, top:12px, right:12px): background:rgba(15,15,15,0.87), borderRadius:tokens.radiusFull, padding:4px 10px, "⏱ 45 min" — typeLabelSm, color:#ffffff, fontWeight:600.
      Title Overlay (position:absolute, bottom:12px, left:12px): "Old Town Loop" — typeBodyMd, color:#ffffff, fontWeight:700.

    Card Body (padding:16px):
      Meta Row (display:flex, gap:8px, marginBottom:8px):
        Difficulty chip: background:tokens.colorGrey100, borderRadius:tokens.radiusFull, padding:4px 10px, "🟢 Easy" — typeLabelSm, colorTextPrimary.
        Category chip: background:tokens.colorGrey100, "🏙 City Tour" — typeLabelSm.
      Stops Label: "3 stops" — typeLabelSm, colorTextSecondary, marginBottom:6px.
      Stops Row (display:flex, gap:6px):
        3 stop chips (background:tokens.colorGrey050, borderRadius:tokens.radiusFull, padding:4px 10px): "Town Hall", "Market", "River Park" — typeLabelSm, colorTextPrimary.
      Start Route Button (marginTop:12px, height:48px, full-width, background:tokens.colorActionPrimary, borderRadius:tokens.radiusFull, "▶ Start Route" — colorTextPrimary, typeHeadingSm, fontWeight:600, border:none, cursor:pointer).

    EXPORT: Interactive (returns DOM element).
    makePhoneFrame() inline. Inner screen #ffffff.
    Show 2 route cards stacked in the phone (scrollable container).
    Start Route button press state on each: pointerdown → colorGreen600, scale(0.97); up/leave → reset.
    Return frame.

    EXPORT: SourceCode. _esc()/_blk(). RN Paper JSX. Literals: '#FFFFFFEE' = 'rgba(255,255,255,0.93)', '#0F0F0FDD' = 'rgba(15,15,15,0.87)', '#e8e8e8', '#C6FF2D'.
  </action>
  <acceptance_criteria>
    - stories/components/route-card.stories.js exists
    - `node --input-type=module --eval "import './stories/components/route-card.stories.js'" 2>&1` exits 0
    - Exports Default, Interactive, SourceCode (all PascalCase)
    - File contains 'rgba(255,255,255,0.93)' or '#FFFFFFEE' for range badge bg
    - File contains 'rgba(15,15,15,0.87)' or '#0F0F0FDD' for time badge bg
    - File contains '180' (hero image height)
    - File contains 'Start Route' button label
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>node --input-type=module --eval "import './stories/components/qr-viewfinder.stories.js'; import './stories/components/hub-card.stories.js'; import './stories/components/route-card.stories.js'" 2>&1</command>
  <expected>Exits 0 (no parse errors on all three files)</expected>
</verification>

<success_criteria>
- qr-viewfinder: 210×210 QR frame, 32×4+4×32 corner accents, animated scan line, dark viewport
- hub-card: 72×72 cafe photo (#e8e8e8), info column, chevron, press feedback on all 4 rows
- route-card: 180px hero (#e8e8e8), Range badge (rgba(255,255,255,0.93)), Time badge (rgba(15,15,15,0.87)), Start Route CTA
- All three: phone-framed Interactive, SourceCode
- No parse errors on import
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-11-SUMMARY.md` when done
</output>
