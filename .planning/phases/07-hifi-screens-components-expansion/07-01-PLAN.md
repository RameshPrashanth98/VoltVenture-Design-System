---
phase: 7
plan: "07-01"
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "npm run build-storybook exits 0 from the current baseline"
    - "11 component story files exist in stories/components/"
    - "9 screen story files exist in stories/screens/"
    - "generated/tokens.js is importable and exports all token constants needed for Phase 7"
  artifacts:
    - path: "storybook-static/"
      provides: "Proof that the current baseline builds successfully"
  key_links:
    - from: "stories/**/*.stories.js"
      to: "generated/tokens.js"
      via: "import * as tokens from '../../generated/tokens.js'"
      pattern: "import.*generated/tokens"
---

<objective>
Preflight verification before any Phase 7 story writing begins.

Purpose: Confirm the Storybook build passes with the current Phase 6 baseline, verify exact file counts, and confirm all token constants needed in Phase 7 are accessible from generated/tokens.js. No story files are created or modified in this plan.
Output: Build confirmation + token availability report. Any discrepancy blocks subsequent waves.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
</context>

<tasks>

<task id="T-01">
  <title>Verify Storybook build baseline and file counts</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/package.json
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.storybook/main.js
  </read_first>
  <action>
    Run the following verification checks from the project root (D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/):

    1. Count component story files: `ls stories/components/*.stories.js | wc -l` — expect 11.
    2. Count screen story files: `ls stories/screens/*.stories.js | wc -l` — expect 9.
    3. Run `npm run build-storybook` — must exit 0 and produce storybook-static/ directory.
    4. Run `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts` immediately after build if build-storybook internally calls build:tokens. (Confirmed lib/ mutation guard per D-13.)

    If build fails: read the error output and report the blocker. Do NOT proceed to Wave 1 until build passes.
    If file counts differ from expected: list actual files and report discrepancy.
  </action>
  <acceptance_criteria>
    - `npm run build-storybook` exits 0
    - storybook-static/ directory exists after build
    - stories/components/ contains exactly 11 .stories.js files
    - stories/screens/ contains exactly 9 .stories.js files
    - No TypeScript or ESM parse errors in build output
  </acceptance_criteria>
</task>

<task id="T-02">
  <title>Confirm Phase 7 token constants are present in generated/tokens.js</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/generated/tokens.js
  </read_first>
  <action>
    Run a Node.js one-liner to verify all token constants required by Phase 7 stories are exported from generated/tokens.js:

    ```
    node --input-type=module --eval "
      import * as t from './generated/tokens.js';
      const required = [
        'colorActionPrimary','colorSurfaceBase','colorSurfaceInverse',
        'colorTextPrimary','colorTextSecondary','colorGrey050','colorGrey100',
        'colorGrey200','colorGrey300','colorGrey500','colorGrey700','colorGrey800','colorGrey900',
        'colorGreen100','colorGreen600','colorTextAccent',
        'colorBorderSubtle','colorTextOnInverse','colorTextDisabled',
        'radiusFull','radiusLg','radiusMd','radiusXl',
        'elevationFloating','elevationRaised',
        'space100','space200','space300','space400','space500','space600','space800','space1200',
        'typeLabelSm','typeLabelMd','typeBodyMd','typeHeadingSm','typeHeadingMd','typeHeadingLg'
      ];
      const missing = required.filter(k => t[k] === undefined);
      if (missing.length) { console.error('MISSING:', missing); process.exit(1); }
      console.log('ALL PRESENT — token audit passed');
    "
    ```

    If any token is missing: document which ones and note that those screens must hardcode the hex value directly. The researcher already confirmed all tokens are present — this task is a final gate before execution begins.
  </action>
  <acceptance_criteria>
    - Node command exits 0 and prints "ALL PRESENT — token audit passed"
    - OR: missing tokens are documented with their fallback hex values for use by subsequent waves
    - generated/tokens.js is importable as an ESM module (no parse errors)
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>npm run build-storybook</command>
  <expected>Exits 0; storybook-static/ directory produced; no parse errors</expected>
</verification>

<success_criteria>
- Storybook build passes at Phase 6 baseline
- 11 component + 9 screen story files confirmed
- All Phase 7 token constants verified present
- Wave 1 plans are unblocked
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-01-SUMMARY.md` when done
</output>
