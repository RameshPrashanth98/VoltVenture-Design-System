---
phase: 7
plan: "07-08"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - stories/components/settings-row.stories.js
  - stories/components/faq-row.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Settings Row: Default (chevron), WithToggle (50×29px pill, colorActionPrimary on / colorGrey200 off), WithValue variants"
    - "Settings Row Interactive: toggle cycles On↔Off, press highlight colorGrey050 on pointerdown"
    - "FAQ Row: Collapsed (ChevronDown colorGrey300) and Expanded (answer visible, ChevronUp colorTextPrimary) states"
    - "FAQ Row Interactive: click expands/collapses, chevron rotates 0°→90°"
    - "Both component stories follow Phase 6 pattern: Default, Interactive (phone-framed), SourceCode"
    - "Icon chip: 30–34px, colorGrey100 background"
  artifacts:
    - path: "stories/components/settings-row.stories.js"
      provides: "Settings Row component story (C-01)"
      exports: ["Default", "WithToggle", "WithValue", "Interactive", "SourceCode"]
    - path: "stories/components/faq-row.stories.js"
      provides: "FAQ Row component story (C-12)"
      exports: ["Collapsed", "Expanded", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/components/settings-row.stories.js"
      to: "generated/tokens.js"
      via: "import * as tokens"
      pattern: "colorGrey100|colorActionPrimary"
---

<objective>
Create settings-row.stories.js (C-01) and faq-row.stories.js (C-12) as new component stories.

Purpose: These two components are dependencies for Wave 3 screen stories (Settings, Profile, Login & Security, Preferences use settings-row; Support uses faq-row). Must be built in Wave 2 before those screens.
Output: Two new component story files in stories/components/, each with phone-framed Interactive exports.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- Settings Row anatomy: 48px min-height, display:flex, align-items:center, padding:0 16px, gap:12px -->
<!-- Left: RowIconChip (30-34×30-34px, colorGrey100 bg, colorGrey700 icon emoji, borderRadius:radiusSm) -->
<!-- Center: Text Col (title colorTextPrimary, optional sub colorTextSecondary, flex:1) -->
<!-- Right: ChevronIcon ("›" colorGrey300) OR toggle pill OR value text -->
<!-- Toggle pill: 50×29px (on=colorActionPrimary, off=colorGrey200), knob=21px circle (#ffffff), translate 0→21px -->

<!-- FAQ Row anatomy: padding:16px 0, display:flex, flex-direction:column -->
<!-- Question Row: display:flex, align-items:center, cursor:pointer -->
<!--   FaqQ: flex:1, typeBodyMd, colorTextPrimary -->
<!--   Chevron: "›" rotated 0° (collapsed) or 90° (expanded) via CSS transform -->
<!-- Answer: display:none (collapsed) or display:block (expanded), typeBodyMd, colorTextSecondary -->
<!-- Divider: borderBottom:1px solid colorGrey100 -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Create settings-row.stories.js — C-01 Settings Row component</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/tab-bar.stories.js
  </read_first>
  <action>
    Create stories/components/settings-row.stories.js. export default { title: 'Components/SettingsRow' }.

    EXPORT: Default (HTML string) — chevron variant.
    Container: display:flex, align-items:center, min-height:48px, padding:12px 16px, gap:12px, background:colorSurfaceBase, borderBottom:1px solid tokens.colorGrey100, box-sizing:border-box.
    RowIconChip: 34×34px div, background:tokens.colorGrey100, borderRadius:tokens.radiusSm, display:flex, align-items:center, justify-content:center, "⚙" emoji at fontSize:18px.
    Text Col (flex:1): Title "Account Settings" colorTextPrimary typeBodyMd fontWeight:600 + Sub "Manage your account" colorTextSecondary typeLabelSm.
    Chevron: "›" colorGrey300, fontSize:20px.

    EXPORT: WithToggle (HTML string) — toggle switch variant (ON state shown).
    Same layout but right side = toggle pill instead of chevron.
    Toggle pill: width:50px, height:29px, background:tokens.colorActionPrimary, borderRadius:tokens.radiusFull, position:relative, display:inline-flex, align-items:center, padding:0 4px.
    Knob: 21×21px circle (background:#ffffff, borderRadius:50%, position:absolute, left:26px — ON position).
    Icon: "🔔" in chip. Label: "Push Notifications".

    EXPORT: WithValue (HTML string) — value string variant.
    Same layout but right side = value text "English" (colorTextSecondary, typeLabelSm) + "›" colorGrey300.
    Icon: "🌐". Label: "Language".

    EXPORT: Interactive (returns DOM element).
    makePhoneFrame() inline (402×874px, #0F0F0F, 44px radius, #ffffff inner screen).
    Show 3 rows in the phone frame:
      Row 1: Default/chevron row (with press feedback: pointerdown → colorGrey050 bg; up/leave → colorSurfaceBase).
      Row 2: WithToggle row (interactive toggle). let toggleOn = true. Toggle state:
        pointerdown on pill: toggleOn = !toggleOn.
        ON: pill.background = tokens.colorActionPrimary, knob.style.left = '26px'.
        OFF: pill.background = tokens.colorGrey200, knob.style.left = '4px'.
      Row 3: WithValue row (press feedback only).
    All three rows have pointerdown press highlight (colorGrey050 background).
    Return frame (DOM element).

    EXPORT: SourceCode (HTML string).
    _esc()/_blk() helpers inline. Show RN Paper JSX for all 3 variants.
    Key token literals: colorGrey100:'#F5F5F5', colorActionPrimary:'#C6FF2D', colorSurfaceBase:'#FFFFFF'.
  </action>
  <acceptance_criteria>
    - stories/components/settings-row.stories.js exists
    - `node --input-type=module --eval "import './stories/components/settings-row.stories.js'" 2>&1` exits 0
    - Exports Default, WithToggle, WithValue, Interactive, SourceCode (all PascalCase)
    - File contains '50' and '29' (toggle pill dimensions)
    - File contains '21' (knob size)
    - File contains 'colorGrey050' or 'colorGrey100' for press highlight
    - Interactive is a function (not a string export)
  </acceptance_criteria>
</task>

<task id="T-02">
  <title>Create faq-row.stories.js — C-12 FAQ Row component with expand/collapse</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/settings-row.stories.js
  </read_first>
  <action>
    Create stories/components/faq-row.stories.js. export default { title: 'Components/FaqRow' }.

    EXPORT: Collapsed (HTML string).
    Container: padding:16px 0, borderBottom:1px solid tokens.colorGrey100, cursor:pointer.
    Question Row: display:flex, align-items:center.
      FaqQ text (flex:1): "How is the price calculated?" — typeBodyMd, colorTextPrimary, fontWeight:600.
      Chevron: "›" style transform:rotate(0deg), color:tokens.colorGrey300, fontSize:20px, transition:transform 200ms.
    No answer visible.

    EXPORT: Expanded (HTML string).
    Same container. Question Row: FaqQ text + chevron at transform:rotate(90deg), color:tokens.colorTextPrimary.
    Answer text (marginTop:8px, display:block): "Rides are billed per minute based on the rate shown at ride start. Electricity surcharges may apply during peak hours. The final bill is displayed on the Ride Complete screen." — typeBodyMd or fontSize:14px, colorTextSecondary, lineHeight:1.5.

    EXPORT: Interactive (returns DOM element).
    makePhoneFrame() inline. Inner screen #ffffff.
    Show 4 FAQ rows in the phone frame (stacked):
      Row 1 (pre-expanded): "How is the price calculated?" — expanded state (answer visible, chevron at 90°).
      Row 2: "What if I can't find a charging hub?" — collapsed.
      Row 3: "Is my deposit refundable?" — collapsed.
      Row 4: "What happens if I'm in an accident?" — collapsed.
    Each row: let expanded = (row === row1). On pointerdown on question row:
      Toggle expanded. If expanding: answer.style.display = 'block', chevron.style.transform = 'rotate(90deg)', chevron.style.color = tokens.colorTextPrimary.
      If collapsing: answer.style.display = 'none', chevron.style.transform = 'rotate(0deg)', chevron.style.color = tokens.colorGrey300.
    State is per-row (independent toggles). DOM mutations via .style.* only.
    Return frame (DOM element).

    EXPORT: SourceCode (HTML string). _esc()/_blk() pattern.

    Answer text for each FAQ row:
      Row 2: "Charging hubs are visible on the map. If all nearby hubs are full, the app shows the next available hub within 2 km. You can also dock the bike at any authorised zone."
      Row 3: "Yes. The security deposit is fully refundable if the bike is returned in good condition to an authorised zone. Releases typically take 24–72 hours."
      Row 4: "Your safety is our priority. In case of an accident, press the SOS button in the Active Ride screen to alert emergency services. Report the incident through the in-app Support section."
  </action>
  <acceptance_criteria>
    - stories/components/faq-row.stories.js exists
    - `node --input-type=module --eval "import './stories/components/faq-row.stories.js'" 2>&1` exits 0
    - Exports Collapsed, Expanded, Interactive, SourceCode (all PascalCase)
    - File contains 'rotate(90deg)' for expanded chevron
    - File contains 'rotate(0deg)' for collapsed chevron
    - File contains at least 4 FAQ question strings
    - Interactive function toggles display style (display:block / display:none) on answer elements
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>node --input-type=module --eval "import './stories/components/settings-row.stories.js'; import './stories/components/faq-row.stories.js'" 2>&1</command>
  <expected>Exits 0 (no parse errors on both files)</expected>
</verification>

<success_criteria>
- settings-row: Default/WithToggle/WithValue/Interactive/SourceCode exports; toggle cycles colorActionPrimary ↔ colorGrey200
- faq-row: Collapsed/Expanded/Interactive/SourceCode exports; 4 FAQ rows with independent expand/collapse
- Both component stories follow Phase 6 conventions (phone-framed Interactive, DOM element return)
- No parse errors on import
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-08-SUMMARY.md` when done
</output>
