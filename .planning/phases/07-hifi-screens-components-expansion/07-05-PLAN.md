---
phase: 7
plan: "07-05"
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - stories/screens/qr-unlock-scan.stories.js
  - stories/screens/safety-mount.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "QR Unlock Scan: dark screen, 210×210px QR frame with 4-corner green accents (32×4 H + 4×32 V per corner)"
    - "QR Unlock Interactive: scan line animates up/down within the QR frame"
    - "Safety Mount: geometric illustration built from CSS shapes (no images)"
    - "Safety Mount Interactive: slider thumb moves right on tap; checkbox toggles green/grey"
    - "Both Interactive exports return DOM elements"
    - "QR Unlock inner screen overridden to #0f0f0f"
  artifacts:
    - path: "stories/screens/qr-unlock-scan.stories.js"
      provides: "QR Unlock Scan Hi-Fi story (frame pE4ag) — dark camera screen"
      exports: ["Default", "Interactive", "SourceCode"]
    - path: "stories/screens/safety-mount.stories.js"
      provides: "Safety Mount Hi-Fi story (frame L3K2a) — geometric illustration"
      exports: ["Default", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/screens/qr-unlock-scan.stories.js"
      to: "generated/tokens.js"
      via: "import * as tokens"
      pattern: "colorGrey900|colorActionPrimary"
---

<objective>
Create qr-unlock-scan.stories.js (frame pE4ag) and safety-mount.stories.js (frame L3K2a).

Purpose: QR Unlock is a dark camera screen with corner accent pattern similar to ID Scan but for a QR code frame. Safety Mount is the most complex Static-category screen — a geometric illustration of a phone mount built entirely from CSS shapes. Both are interaction category E (Static).
Output: Two new story files each with Default, Interactive, and SourceCode exports.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- QR corner accents (DIFFERENT from ID Scan): 32×4px H + 4×32px V per corner (larger than ID's 28×3) -->
<!-- #C6FF2DCC = rgba(198,255,45,0.80) — QR scan line -->
<!-- #FFFFFF12 = rgba(255,255,255,0.07) — very faint white (QR bottom panel chips) -->
<!-- #FFFFFF18 = rgba(255,255,255,0.094) — QR divider -->
<!-- Safety Mount: makePhoneFrame() inner screen stays #ffffff (light screen) -->
<!-- Swipe slider: 50×29px track (colorGrey100), 44×44px thumb (colorSurfaceInverse) -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Create qr-unlock-scan.stories.js from frame pE4ag — dark QR camera screen</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/id-scan.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Create stories/screens/qr-unlock-scan.stories.js. export default { title: 'Screens/QrUnlockScan' }.

    LAYOUT: width:393px, min-height:852px, flex-direction:column, background:tokens.colorSurfaceInverse.

    Status Bar (62px): white text on black.

    Top Navigation (52px): Close (rgba(255,255,255,0.13) circle) | "Scan QR Code" title white | Torch (rgba(255,255,255,0.13) circle).

    Camera Viewport (flex:1, background:tokens.colorGrey900 = #1A1A1A, position:relative, overflow:hidden):
      QR Frame: position:absolute, top:50%, left:50%, transform:translate(-50%,-55%), width:210px, height:210px. Border: 1px solid rgba(255,255,255,0.20).
      4-corner accents (position:absolute, relative to QR Frame, background:tokens.colorActionPrimary):
        Each corner: H = 32×4px, V = 4×32px. Arranged at all 4 corners (top-left, top-right, bottom-left, bottom-right).
      Scan Line: position:absolute, width:178px, height:2px, background:rgba(198,255,45,0.80), left:50%, transform:translateX(-50%), top:50% (static in Default; animated in Interactive).
      Instructions Banner: position:absolute, bottom:0, width:393px, height:48px, background:rgba(0,0,0,0.53), "Hold QR code steady inside the frame" — white typeLabelSm.

    Bottom Status Panel (background:tokens.colorGrey900, padding:20px 16px):
      Bike Reminder Chip: display:inline-flex, background:rgba(255,255,255,0.07), borderRadius:tokens.radiusFull, padding:6px 12px, "🚲 VV-4829" — white typeLabelSm.
      Scanning Status Row: "● Scanning..." — green dot (8×8px colorActionPrimary) + colorGrey500 text.
      Divider: 1px, rgba(255,255,255,0.094).
      Enter Code Button: width:100%, height:48px, background:rgba(255,255,255,0.07), borderRadius:tokens.radiusLg, color:#ffffff, "Enter Code Manually".

    INTERACTIVE EXPORT:
    makePhoneFrame() inline. screen.style.background = '#0f0f0f'. (Dark screen override.)
    Inject keyframe: @keyframes qrScan { 0%,100%{top:10px} 50%{top:190px} } on scan line.
    Also pulse the green scanning dot: @keyframes dotPulse { 0%,100%{opacity:1} 50%{opacity:0.3} }
    Apply dotPulse to the green dot indicator.
    Return frame.

    SOURCECODE EXPORT: _esc()/_blk() pattern.
  </action>
  <acceptance_criteria>
    - stories/screens/qr-unlock-scan.stories.js exists
    - `node --input-type=module --eval "import './stories/screens/qr-unlock-scan.stories.js'" 2>&1` exits 0
    - File contains 'rgba(198,255,45,0.80)' for QR scan line
    - File contains 'rgba(255,255,255,0.07)' for faint white chips
    - File contains '210' (QR frame dimension)
    - File contains '32' and '4' (corner accent dimensions)
    - File contains keyframe animation for scan line
    - PascalCase exports: Default, Interactive, SourceCode
  </acceptance_criteria>
</task>

<task id="T-02">
  <title>Create safety-mount.stories.js from frame L3K2a — geometric phone mount illustration</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/onboarding-1.stories.js
  </read_first>
  <action>
    Create stories/screens/safety-mount.stories.js. export default { title: 'Screens/SafetyMount' }.

    LAYOUT: width:393px, min-height:852px, display:flex, flex-direction:column, background:tokens.colorSurfaceBase.

    Status Bar (62px): standard light.

    Header Section (padding:20px 16px):
      Safety Badge pill (background:tokens.colorSurfaceInverse, color:#ffffff, padding:4px 12px, borderRadius:radiusFull): "🛡 Safety Step".
      Step Dots: 3× 8×8px circles (first two colorSurfaceInverse = active/done, third colorGrey300 = current step indicator).
      Main Title: "Mount Your Phone Securely" — typeHeadingMd, colorTextPrimary.
      Subtitle: "Attach before scanning your QR code." — typeBodyMd, colorTextSecondary.

    Mount Illustration (background:tokens.colorGrey100, padding:24px 0, display:flex, align-items:center, justify-content:center, position:relative):
      BG Circle: 148×148px, background:tokens.colorGrey200, borderRadius:50%, position:absolute, zIndex:0.
      Handlebar: 285×22px rect, background:tokens.colorSurfaceInverse, borderRadius:tokens.radiusMd, position:absolute, zIndex:1.
      Left Grip: 42×42px ellipse (background:tokens.colorGrey700, borderRadius:50%), overlapping handlebar left end.
      Right Grip: 42×42px ellipse (background:tokens.colorGrey700, borderRadius:50%), overlapping handlebar right end.
      Mount Clamp: 47×36px rect (background:tokens.colorGrey800, borderRadius:6px), centered on handlebar.
      Phone Body: 59×100px rect (background:tokens.colorSurfaceInverse, borderRadius:8px), above clamp.
      Phone Screen: 49×74px rect (background:tokens.colorGrey900, borderRadius:4px), inside phone body (centered).
      App Icon on Screen: "⚡" — colorActionPrimary, fontSize:17px, centered on phone screen.
      Secure Label: small pill (black bg, white text, lock icon + "Secured") — positioned bottom-center of illustration.

    Warning Card (background:tokens.colorGrey050, borderRadius:tokens.radiusLg, margin:16px, padding:16px):
      "⚠ Do not ride without securing your phone. Unsecured phones can be a hazard."
      colorTextSecondary, typeBodySm.

    Confirmation Area (padding:0 16px 32px, display:flex, flex-direction:column, gap:16px):
      Checkbox Row: 22×22px checkbox (background:tokens.colorActionPrimary, borderRadius:4px, "✓" black) + "I've safely mounted my phone" text (colorTextPrimary, typeBodyMd).
      Swipe Slider: position:relative, width:100%, height:29px, background:tokens.colorGrey100, borderRadius:tokens.radiusFull.
        Track label: "Slide to confirm →" — colorGrey500, typeLabelSm, centered.
        Thumb: 44×44px circle (background:tokens.colorSurfaceInverse, position:absolute, left:4px, top:-7px, borderRadius:50%).
      Secured CTA Button: 56px height, width:100%, background:tokens.colorActionPrimary, borderRadius:tokens.radiusFull, "🛡 Proceed to Scan" — disabled appearance (opacity:0.5) until slider moves in Interactive.

    No tab bar on this screen (it's a modal-style flow step).

    INTERACTIVE EXPORT:
    makePhoneFrame() inline. Inner screen stays #ffffff.
    Checkbox toggle: let checked = true. pointerdown on checkbox toggles background colorActionPrimary ↔ colorGrey200 and text "✓" ↔ "".
    Slider thumb: pointerdown on thumb sets thumb.style.left = 'calc(100% - 48px)'. Track label changes to "✓ Confirmed". CTA button opacity → 1.
    CTA press state: pointerdown → scale(0.97); pointerup/leave → scale(1).
    Return frame.

    SOURCECODE EXPORT: _esc()/_blk() pattern.
  </action>
  <acceptance_criteria>
    - stories/screens/safety-mount.stories.js exists
    - `node --input-type=module --eval "import './stories/screens/safety-mount.stories.js'" 2>&1` exits 0
    - File contains '285' (handlebar width)
    - File contains '59' and '100' (phone body dimensions)
    - File contains 'Slide to confirm'
    - File contains checkbox toggle logic (pointerdown on checkbox)
    - File contains slider thumb left position change ('calc(100%')
    - PascalCase exports: Default, Interactive, SourceCode
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>node --input-type=module --eval "import './stories/screens/qr-unlock-scan.stories.js'; import './stories/screens/safety-mount.stories.js'" 2>&1</command>
  <expected>Exits 0 (no parse errors on both files)</expected>
</verification>

<success_criteria>
- qr-unlock-scan: dark camera, 210×210px QR frame, 4-corner green accents (32×4 + 4×32), animated scan line
- safety-mount: geometric illustration (handlebar 285×22, phone body 59×100, grips, clamp), slider interaction, checkbox toggle
- Both Interactive exports return DOM elements
- QR inner screen #0f0f0f; Safety Mount inner screen #ffffff
- No parse errors on import
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-05-SUMMARY.md` when done
</output>
