---
phase: 7
plan: "07-12"
type: execute
wave: 3
depends_on: ["07-01", "07-08"]
files_modified:
  - stories/screens/registration.stories.js
  - stories/screens/login.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Registration rebuilt from Hi-Fi frame Y9ojN: 44×44px colorActionPrimary logo circle, Apple/Google social buttons, WhatsApp CTA (#25D366), checkbox"
    - "Login rebuilt from Hi-Fi frame TS9Td (2nd frame per D-03): Email/Phone segmented toggle, phone input row (colorGrey050), Continue button (colorSurfaceInverse = black)"
    - "Registration Interactive: checkbox toggles colorActionPrimary↔colorGrey200; Apple/Google press states; hidden phone input"
    - "Login Interactive: segmented toggle switches active segment; hidden phone input"
    - "WhatsApp button color is #25D366 — hardcoded, no VoltVenture token"
  artifacts:
    - path: "stories/screens/registration.stories.js"
      provides: "Registration screen rebuilt from Hi-Fi (frame Y9ojN)"
      exports: ["Default", "Interactive", "SourceCode"]
    - path: "stories/screens/login.stories.js"
      provides: "Login screen rebuilt from Hi-Fi (frame TS9Td)"
      exports: ["Default", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/screens/registration.stories.js"
      to: "generated/tokens.js"
      via: "colorActionPrimary|colorSurfaceBase"
      pattern: "#25D366"
---

<objective>
Rebuild registration.stories.js (frame Y9ojN) and login.stories.js (frame TS9Td) in-place from Hi-Fi designs.

Purpose: Both are existing files that must be rebuilt with Hi-Fi visual compositions per D-02 and D-04. Registration is an Auth screen (Category A — Form). Login uses frame TS9Td (second frame) per D-03. Both have form inputs and social auth patterns.
Output: Two rebuilt screen story files with Default, Interactive, and SourceCode exports replacing prior wireframe content.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- WhatsApp brand green: #25D366 — hardcoded, no VoltVenture token -->
<!-- Logo Section: 44×44px circle (colorActionPrimary bg, "V" black 22px) + "VoltVenture" text (typeHeadingSm) -->
<!-- Apple button: bg=colorSurfaceInverse (black), text="#ffffff", "  Sign up with Apple" -->
<!-- Google button: bg=colorSurfaceBase, border:1px solid colorGrey200, text=colorTextPrimary, "G  Sign up with Google" -->
<!-- OR Divider: 1px colorGrey200 lines + "OR" text centered, colorTextSecondary -->
<!-- Checkbox: 22×22px, colorActionPrimary when checked (green), colorGrey200 when unchecked, borderRadius:4px -->
<!-- Phone input: colorGrey050 bg (#FAFAFA), flag + phone number, no border visible -->
<!-- Login Continue button: colorSurfaceInverse bg (black), color:#ffffff — NOT green -->
<!-- Segmented toggle: 2 options (Email/Phone), colorGrey100 bg, active option: colorSurfaceInverse + white text -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Rebuild registration.stories.js from Hi-Fi frame Y9ojN</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/registration.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Rebuild stories/screens/registration.stories.js completely. export default { title: 'Screens/Registration' }.

    EXPORT: Default (HTML string, width:393px, min-height:852px, display:flex, flex-direction:column, background:tokens.colorSurfaceBase, padding:0 20px, box-sizing:border-box).

    Status Bar (62px flex-shrink:0): light — "9:41" colorTextPrimary + icons colorTextPrimary.

    Back Navigation (44px): "←" back arrow, 24×24px area, colorTextPrimary.

    Logo Section (text-align:center, padding:24px 0 16px):
      Logo Circle: 44×44px, background:tokens.colorActionPrimary, borderRadius:50%, display:flex, align-items:center, justify-content:center, "V" (fontSize:22px, fontWeight:700, color:tokens.colorTextPrimary). Centered.
      Brand Name: "VoltVenture" — typeHeadingSm or fontSize:17px, fontWeight:700, colorTextPrimary, marginTop:8px.

    Header Section (marginBottom:24px):
      Page Title: "Create Account" — typeHeadingMd, colorTextPrimary, fontWeight:700.
      Page Subtitle: "Join VoltVenture to start your first ride." — typeBodyMd, colorTextSecondary.

    Social Buttons Section (display:flex, flex-direction:column, gap:12px):
      Apple Button (56px height, full-width, background:tokens.colorSurfaceInverse, borderRadius:tokens.radiusFull, border:none, cursor:pointer, display:flex, align-items:center, justify-content:center, gap:8px):
        "  Continue with Apple" — color:#ffffff, typeHeadingSm, fontWeight:600.
      Google Button (56px height, full-width, background:tokens.colorSurfaceBase, border:1px solid tokens.colorGrey200, borderRadius:tokens.radiusFull, cursor:pointer, display:flex, align-items:center, justify-content:center, gap:8px):
        "G  Continue with Google" — color:tokens.colorTextPrimary, typeHeadingSm, fontWeight:600.

    OR Divider (display:flex, align-items:center, gap:12px, margin:20px 0):
      Left line: flex:1, height:1px, background:tokens.colorGrey200.
      "OR" text: colorTextSecondary, typeLabelSm.
      Right line: flex:1, height:1px, background:tokens.colorGrey200.

    WhatsApp Section:
      WhatsApp Button (56px height, full-width, background:#25D366, borderRadius:tokens.radiusFull, border:none, cursor:pointer, display:flex, align-items:center, justify-content:center, gap:8px):
        /* WhatsApp brand green — no VV token */
        "  Continue with WhatsApp" — color:#ffffff, typeHeadingSm, fontWeight:600.
      Email Link Row (44px, display:flex, align-items:center, justifyContent:center): "Continue with email" — colorTextSecondary, typeBodyMd, textDecoration:underline, cursor:pointer.
      Sign In Row (44px, text-align:center): "Already have an account? " + "Sign In" — colorTextAccent, typeBodyMd.

    Terms Section (display:flex, align-items:flex-start, gap:12px, marginTop:16px):
      Checkbox: 22×22px div, background:tokens.colorActionPrimary, borderRadius:4px, display:flex, align-items:center, justify-content:center, "✓" fontSize:12px, color:tokens.colorTextPrimary, cursor:pointer, flex-shrink:0.
      Terms text: fontSize:13px, colorTextSecondary, lineHeight:1.5, "By continuing, you agree to our Terms of Service and Privacy Policy".

    INTERACTIVE EXPORT:
    makePhoneFrame() inline. Inner screen #ffffff.
    Hidden phone input: position:absolute, opacity:0, top:-9999px — keyboard-capture pattern.
    Apple button press state: pointerdown → backgroundColor=tokens.colorGrey900, scale(0.97); up/leave → reset to colorSurfaceInverse.
    Google button press state: pointerdown → backgroundColor=tokens.colorGrey050; up/leave → reset.
    WhatsApp press state: pointerdown → backgroundColor='#1eb858', scale(0.97); up/leave → '#25D366'.
    Checkbox toggle: let checked = true. pointerdown → checked = !checked; checked: bg=colorActionPrimary + "✓"; unchecked: bg=colorGrey200 + "".
    Return frame.

    SOURCECODE EXPORT: _esc()/_blk(). RN Paper JSX. Key literals: '#25D366' (WhatsApp), '#C6FF2D' (colorActionPrimary), '#0F0F0F' (colorSurfaceInverse).
  </action>
  <acceptance_criteria>
    - stories/screens/registration.stories.js exists (rebuilt in-place)
    - `node --input-type=module --eval "import './stories/screens/registration.stories.js'" 2>&1` exits 0
    - File contains '#25D366' for WhatsApp button (with inline comment about brand color)
    - File contains 'colorActionPrimary' for logo circle and checkbox
    - File contains 'checkbox' or Checkbox toggle logic
    - File contains 'Sign In' and 'Terms' text
    - Default export returns string containing 'width:393px'
    - PascalCase exports: Default, Interactive, SourceCode
  </acceptance_criteria>
</task>

<task id="T-02">
  <title>Rebuild login.stories.js from Hi-Fi frame TS9Td (2nd frame — per D-03)</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/login.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/registration.stories.js
  </read_first>
  <action>
    Rebuild stories/screens/login.stories.js completely. export default { title: 'Screens/Login' }.
    Uses frame TS9Td per D-03 (discard yfZaz).

    EXPORT: Default (HTML string, same container as registration).

    Status Bar (62px): light.
    Back Navigation (44px): "←" back arrow.

    Main Content / Top Section (padding:24px 0):
      Header: "Welcome back" — typeBodyMd, colorTextSecondary.
      Brand Title: "VoltVenture" — typeHeadingLg, colorTextPrimary, fontWeight:700.
      Subtitle: "Sign in to continue your journey." — typeBodyMd, colorTextSecondary. marginBottom:24px.

      Social Buttons (same pattern as registration):
        Apple button (colorSurfaceInverse black, "  Sign in with Apple", white text).
        Google button (white border, "G  Sign in with Google", colorTextPrimary text).

      OR Divider (same pattern).

      Input Section:
        Method Toggle (segmented control, display:flex, background:tokens.colorGrey100, borderRadius:tokens.radiusFull, padding:4px, gap:4px):
          "Email" tab: initially active — background:tokens.colorSurfaceInverse, color:#ffffff, borderRadius:tokens.radiusFull, padding:8px 24px.
          "Phone" tab: inactive — background:transparent, color:tokens.colorTextSecondary, padding:8px 24px.
        Phone Input Row (marginTop:16px, background:tokens.colorGrey050, borderRadius:tokens.radiusLg, padding:12px 16px, display:flex, align-items:center, gap:8px):
          Flag: "🇮🇳 +91" — colorTextSecondary, typeLabelMd.
          Separator line: 1px colorGrey200, 24px tall.
          Input value: "98XXXXXXXX" — typeBodyMd, colorTextPrimary (placeholder text).
        Cached Hint row (marginTop:8px, display:flex, align-items:center, gap:6px): "⏱" + "Last used: phone ending in 29" — typeLabelSm, colorTextSecondary.

      Continue Login Button (marginTop:24px, 56px height, full-width, background:tokens.colorSurfaceInverse (#0F0F0F), borderRadius:tokens.radiusFull, border:none, cursor:pointer):
        "Continue →" — color:#ffffff, typeHeadingSm, fontWeight:600. Note: this is the BLACK button (not green).

    Sign Up Anchor (text-align:center, marginTop:16px):
      "Don't have an account? " + "Sign Up" — colorTextAccent, typeBodyMd.

    INTERACTIVE EXPORT:
    makePhoneFrame() inline. Inner screen #ffffff.
    Segmented toggle: let activeMethod = 'email'. On pointerdown on each option:
      Switch active: active option gets colorSurfaceInverse bg + white text; inactive gets transparent bg + colorTextSecondary.
    Continue button press state: pointerdown → backgroundColor=tokens.colorGrey800, scale(0.97); up/leave → reset to colorSurfaceInverse.
    Hidden phone input for keyboard capture.
    Return frame.

    SOURCECODE EXPORT: _esc()/_blk(). RN Paper JSX. Key literal: '#0F0F0F' for Continue button (black, not green).
  </action>
  <acceptance_criteria>
    - stories/screens/login.stories.js exists (rebuilt in-place)
    - `node --input-type=module --eval "import './stories/screens/login.stories.js'" 2>&1` exits 0
    - File contains 'colorSurfaceInverse' for Continue button (black CTA — NOT colorActionPrimary)
    - File contains 'Welcome back' or 'VoltVenture' header
    - File contains 'Email' and 'Phone' segmented toggle options
    - File contains 'colorGrey050' for phone input row background
    - Segmented toggle has click handler that switches active state
    - PascalCase exports: Default, Interactive, SourceCode
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>node --input-type=module --eval "import './stories/screens/registration.stories.js'; import './stories/screens/login.stories.js'" 2>&1</command>
  <expected>Exits 0 (no parse errors on both files)</expected>
</verification>

<success_criteria>
- Registration rebuilt: 44px logo circle, Apple/Google social buttons, WhatsApp (#25D366), checkbox toggle
- Login rebuilt from TS9Td: Email/Phone segmented toggle, colorGrey050 phone input, black Continue button (NOT green)
- Both Interactive: hidden phone input, button press states, DOM element returns
- No parse errors on import
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-12-SUMMARY.md` when done
</output>
