---
phase: 7
plan: "07-16"
type: execute
wave: 3
depends_on: ["07-01"]
files_modified:
  - stories/screens/edit-profile.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Avatar section: 76×76px black circle + 28×28px colorActionPrimary camera button (position:absolute bottom-right) + 'Change photo' text"
    - "Three form fields (FULL NAME, EMAIL ADDRESS, PHONE NUMBER): each with colorGrey500 label + optional VerifiedChip (colorGreen100) + colorGrey050 input row"
    - "Save Changes CTA: colorActionPrimary button with check icon"
    - "Interactive: hidden input overlays on each field; focus changes border color; Save press state"
    - "Interaction category: A — Form (per D-07)"
  artifacts:
    - path: "stories/screens/edit-profile.stories.js"
      provides: "Edit Profile screen (frame amAsI)"
      exports: ["Default", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/screens/edit-profile.stories.js"
      to: "generated/tokens.js"
      via: "colorGrey050|colorGreen100|colorActionPrimary"
      pattern: "colorGrey050|VerifiedChip"
---

<objective>
Create edit-profile.stories.js from Hi-Fi frame amAsI.

Purpose: Edit Profile is a form screen (Category A) where users can update their name, email, and phone. The avatar section has a camera button overlapping the circle (position:absolute). Three form fields with verified chips on email and phone. Interactive uses hidden input elements for keyboard capture.
Output: One new screen story file.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- Avatar Wrap: position:relative, display:inline-block -->
<!-- Avatar Circle: 76×76px, colorSurfaceInverse bg, borderRadius:50% -->
<!-- Camera Button: 28×28px circle, colorActionPrimary bg, position:absolute, bottom:0, right:0 -->
<!--   "📷" or "📸" icon, fontSize:14px -->
<!-- Form field structure: -->
<!--   Label Row: FieldLabel (typeLabelSm, colorGrey500, textTransform:uppercase) + optional VerifiedChip (colorGreen100 bg, colorTextAccent text, typeLabelSm, borderRadius:radiusFull, padding:2px 8px) -->
<!--   Input Row: colorGrey050 bg (#FAFAFA), borderRadius:radiusMd, padding:12px 16px, display:flex, align-items:center, gap:8px -->
<!--     FieldIcon (16px, colorGrey300) + FieldValue (typeBodyMd, colorTextPrimary) -->
<!-- Focus state: borderBottom or border changes from colorGrey200 to colorSurfaceInverse -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Create edit-profile.stories.js from Hi-Fi frame amAsI — form with avatar + 3 fields</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/registration.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Create stories/screens/edit-profile.stories.js. export default { title: 'Screens/EditProfile' }.

    LAYOUT: width:393px, min-height:852px, display:flex, flex-direction:column, background:tokens.colorSurfaceBase, box-sizing:border-box.

    Status Bar (62px): light.
    Header Row (44px, display:flex, align-items:center, padding:0 16px, gap:12px):
      Back Button: 36×36px colorGrey100 "←". Title "Edit Profile" typeHeadingMd colorTextPrimary fontWeight:700.

    Avatar Section (padding:24px 20px, display:flex, flex-direction:column, align-items:center, gap:12px):
      Avatar Wrap (position:relative, width:88px, height:88px):
        Avatar Circle: 76×76px, background:tokens.colorSurfaceInverse, borderRadius:50%, display:flex, align-items:center, justify-content:center, "👤" fontSize:34px, color:#ffffff.
        Camera Button: 28×28px circle, position:absolute, bottom:0, right:0, background:tokens.colorActionPrimary, borderRadius:50%, display:flex, align-items:center, justify-content:center, "📷" fontSize:13px, cursor:pointer.
      "Change photo" text: typeLabelSm, colorTextAccent (colorGreen700 = #7D9220), cursor:pointer.

    Form Section (padding:0 16px, display:flex, flex-direction:column, gap:16px):
      FIELD 1 — FULL NAME:
        Label Row (display:flex, align-items:center, justifyContent:space-between, marginBottom:6px):
          Label: "FULL NAME" — typeLabelSm, colorGrey500, textTransform:uppercase, letterSpacing:0.5px.
          (No VerifiedChip on Name field)
        Input Row (background:tokens.colorGrey050, borderRadius:tokens.radiusMd, padding:12px 16px, border:1px solid tokens.colorGrey200, display:flex, align-items:center, gap:8px, cursor:text):
          "👤" icon (colorGrey300, fontSize:16px). "Alex Johnson" (typeBodyMd, colorTextPrimary).

      FIELD 2 — EMAIL ADDRESS:
        Label Row:
          Label: "EMAIL ADDRESS" — typeLabelSm, colorGrey500, textTransform:uppercase.
          VerifiedChip: background:tokens.colorGreen100, borderRadius:tokens.radiusFull, padding:2px 8px, "✓ Verified" — typeLabelSm, colorTextAccent.
        Input Row (same bg/style):
          "✉" icon (colorGrey300, fontSize:16px). "alex.johnson@email.com" (typeBodyMd, colorTextPrimary).

      FIELD 3 — PHONE NUMBER:
        Label Row:
          Label: "PHONE NUMBER" typeLabelSm colorGrey500 uppercase.
          VerifiedChip: colorGreen100 "✓ Verified" colorTextAccent.
        Input Row: "📱" icon colorGrey300. "+91 98XXXXXXXX" typeBodyMd colorTextPrimary.

    Spacer (flex:1).

    CTA Wrap (padding:16px):
      Save Changes Button: 56px height, full-width, background:tokens.colorActionPrimary, borderRadius:tokens.radiusFull, border:none, cursor:pointer, display:flex, align-items:center, justify-content:center, gap:8px.
        "✓" icon (fontSize:16px) + "Save Changes" — colorTextPrimary, typeHeadingSm, fontWeight:600.

    INTERACTIVE EXPORT:
    makePhoneFrame() inline. Inner screen #ffffff.
    For each input row, create a hidden native <input> (position:absolute, opacity:0, width:100%, height:100%, cursor:text) overlaid on the row. On focus of hidden input: change input row border to 1px solid tokens.colorSurfaceInverse. On blur: revert to colorGrey200. (Same technique as registration.stories.js Phase 3 pattern.)
    Camera button press: pointerdown → scale(0.90); up/leave → scale(1).
    Save Changes press state: pointerdown → backgroundColor=tokens.colorGreen600, scale(0.97); up/leave → reset.
    Return frame.

    SOURCECODE EXPORT: _esc()/_blk(). RN Paper JSX. Key: '#C6FF2D' (camera button), '#FAFAFA' (colorGrey050 input), '#F4FFD9' (VerifiedChip).
  </action>
  <acceptance_criteria>
    - stories/screens/edit-profile.stories.js exists
    - `node --input-type=module --eval "import './stories/screens/edit-profile.stories.js'" 2>&1` exits 0
    - File contains '76' and '28' (avatar and camera button sizes)
    - File contains 'colorGrey050' for input field backgrounds
    - File contains 'VerifiedChip' or 'Verified' text in email/phone rows
    - File contains 'colorActionPrimary' for camera button and Save CTA
    - Interactive contains hidden input elements (opacity:0, position:absolute pattern)
    - PascalCase exports: Default, Interactive, SourceCode
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>node --input-type=module --eval "import './stories/screens/edit-profile.stories.js'" 2>&1</command>
  <expected>Exits 0 (no parse errors)</expected>
</verification>

<success_criteria>
- Edit Profile: avatar (76px black) + camera button (28px colorActionPrimary, position:absolute), 3 form fields with colorGrey050 bg, email+phone have VerifiedChip (colorGreen100), Save CTA (colorActionPrimary)
- Interactive: hidden inputs for keyboard capture, focus border change, press states
- No parse errors
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-16-SUMMARY.md` when done
</output>
