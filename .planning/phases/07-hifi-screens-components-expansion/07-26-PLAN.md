---
phase: 7
plan: "07-26"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-11"
files_modified:
  - stories/screens/curated-routes.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Status Bar + Header with Screen Title and Screen Subtitle render"
    - "Range Pills Row shows 3 pills: 100km active (colorActionPrimary background), 200km and 300km inactive (white + border)"
    - "3 Route Cards scroll vertically: Old Town Loop, Coastal Sunset Ride, Mountain Pass Adventure"
    - "Each Route Card has #e8e8e8 hero image (~180px), Range badge (#FFFFFFEE bg), Time badge (#0F0F0FDD bg), title overlay, card body with meta/stops/Start Route button"
    - "Tab Bar renders with Discover tab active"
    - "Interactive: clicking a Range Pill updates active pill background to colorActionPrimary; inactive pills revert to white"
    - "Interactive: Start Route button press state changes background (colorActionPrimary → #a8de1a) and scale(0.97)"
  artifacts:
    - path: "stories/screens/curated-routes.stories.js"
      provides: "Curated Routes screen story (frame R1tiK)"
      exports: ["Default", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/screens/curated-routes.stories.js"
      to: "stories/components/route-card.stories.js"
      via: "route-card visual composition reference"
      pattern: "#FFFFFFEE|#0F0F0FDD|180"
    - from: "stories/screens/curated-routes.stories.js"
      to: "generated/tokens.js"
      via: "colorActionPrimary|colorSurfaceBase|colorGrey100"
      pattern: "tokens\\.colorActionPrimary"
---

<objective>
Create curated-routes.stories.js — the Curated Routes screen (frame R1tiK) — with Default, Interactive, and SourceCode exports.

Purpose: This is the final screen in the D-01 scope of 34 Hi-Fi screens. The screen is a scrollable list of Route Cards with Range Pill filtering and a Tab Bar. It depends on route-card (built in 07-11, Wave 2) as the visual composition reference.
Output: stories/screens/curated-routes.stories.js with 3 named exports.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- Route card visual composition (from 07-11):
  Hero Image: width:100%, height:180px, background:#e8e8e8
  Photo Gradient overlay: bottom:0, height:80px, linear-gradient(to top, rgba(0,0,0,0.50), transparent)
  Range Badge: position:absolute top:12px left:12px, bg rgba(255,255,255,0.93) = #FFFFFFEE, borderRadius:radiusFull, padding:4px 10px
  Time Badge: position:absolute top:12px right:12px, bg rgba(15,15,15,0.87) = #0F0F0FDD, borderRadius:radiusFull, padding:4px 10px, color:#ffffff
  Title Overlay: position:absolute bottom:12px left:12px, color:#ffffff fontWeight:700
  Card Body (padding:16px): Meta Row (difficulty chip + category chip) + Stops Label + Stops Row (stop chips) + Start Route button
  Start Route: background:colorActionPrimary, height:48px, full-width, borderRadius:radiusFull, border:none, cursor:pointer
-->
<!-- Range Pills active: background:tokens.colorActionPrimary (#C6FF2D), color:tokens.colorTextPrimary (#0F0F0F) -->
<!-- Range Pills inactive: background:#ffffff, border:1px solid tokens.colorGrey100, color:tokens.colorTextPrimary -->
<!-- Active pill sub-label: "Best range" or similar — typeLabelSm, colorTextSecondary beneath pill label -->
<!-- Tab Bar: tabs=['Ride','Discover','Wallet','Account'], Discover active (colorSurfaceInverse fill) -->
<!-- makePhoneFrame() inline: 402×874px, #0F0F0F bezel, 44px cornerRadius, inner screen #ffffff -->
<!-- hexToRgba not needed — use literal rgba() strings throughout -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Create curated-routes.stories.js — Default and SourceCode exports</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/route-card.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/home-map.stories.js
  </read_first>
  <action>
    Create `stories/screens/curated-routes.stories.js`.

    Top of file:
      `export default { title: 'Screens/CuratedRoutes' };`
      `import tokens from '../../generated/tokens.js';`

    EXPORT: Default — returns an HTML string (width:393px, min-height:852px layout).

    Structure (top to bottom):

    1. Status Bar (height:62px, display:flex, align-items:center, padding:0 16px, justify-content:space-between):
       Left: "9:41" — typeLabelSm, colorTextPrimary, fontWeight:600.
       Right: "● ▲ ▲" battery/signal icons — typeLabelSm, colorTextSecondary.

    2. Header (padding:16px 16px 8px):
       Screen Title: "Curated Routes" — typeHeadingLg, colorTextPrimary, fontWeight:700.
       Screen Subtitle: "Handpicked rides for every rider" — typeBodyMd, colorTextSecondary, marginTop:4px.

    3. Range Pills Row (display:flex, gap:8px, padding:0 16px 12px, overflowX:auto):
       Pill 1 (active): background:tokens.colorActionPrimary, color:tokens.colorTextPrimary, borderRadius:tokens.radiusFull, padding:8px 16px, border:none.
         Pill label: "100km" — typeLabelSm, fontWeight:600.
         Sub-label below: "Best range" — typeLabelSm, colorTextSecondary (inside pill via flex column).
       Pill 2: background:#ffffff, border:1px solid tokens.colorGrey100, color:tokens.colorTextPrimary, same borderRadius/padding. Label: "200km".
       Pill 3: same inactive style. Label: "300km".

    4. Header Divider: 1px solid tokens.colorGrey100, margin:0 16px.

    5. Route List (display:flex, flexDirection:column, gap:16px, padding:16px, overflowY:auto):
       3 Route Cards. Each card: background:tokens.colorSurfaceBase, borderRadius:tokens.radiusLg, overflow:hidden.

       Route 1 — "Old Town Loop":
         Hero (position:relative, width:100%, height:180px, background:#e8e8e8):
           Photo Gradient: position:absolute, bottom:0, width:100%, height:80px, background:linear-gradient(to top, rgba(0,0,0,0.50), transparent).
           Range Badge (position:absolute, top:12px, left:12px): background:rgba(255,255,255,0.93), borderRadius:tokens.radiusFull, padding:4px 10px. Text: "⚡ 32km" — typeLabelSm, colorTextPrimary, fontWeight:600.
           Time Badge (position:absolute, top:12px, right:12px): background:rgba(15,15,15,0.87), borderRadius:tokens.radiusFull, padding:4px 10px. Text: "⏱ 45 min" — typeLabelSm, color:#ffffff, fontWeight:600.
           Title Overlay (position:absolute, bottom:12px, left:12px): "Old Town Loop" — typeBodyMd, color:#ffffff, fontWeight:700.
         Card Body (padding:16px):
           Meta Row (display:flex, gap:8px, marginBottom:8px, flexWrap:wrap):
             Chip: background:tokens.colorGrey100, borderRadius:tokens.radiusFull, padding:4px 10px. Text: "🟢 Easy" — typeLabelSm.
             Chip: background:tokens.colorGrey100, borderRadius:tokens.radiusFull, padding:4px 10px. Text: "🏙 City Tour" — typeLabelSm.
           Stops Label: "3 stops" — typeLabelSm, colorTextSecondary, marginBottom:6px.
           Stops Row (display:flex, gap:6px, flexWrap:wrap):
             3 chips (background:tokens.colorGrey050, borderRadius:tokens.radiusFull, padding:4px 10px): "Town Hall", "Market", "River Park" — typeLabelSm, colorTextPrimary.
           Start Route Button (marginTop:12px, width:100%, height:48px, background:tokens.colorActionPrimary, borderRadius:tokens.radiusFull, border:none, cursor:pointer):
             Text: "▶ Start Route" — typeHeadingSm, colorTextPrimary, fontWeight:600.

       Route 2 — "Coastal Sunset Ride":
         Same structure. Range Badge: "⚡ 58km". Time Badge: "⏱ 1h 20 min". Title Overlay: "Coastal Sunset Ride".
         Meta: "🟡 Moderate", "🌊 Coastal". Stops: "4 stops". Stop chips: "Harbor", "Pier", "Lighthouse", "Dunes".

       Route 3 — "Mountain Pass Adventure":
         Same structure. Range Badge: "⚡ 89km". Time Badge: "⏱ 2h 10 min". Title Overlay: "Mountain Pass Adventure".
         Meta: "🔴 Challenging", "⛰ Mountain". Stops: "5 stops". Stop chips: "Base Camp", "Vista Point", "Ridge", "Summit", "Valley".

    6. Tab Bar (position:sticky bottom fixed look — use display:flex, borderTop:1px solid tokens.colorGrey100, background:tokens.colorSurfaceBase, height:60px):
       4 tabs: Ride, Discover (active — colorSurfaceInverse bg chip or bold text), Wallet, Account.
       Each tab: display:flex, flexDirection:column, align-items:center, justifyContent:center, flex:1.
       Active tab (Discover): fontWeight:700, colorTextPrimary.
       Inactive tabs: colorTextSecondary.

    EXPORT: SourceCode — returns an HTML string with a `<pre><code>` block containing RN Paper JSX as plain text. Use _esc() helper (escapes < > & for HTML embedding) and _blk() (returns the pre/code wrapper). Show FlatList + PressableRangePill + RouteCard + TabBar import structure. Reference tokens from lib/voltventure_tokens.ts. Do NOT import lib/ in the story file — the SourceCode string is a static display.
  </action>
  <acceptance_criteria>
    - stories/screens/curated-routes.stories.js exists
    - `node --input-type=module --eval "import './stories/screens/curated-routes.stories.js'" 2>&1` exits 0
    - File exports named functions Default and SourceCode (PascalCase)
    - File contains 'rgba(255,255,255,0.93)' for Range badge background
    - File contains 'rgba(15,15,15,0.87)' for Time badge background
    - File contains '180' (hero image height)
    - File contains 'Old Town Loop', 'Coastal Sunset Ride', 'Mountain Pass Adventure'
    - File contains 'CuratedRoutes' in the default export title
  </acceptance_criteria>
</task>

<task id="T-02">
  <title>Add Interactive export to curated-routes.stories.js</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/home-map.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/route-card.stories.js
  </read_first>
  <action>
    Append to `stories/screens/curated-routes.stories.js` the Interactive named export (per D-05/D-06).

    EXPORT: Interactive — returns a DOM element (never an HTML string).

    1. Copy makePhoneFrame() inline helper (402×874px, #0F0F0F bezel, 44px cornerRadius, inner screen #ffffff). Pattern from Phase 6.

    2. Build screen DOM using document.createElement. Inner screen content (width:393px, height:874px, overflowY:auto, display:flex, flexDirection:column, background:#ffffff):

    a. Status Bar element (same layout as Default).

    b. Header element (title + subtitle).

    c. Range Pills Row: Create 3 pill elements. Track active state in a plain JS variable: `let activePill = 0;`.
       Each pill button (type="button"): pointerdown fires updateActivePill(index).
       updateActivePill(i): loops over pillEls array — sets active pill background to tokens.colorActionPrimary (#C6FF2D), others to #ffffff with border:1px solid tokens.colorGrey100. Mutates .style.background and .style.border only — never rebuilds innerHTML.

    d. Header Divider.

    e. Route List: 3 route card elements. Each is built with createElement calls matching the Default structure (hero image, overlaid badges, card body, Start Route button).
       For each Start Route button:
         pointerdown: button.style.background = '#a8de1a'; button.style.transform = 'scale(0.97)'.
         pointerup + pointerleave: button.style.background = tokens.colorActionPrimary; button.style.transform = 'scale(1)'.
       State held in per-button closure — no shared variable needed (per D-08).

    f. Tab Bar element (same 4 tabs, Discover active).

    3. Append all child elements to the inner screen (not with innerHTML).

    4. Call makePhoneFrame(), append screen as its inner content, return the frame (DOM element).

    Reminder: NEVER use innerHTML after initial construction to update state. Use .style.* only (per D-08).
  </action>
  <acceptance_criteria>
    - `node --input-type=module --eval "import('./stories/screens/curated-routes.stories.js').then(m => { const el = m.Interactive(); if (el.nodeType !== 1) throw new Error('Not DOM element'); })"` exits 0
    - File exports named function Interactive (PascalCase)
    - File contains 'makePhoneFrame' inline helper
    - File contains 'activePill' variable for range pill state
    - File contains 'pointerdown' event listeners (range pills + Start Route buttons)
    - File contains '#a8de1a' (Start Route pressed state)
    - Interactive() returns a DOM element with nodeType === 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Story file → Storybook renderer | HTML strings and DOM elements are rendered in the Storybook iframe sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-26-01 | Tampering | stories/screens/curated-routes.stories.js | accept | Static story file; no user input, no network calls, no secrets. Low-value target. |
| T-07-26-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — imports only from generated/tokens.js (already in repo). |
</threat_model>

<verification>
  <automated>node --input-type=module --eval "import('./stories/screens/curated-routes.stories.js').then(m => { if (typeof m.Default !== 'function') throw new Error('Missing Default'); if (typeof m.Interactive !== 'function') throw new Error('Missing Interactive'); if (typeof m.SourceCode !== 'function') throw new Error('Missing SourceCode'); const el = m.Interactive(); if (el.nodeType !== 1) throw new Error('Interactive must return DOM element'); console.log('OK'); })"</automated>
</verification>

<success_criteria>
- stories/screens/curated-routes.stories.js exists with Default, Interactive, SourceCode exports
- Default renders Status Bar, Header, 3 Range Pills (100km active), Divider, 3 Route Cards, Tab Bar (Discover active)
- Interactive returns DOM element; makePhoneFrame() inline; Range pill click updates active pill styling; Start Route button shows press state (#a8de1a, scale:0.97)
- SourceCode returns pre/code block with RN Paper JSX string
- Node.js parse check exits 0
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-26-SUMMARY.md` when done
</output>
