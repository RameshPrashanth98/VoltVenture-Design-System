---
phase: 7
plan: "07-02"
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - stories/screens/splash.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Splash screen story exists at stories/screens/splash.stories.js"
    - "Default export returns HTML string with white background and centered V logo badge"
    - "Interactive export returns a DOM element with loader fill CSS keyframe animation"
    - "SourceCode export returns a pre/code block with RN Paper JSX string"
    - "All named exports are PascalCase"
  artifacts:
    - path: "stories/screens/splash.stories.js"
      provides: "Splash screen Hi-Fi story (frame O94n2)"
      exports: ["Default", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/screens/splash.stories.js"
      to: "generated/tokens.js"
      via: "import * as tokens from '../../generated/tokens.js'"
      pattern: "import.*generated/tokens"
---

<objective>
Rebuild stories/screens/splash.stories.js from Hi-Fi frame O94n2 (Splash Screen — Hi-Fi).

Purpose: Replace the Phase 3 wireframe version with the full Hi-Fi design. The splash screen is white-background with a centered logo badge, brand name, divider, tagline, and progress loader bar. Interaction category: E — Static (animated loader fill only).
Output: stories/screens/splash.stories.js with Default, Interactive, and SourceCode exports.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-UI-SPEC.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- makePhoneFrame() pattern — copy verbatim from button.stories.js, add screenBg param -->
<!-- For splash: inner screen background stays #ffffff (light variant) -->
<!-- _esc() and _blk() helpers from button.stories.js lines 154-156 -->
<!-- Token import: import * as tokens from '../../generated/tokens.js' -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Rebuild splash.stories.js from Hi-Fi frame O94n2</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/splash.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Rebuild stories/screens/splash.stories.js as a full replacement. All three exports required.

    EXPORT: Default (HTML string, width:393px, min-height:852px)
    Background: white (tokens.colorSurfaceBase).
    Background Pattern layer (opacity:0.08): 8 thin angled decorative rectangles (3×40px, colorGrey200 fill) scattered across the canvas at various rotations — approximates the SVG route path pattern in frame O94n2. Plus 6–8 small grey dot divs (6×6px circles, colorGrey300) positioned at random coordinates.
    Center Content (position:absolute, centered):
      - Logo Badge: 96×96px circle (background: tokens.colorActionPrimary = #C6FF2D), containing "V" lettermark text (fontSize:48px, fontWeight:700, color: tokens.colorTextPrimary = #0F0F0F, font-family:Inter).
      - Brand Name: "VoltVenture" text below badge (tokens.typeHeadingMd or typeHeadingLg size, colorTextPrimary).
      - Divider: 48×2px horizontal bar (background: tokens.colorGrey200), centered, margin 16px vertical.
      - Tagline: "Ride. Explore. Repeat." text (tokens.typeBodyMd, colorTextSecondary).
    Loader Area (position:absolute, bottom:120px, centered):
      - Track: 180×4px div (background: tokens.colorGrey200, borderRadius:4px).
      - Fill: 108×4px div (background: tokens.colorActionPrimary, borderRadius:4px) — 60% of track width.
    Bottom: version text "v1.0.0" (colorGrey300, typeLabelSm, bottom:24px centered).
    No tab bar, no status bar (pre-auth splash).

    EXPORT: Interactive (returns DOM element via document.createElement)
    Copy makePhoneFrame() helper inline from button.stories.js. Inner screen stays #ffffff for splash (light variant — no screenBg override needed).
    Inject CSS keyframe via document.head.appendChild(style):
      @keyframes loaderFill { from { width:0px } to { width:108px } }
      Applied to the loader fill div: animation: loaderFill 2s ease-out forwards
    No buttons or interactive elements — the animated loader IS the interaction.
    Build the same visual layout using DOM createElement and style assignment (no innerHTML rebuild pattern).
    Return frame (DOM element).

    EXPORT: SourceCode (HTML string containing pre/code block)
    Use _esc() and _blk() helpers (copy inline from button.stories.js lines 154-156).
    Show React Native Paper JSX as a static string inside a pre/code block. The JSX snippet should reference VoltVenture token constants as string literals (not imported):
      - colorActionPrimary: '#C6FF2D', colorSurfaceBase: '#FFFFFF', colorTextPrimary: '#0F0F0F'
    Example snippet pattern: `<View style={{flex:1,backgroundColor:'#FFFFFF',alignItems:'center',justifyContent:'center'}}>\n  <View style={{width:96,height:96,borderRadius:48,backgroundColor:'#C6FF2D',alignItems:'center',justifyContent:'center'}}>\n    <Text style={{fontSize:48,fontWeight:'700',color:'#0F0F0F'}}>V</Text>\n  </View>\n  ...\n</View>`

    FILE CONVENTIONS (per D-05, D-06, D-08, D-09):
    - export default { title: 'Screens/Splash' }
    - All exports PascalCase: Default, Interactive, SourceCode
    - Token import: import * as tokens from '../../generated/tokens.js'
    - State held in plain JS variables; DOM mutations via .style.* only
    - Interactive returns DOM element (not string)
  </action>
  <acceptance_criteria>
    - stories/screens/splash.stories.js exists
    - `node --input-type=module --eval "import './stories/screens/splash.stories.js'" 2>&1` exits 0
    - File exports: Default (function returning string), Interactive (function returning DOM element), SourceCode (function returning string containing 'pre')
    - Default return value contains 'width:393px' and 'colorActionPrimary' reference (green badge)
    - Interactive function does not call innerHTML after initial construction
    - File contains 'loaderFill' keyframe animation string
    - File does not contain lowercase export names
    - No import statements other than tokens import
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>node --input-type=module --eval "import './stories/screens/splash.stories.js'" 2>&1</command>
  <expected>Exits 0 (no parse errors)</expected>
</verification>

<success_criteria>
- stories/screens/splash.stories.js rebuilt from frame O94n2
- Default export: white bg, V logo badge (96px green circle), brand name, divider, tagline, loader bar
- Interactive export: DOM element with CSS keyframe loader animation
- SourceCode export: RN Paper JSX pre/code block
- No parse errors on import
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-02-SUMMARY.md` when done
</output>
