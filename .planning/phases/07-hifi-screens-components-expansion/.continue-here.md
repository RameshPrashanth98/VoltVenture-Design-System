---
context: phase
phase: 07-hifi-screens-components-expansion
task: 18
total_tasks: 26
status: in_progress
last_updated: 2026-08-06
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure in prior phases.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No worktree isolation — worktree-isolated background agents intermittently lose Bash permission, writing files but failing to commit. Run each plan as a foreground (non-background) gsd-executor agent with no `isolation:"worktree"` parameter.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| worktree-bash-loss | Background gsd-executor agents with `isolation:"worktree"` intermittently lose Bash permission. In Phase 3 Wave 3, plans 03-05 and 03-07 failed immediately; 03-06 wrote files but couldn't commit. | blocking | Run all gsd-executor agents as foreground calls (no `run_in_background:true`, no `isolation:"worktree"`). Execute Wave plans sequentially. |
| foundation-story-staging | stories/border.stories.js, color, elevation, grid, iconography, radius, spacing, typography.stories.js have pre-existing uncommitted modifications unrelated to Phase 7. Staging them would pollute commits. | advisory | When committing Phase 7 story files, always use `git add stories/screens/... stories/components/...` — never `git add -A` or `git add stories/`. |
| lib-mutation | `npm run build-storybook` (and `build:tokens`) regenerates lib/voltventure_theme.ts and lib/voltventure_tokens.ts, dropping `onPrimary` override and `export const` declarations. | advisory | After EVERY `npm run build-storybook` call, immediately run: `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts` |

<current_state>
Phase 7, Wave 4 about to begin. Waves 0-3 are complete (17/26 plans done).

- **Next up**: Wave 4 plan 07-17 (Settings + Preferences + Login Security screens)
- **Git status**: Clean except 8 pre-existing uncommitted foundation story modifications
- **Last commit**: ec1a35b (docs(07-26): complete curated-routes plan)
</current_state>

<completed_work>

**Wave 0:**
- 07-01 ✓ Preflight (commit 41cd29c) — build exits 0, 11+9 stories, 37 tokens confirmed

**Wave 1 — static screens (all 6 done):**
- 07-02 ✓ Splash (commit 2ef0d70) — white bg, V badge, CSS keyframe loader
- 07-03 ✓ Onboarding 1-2-3 (commit df31d36) — step dots, badges, CTAs
- 07-04 ✓ ID Scan + Facial Scan (commit 1116935) — dark screens #0f0f0f, scan animations
- 07-05 ✓ QR Unlock + Safety Mount (commit acf388e) — dark QR viewfinder, checkbox toggles
- 07-06 ✓ Ride Complete + Cafe Detail (commit 8365cd2) — check circle, VIP gold, Discover tab
- 07-07 ✓ ToS + Privacy Policy (commit 4eb6669) — scrollable legal, sticky agree footer

**Wave 2 — component batch A (all 4 done):**
- 07-08 ✓ SettingsRow + FAQRow (commit f4c2be7) — expand/collapse, toggle variants
- 07-09 ✓ PaymentCardRow + RideSummaryCard (commit 88b0d46) — radio selection, dark stats
- 07-10 ✓ VoltCoinsBalance + StationInfoCard (commit 08fef27) — level badge, slots availability
- 07-11 ✓ QRViewfinder + HubCard + RouteCard (commit e9aaf9f) — scan animation, VIP gold, hero

**Wave 3 — form/list/profile screens (all 6 done, including 07-26):**
- 07-12 ✓ Registration + Login (commit 65b27b0) — social auth, segmented toggle, phone input
- 07-13 ✓ Add/Select/List Payment (commit 7d409db) — radio selection, dark credit card, Wallet tab
- 07-14 ✓ Security Deposit (commit 18fc393) — 4-step tracker, deposit banner
- 07-15 ✓ Profile + Ride History Stats (commit 5433957) — trust card, destructive sign out
- 07-16 ✓ Edit Profile (commit b57d380) — form fields, hidden input overlays, real-time sync
- 07-26 ✓ Curated Routes (commit 972a2bb) — range pills, route cards, Discover tab active
</completed_work>

<remaining_work>

**Wave 4 — settings screens + component batch B (3 plans — NEXT):**
- 07-17: settings.stories.js + preferences.stories.js + login-security.stories.js (frames oOcGF, Kaf7F, aeptx)
- 07-18: voltcoins-rewards.stories.js + support.stories.js (frames GH4KX, r504Z)
- 07-19: dashboard-panel.stories.js (C-02) + nav-turn-card.stories.js (C-05) + riding-progress-card.stories.js (C-06)

**Wave 5 — map/ride screens (5 plans, depend on Wave 4 components):**
- 07-20: home-map.stories.js rebuild (frame E9hST)
- 07-21: navigate-to-bike.stories.js rebuild (frame kUCG9)
- 07-22: walking-directions.stories.js rebuild (frame FZnNd)
- 07-23: active-ride-dashboard.stories.js (frame hQMrX)
- 07-24: end-ride + riding-to-charging + discover-vip-hubs (frames AH8t6, gqQ8M, PS2Xe)

**Wave 6 — done-bar (1 plan):**
- 07-25: build-storybook, count 34+23 story files, lib/ restore, git commit, human verify
</remaining_work>

<decisions_made>

- Tab bar labels in ALL Phase 7 screen stories: `['Ride', 'Discover', 'Wallet', 'Account']` — differs from Phase 6 component Interactive `['Home', 'Ride', 'Rewards', 'Profile']`
- Dark screens (ID Scan, Facial Scan, QR Unlock Scan): set `screen.style.background = '#0f0f0f'` after makePhoneFrame() — already applied in 07-04 and 07-05
- Special brand colors (hardcoded, no token): #EF4444 SOS, #25D366 WhatsApp, #D64545 destructive, #F5C518 VIP gold
- After every `npm run build-storybook`: run `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts`
- Wave executors: foreground agents, NO worktree isolation, NO run_in_background flag
- 07-26 Curated Routes is DONE (was in Wave 3, not Wave 4)
</decisions_made>

<blockers>
None. 07-17 was never started — clean state.
</blockers>

## Required Reading (in order)

1. `.planning/phases/07-hifi-screens-components-expansion/07-17-PLAN.md` — next plan
2. `.planning/phases/07-hifi-screens-components-expansion/07-UI-SPEC.md` — design contract
3. `.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md` — shared conventions
4. `stories/components/settings-row.stories.js` — SettingsRow list row pattern for 07-17

## Critical Anti-Patterns (do NOT repeat these)

- **WORKTREE-ISOLATION**: Do not use `isolation:"worktree"` — agents lose Bash permission. Foreground only.
- **STAGING-FOUNDATION-STORIES**: Never `git add stories/` broadly — 8 foundation stories must stay unstaged.
- **LIB-MUTATION**: Always `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts` after build-storybook.
- **LOWERCASE-EXPORTS**: All exports must be PascalCase — Storybook 10 silently ignores lowercase.

## Infrastructure State

- Storybook: not running (build-only workflow)
- Generated tokens: `generated/tokens.js` — all 37 Phase 7 constants confirmed present (from 07-01)
- Git: clean on main branch, last commit ec1a35b

<context>
Waves 0-3 are complete (17/26 plans done). The session paused cleanly at Wave 4 start — 07-17 was rejected before it ran.

Wave 4 has 3 plans: settings/preferences/login-security screens (07-17), rewards/support screens (07-18), and component batch B (07-19 — DashboardPanel, NavTurnCard, RidingProgressCard).
Wave 5 map screens depend on the components built in 07-19, so 07-19 must complete before Wave 5 starts.
Wave 6 is the final done-bar plan (07-25).

Execution pattern: one foreground gsd-executor agent per plan, sequential, no worktrees.
</context>

<next_action>
Start with: Execute Wave 4 plan 07-17 (Settings + Preferences + Login Security) as a foreground gsd-executor agent. Reference `stories/components/settings-row.stories.js` for the SettingsRow list row HTML pattern.
</next_action>
