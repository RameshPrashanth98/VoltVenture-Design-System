---
context: phase
phase: 07-hifi-screens-components-expansion
task: 24
total_tasks: 26
status: in_progress
last_updated: 2026-08-06T12:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No worktree isolation — worktree-isolated background agents intermittently lose Bash permission, writing files but failing to commit. Run each plan as a foreground (non-background) gsd-executor agent with no `isolation:"worktree"` parameter.
- [ ] CONSTRAINT: lib/ mutation — `npm run build-storybook` regenerates `lib/voltventure_theme.ts` and `lib/voltventure_tokens.ts`, dropping manual overrides. Always run `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts` after any build:tokens or build-storybook call.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| worktree-bash-loss | Background gsd-executor agents with `isolation:"worktree"` intermittently lose Bash permission. Plans write files but fail to commit. | blocking | Run all gsd-executor agents as foreground calls (no `run_in_background:true`, no `isolation:"worktree"`). |
| foundation-story-staging | stories/border/color/elevation/grid/iconography/radius/spacing/typography.stories.js have pre-existing uncommitted mods unrelated to Phase 7. | advisory | Always use explicit `git add stories/screens/... stories/components/...` — never `git add -A` or `git add stories/`. |
| lib-mutation | `npm run build-storybook` (and `build:tokens`) regenerates lib/ dropping `onPrimary` override and `export const` declarations. | advisory | After EVERY build-storybook call: `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts` |
| wrong-tab-labels | Using Phase 6 labels `['Home','Ride','Rewards','Profile']` for Phase 7 screens | advisory | Phase 7 screens use `['Ride','Discover','Wallet','Account']`. Check TABS const at top of each file. |

<current_state>
Phase 7, Wave 5 PARTIAL. 23 of 26 plans done. Last commit: 001420c.

Wave 5 remaining (2 plans):
- 07-23: Active Ride Dashboard (frame hQMrX) — flex-column: 460px map area + dashboard panel (flex:1) + 56px tab bar
- 07-24: EndRideFindCharging (AH8t6) + RidingToCharging (gqQ8M) + DiscoverVIPHubs (PS2Xe)

Then Wave 6 (1 plan):
- 07-25: done-bar — build-storybook, verify 34+23 files, git restore lib/, human verify
</current_state>

<completed_work>

**Wave 0:** 07-01 ✓ Preflight (41cd29c)

**Wave 1 — static screens:**
- 07-02 ✓ Splash (2ef0d70)
- 07-03 ✓ Onboarding 1-2-3 (df31d36)
- 07-04 ✓ ID Scan + Facial Scan (1116935) — dark #0f0f0f screens
- 07-05 ✓ QR Unlock + Safety Mount (acf388e)
- 07-06 ✓ Ride Complete + Cafe Detail (8365cd2)
- 07-07 ✓ Terms + Privacy (4eb6669)

**Wave 2 — component batch A:**
- 07-08 ✓ SettingsRow + FAQRow (f4c2be7)
- 07-09 ✓ PaymentCardRow + RideSummaryCard (88b0d46)
- 07-10 ✓ VoltCoinsBalance + StationInfoCard (08fef27)
- 07-11 ✓ QRViewfinder + HubCard + RouteCard (e9aaf9f)

**Wave 3 — form/profile screens:**
- 07-12 ✓ Registration + Login (65b27b0)
- 07-13 ✓ Add/Select/List Payment (7d409db)
- 07-14 ✓ Security Deposit (18fc393)
- 07-15 ✓ Profile + Ride History Stats (5433957)
- 07-16 ✓ Edit Profile (b57d380)
- 07-26 ✓ Curated Routes (972a2bb)

**Wave 4 — settings screens + component batch B:**
- 07-17 ✓ Settings + Preferences + Login & Security screens (5dbc62f)
- 07-18 ✓ VoltCoins Rewards + Support screens (5dbc62f)
- 07-19 ✓ DashboardPanel + NavTurnCard + RidingProgressCard components (5dbc62f)

**Wave 5 — map/ride screens (partial):**
- 07-20 ✓ HomeMap rebuild (001420c) — 6 battery range pins (300km/200km/100km), safe zone polygon, 100px Scan CTA card, locationPulse keyframe
- 07-21 ✓ NavigateToBike rebuild (001420c) — 72px selected bike pin, 7 route dots (6px circles), Selected Bike Card (56px thumb + Get Directions + Choose Different)
- 07-22 ✓ WalkingDirections rebuild (001420c) — L-shaped route (5×186 grey + 5×126 green + 64×5 green), 264×52px Turn Card (Marine Drive), Walking Progress Card (I've Arrived)
</completed_work>

<remaining_work>

**Wave 5 remainder (2 plans — execute inline):**

**07-23: `stories/screens/active-ride-dashboard.stories.js` NEW**
- Layout: `display:flex;flex-direction:column` (not position:relative)
- Map Area: `flex-shrink:0; height:460px; position:relative`
  - Safe Zone Ellipse: 290×260px, colorGrey300 border, borderRadius:50%
  - Route V: 14×162px colorActionPrimary (top:200px, left:190px)
  - Route H: 202×8px colorActionPrimary (top:280px, left:190px)
  - Route H2: 122×12px colorGrey300 remaining (top:200px, left:190px)
  - Destination Flag Pin (🏁 top:188px, left:302px)
  - Location Pulse (top:352px, left:180px, 34×34px ring)
  - Map Fade Bottom (linear-gradient to colorSurfaceBase)
  - Top Gradient + Status Bar
  - Nav Turn Card 210×52px (top:72px, left:20px): "Continue on" + "Victoria Embankment"
  - Safe Zone Warning chip (top:133px, left:20px)
- Dashboard Panel: `flex:1`
  - Handle, Timer (00:23:41 + LIVE), Divider
  - Telemetry: 18 km/h | 12.4 km left
  - Billing section (Base Rental + Electricity + Total ₹58.75)
  - Action Row: SOS (#EF4444 80×56px) + End Ride (colorActionPrimary flex:1)
- Tab Bar: `flex-shrink:0; height:56px` (Ride active)
- Interactive: locationPulse keyframe, nav turn card tap → opacity:0 → remove(), SOS/End Ride press states

**07-24: 3 new files**
- `stories/screens/end-ride-find-charging.stories.js` (frame AH8t6)
  - Route V 5×96px + Route H 76×5px (colorActionPrimary)
  - 80×80px Charging Station Pin (stationPulse keyframe animation)
  - Station Info Card: VoltHub Central, 6 slots, Navigate to Station button
  - Tab Ride active
- `stories/screens/riding-to-charging.stories.js` (frame gqQ8M)
  - Route Ridden V 5×150px colorGrey300 + Remaining V 5×86px + H 76×5px colorActionPrimary
  - 80×80px Station Pin + 264×52px Turn Card + Recenter FAB
  - Riding Progress Card: 1.2 km | 8 min | Bike Chip | "I've Docked" button
  - Tab Ride active
- `stories/screens/discover-vip-hubs.stories.js` (frame PS2Xe)
  - Layout: flex-column
  - Map Area 388px: 5 cafe pins, location pulse, title row
  - Bottom Sheet flex:1: handle + "VIP Hubs Nearby" header + Routes Promo Card (colorSurfaceInverse, rgba(198,255,45,0.13) icon chip) + 4 hub rows
  - Tab Discover active (second tab)

**Wave 6 (07-25):**
- `npm run build-storybook`, count 34 screen + 23 component story files
- `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts`
- commit, human verify in Storybook
</remaining_work>

<decisions_made>

- Tab bars ALL Phase 7 screens: `['Ride', 'Discover', 'Wallet', 'Account']` (differs from Phase 6 component)
- Dark screens: `screen.style.background = '#0f0f0f'` after makePhoneFrame() — ID Scan, Facial Scan, QR Unlock
- colorGrey050 (#FAFAFA) bg for settings-family screens (Settings, Preferences, Login & Security)
- Map screen bg: static `#e8e8e8` placeholder + `position:absolute` overlays
- hexToRgba + shadowFromToken: copied inline in every file that needs elevation shadows
- Wave executors: foreground agents, NO worktree isolation, NO run_in_background
- Hardcoded brand colors: #EF4444 (SOS), #25D366 (WhatsApp), #D64545 (destructive), #F5C518 (VIP gold), #FDECEC (danger bg), #FFF1DC/#F5871F/#B5590A (streak)
- HTML entity encoding (&#x26A1; etc.) for emoji in Default template literals — avoids escaping issues
- tabsHtml() helper function at module scope, called inside Default template literal — confirmed working
- active-ride-dashboard + discover-vip-hubs: outer div is `display:flex;flex-direction:column` (not position:relative); for Interactive, screen div needs same flex layout
- active-ride-dashboard tab bar: height:56px (not 80px) — tighter to fit map+panel in 852px
- discover-vip-hubs tab bar: height:56px, Discover tab active (index 1 in TABS array)
</decisions_made>

<blockers>
None. Wave 5 partial (07-20/21/22 done), all plan specs fully read, all patterns confirmed working.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-hifi-screens-components-expansion/07-23-PLAN.md` — active-ride-dashboard spec (flex layout, route segments, dashboard panel inline)
2. `.planning/phases/07-hifi-screens-components-expansion/07-24-PLAN.md` — 3 files spec (end-ride, riding-to-charging, discover-vip-hubs)
3. `stories/screens/home-map.stories.js` — confirm tabsHtml() helper pattern and makePhoneFrame() shape before writing new files
4. `stories/components/dashboard-panel.stories.js` — dashboard panel visual reference for 07-23 inline rebuild

## Critical Anti-Patterns (do NOT repeat these)

- **WORKTREE-ISOLATION**: Do not use `isolation:"worktree"` — agents lose Bash permission. Foreground only.
- **STAGING-FOUNDATION-STORIES**: Never `git add stories/` broadly — 8 foundation stories must stay unstaged.
- **LIB-MUTATION**: Always `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts` after build-storybook.
- **LOWERCASE-EXPORTS**: All exports must be PascalCase — Storybook 10 silently ignores lowercase.
- **WRONG-TAB-LABELS**: Phase 7 screens use `['Ride','Discover','Wallet','Account']` — not Phase 6 labels.
- **WRONG-FLEX-LAYOUT**: active-ride-dashboard and discover-vip-hubs use flex-column NOT position:relative — failing to use flex means tab bar and panel won't stack correctly.

## Infrastructure State

- Storybook: not running (build-only workflow)
- Generated tokens: `generated/tokens.js` — all token constants confirmed present
- Git: clean on main branch, last commit 001420c (Wave 5 partial: 07-20/21/22 done)
- lib/ files: in correct manually-patched state (not corrupted)
- Uncommitted (pre-existing, do NOT stage): 8 foundation stories (border, color, elevation, grid, iconography, radius, spacing, typography)

<context>
Wave 5 map rebuilds (07-20, 07-21, 07-22) completed and committed in this session. Pattern is solid:
- Default export: HTML string template literal, tabsHtml() helper, HTML entities for emoji
- Interactive export: makePhoneFrame() inline, DOM construction, CSS keyframe injection via document.head.appendChild
- SourceCode export: _esc()/_blk() helpers + reference comment with frame ID

The 2 remaining Wave 5 plans (07-23, 07-24) follow this same pattern but with:
- 07-23: flex-column layout (screen div needs display:flex;flex-direction:column in makePhoneFrame)
- 07-24: end-ride and riding-to-charging use 80×80px station pin with stationPulse animation; discover-vip-hubs uses flex-column layout + Discover tab active

After 07-23 and 07-24 are written and committed, run Wave 6 (07-25): npm run build-storybook, count files, restore lib/, commit, human verify.
</context>

<next_action>
Execute 07-23 (active-ride-dashboard) and 07-24 (3 files) inline — write files, verify with `node --input-type=module`, commit explicitly (never git add -A). Then run 07-25 done-bar.
Resume: `/gsd:execute-phase 7`
</next_action>
