---
phase: 7
plan: "07-17"
type: execute
wave: 4
depends_on: ["07-01", "07-08"]
files_modified:
  - stories/screens/settings.stories.js
  - stories/screens/preferences.stories.js
  - stories/screens/login-security.stories.js
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "Settings: colorGrey050 screen bg, ACCOUNT section (Preferences + Login & Security rows), SUPPORT section (3 rows), Danger Zone (Delete Account with #FDECEC chip + #D64545 chevron), NO tab bar"
    - "Preferences: Regional Card (Language, Distance Units segmented, Currency) + Notifications Card (toggle ON colorActionPrimary 50×29px)"
    - "Login & Security: Security Card with Change Password row + Biometric toggle ON (46×27px) + 2FA toggle OFF; Sessions section"
    - "Preferences Interactive: toggle cycles On↔Off; Login & Security Interactive: each toggle independently"
    - "#FDECEC and #D64545 hardcoded — no VoltVenture tokens"
  artifacts:
    - path: "stories/screens/settings.stories.js"
      provides: "Settings screen (frame oOcGF)"
      exports: ["Default", "Interactive", "SourceCode"]
    - path: "stories/screens/preferences.stories.js"
      provides: "Preferences screen (frame Kaf7F)"
      exports: ["Default", "Interactive", "SourceCode"]
    - path: "stories/screens/login-security.stories.js"
      provides: "Login & Security screen (frame aeptx)"
      exports: ["Default", "Interactive", "SourceCode"]
  key_links:
    - from: "stories/screens/settings.stories.js"
      to: "stories/components/settings-row.stories.js"
      via: "settings-row pattern for all list rows"
      pattern: "#FDECEC|#D64545"
---

<objective>
Create settings.stories.js (frame oOcGF), preferences.stories.js (frame Kaf7F), and login-security.stories.js (frame aeptx) — all Category B (Toggle/Tab) screens with colorGrey050 background.

Purpose: Settings uses settings-row pattern (Wave 2) for all rows. Preferences and Login & Security have toggle switches (50×29px and 46×27px respectively). Settings has no tab bar (accessed from Profile settings button).
Output: Three new screen story files.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/07-hifi-screens-components-expansion/07-CONTEXT.md

<interfaces>
<!-- All three screens: background:tokens.colorGrey050 (#FAFAFA) — slightly off-white for section grouping -->
<!-- Toggle pill (Preferences): 50×29px, ON=colorActionPrimary, OFF=colorGrey200, knob=21px circle white, translate 0→21px -->
<!-- Toggle pill (Login & Security): 46×27px, ON=colorActionPrimary, OFF=colorGrey200, knob=19px white -->
<!-- Settings Danger Zone: #FDECEC = pale red (no token), #D64545 = destructive red (no token) -->
<!-- Section pattern: Label (typeLabelSm, colorGrey500, uppercase) + Card (colorSurfaceBase, borderRadius:radiusLg, overflow:hidden) -->
<!-- Card rows: 48px min-height, display:flex, align-items:center, padding:12px 16px, gap:12px -->
<!-- Row separator: 1px colorGrey100 (via border-top on rows or separate div) -->
</interfaces>
</context>

<tasks>

<task id="T-01">
  <title>Create settings.stories.js from frame oOcGF — grouped settings with danger zone</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/settings-row.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
  </read_first>
  <action>
    Create stories/screens/settings.stories.js. export default { title: 'Screens/Settings' }.

    LAYOUT: width:393px, min-height:852px, display:flex, flex-direction:column, background:tokens.colorGrey050.

    Status Bar (62px): light (on colorGrey050 bg).
    Header Row (44px, display:flex, align-items:center, padding:0 16px, gap:12px):
      Back Button: 36×36px, background:tokens.colorSurfaceBase, border:1px solid tokens.colorGrey200, "←" colorTextPrimary. Title "Settings" typeHeadingMd colorTextPrimary fontWeight:700.

    ACCOUNT Section (margin:16px 16px 0):
      Section label (typeLabelSm, colorGrey500, textTransform:uppercase, marginBottom:8px): "ACCOUNT"
      ACCOUNT Card (background:tokens.colorSurfaceBase, borderRadius:tokens.radiusLg, overflow:hidden):
        Row: "⚙" chip (30×30px colorGrey100) + "Preferences" colorTextPrimary + "›" colorGrey300. 48px min-height.
        Divider: 1px colorGrey100.
        Row: "🔐" chip + "Login & Security" + "›". 48px.

    SUPPORT & LEGAL Section (margin:16px 16px 0):
      Section label: "SUPPORT & LEGAL"
      Card:
        Row: "🎧" chip + "Help & Support" + "›".
        Divider. Row: "📄" chip + "Terms of Service" + "›".
        Divider. Row: "🛡" chip + "Privacy Policy" + "›".
      All rows: cursor:pointer, pointerdown → colorGrey050 bg; up/leave → colorSurfaceBase.

    Danger Zone Section (margin:16px 16px 0):
      Section label: "DANGER ZONE" (color:#D64545) /* hardcoded — no VV token */
      Delete Account Button (background:tokens.colorSurfaceBase, borderRadius:tokens.radiusLg, padding:12px 16px, display:flex, align-items:center, gap:12px, cursor:pointer):
        DeleteIconChip: 34×34px, background:#FDECEC /* pale red — no VV token */, borderRadius:tokens.radiusSm, "🗑" icon fontSize:16px.
        Text Col (flex:1): "Delete Account" typeBodyMd colorTextPrimary fontWeight:600 + "Permanent action, cannot be undone" typeLabelSm colorTextSecondary.
        Chevron "›" color:#D64545.

    Footer (margin:auto 16px 24px, text-align:center):
      "VoltVenture v1.0.0" — typeLabelSm, colorGrey300.

    NO tab bar — settings accessed via Profile, not tab navigation.

    INTERACTIVE EXPORT:
    makePhoneFrame() inline. Inner screen: set to tokens.colorGrey050 (#FAFAFA) rather than #ffffff to match screen bg.
    All rows: pointerdown → colorGrey050 bg; up/leave → colorSurfaceBase (or back to previous).
    Delete Account row: pointerdown → backgroundColor='#FEF2F2'; up/leave → colorSurfaceBase.
    Return frame.

    SOURCECODE EXPORT: _esc()/_blk(). Key: '#FAFAFA' (colorGrey050), '#FDECEC', '#D64545'.
  </action>
  <acceptance_criteria>
    - stories/screens/settings.stories.js exists
    - `node --input-type=module --eval "import './stories/screens/settings.stories.js'" 2>&1` exits 0
    - File contains '#FDECEC' for delete account chip bg
    - File contains '#D64545' for danger zone color
    - File does NOT contain tab bar ('['Ride','Discover','Wallet','Account']' or similar tab array) — no tab bar on settings
    - File contains 'colorGrey050' for screen background
    - PascalCase exports: Default, Interactive, SourceCode
  </acceptance_criteria>
</task>

<task id="T-02">
  <title>Create preferences.stories.js (frame Kaf7F) and login-security.stories.js (frame aeptx)</title>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/settings-row.stories.js
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/screens/settings.stories.js
  </read_first>
  <action>
    CREATE: stories/screens/preferences.stories.js. export default { title: 'Screens/Preferences' }.

    LAYOUT: colorGrey050 bg, same pattern as settings.

    Status Bar (62px): light.
    Header Row: Back Button (colorSurfaceBase border) + "Preferences" title.

    Regional Section (margin:16px 16px 0):
      Label: "REGIONAL". Card (colorSurfaceBase, borderRadius:radiusLg):
        Language Row (48px min-height): "🌐" chip (30×30, colorGrey100) + "Language" + "English →" colorTextSecondary typeLabelSm (value + chevron).
        Divider.
        Distance Units Row: "📏" chip + "Distance Units" + segmented control:
          Segmented: display:inline-flex, background:colorGrey100, borderRadius:radiusFull, padding:3px, gap:3px.
            "km" tab: background:colorSurfaceInverse, color:#ffffff, borderRadius:radiusFull, padding:4px 14px, typeLabelSm.
            "mi" tab: transparent, colorTextSecondary, padding:4px 14px.
        Divider.
        Currency Row: "💱" chip + "Currency" + "INR ₹ →" colorTextSecondary.

    Notifications Section (margin:16px 16px 0):
      Label: "NOTIFICATIONS". Card:
        Push Notifications Row (display:flex, align-items:center, gap:12px, padding:12px 16px):
          "🔔" chip (30×30, colorGrey100). Text Col (flex:1): "Push Notifications" colorTextPrimary + "Ride updates & promotions" colorTextSecondary typeLabelSm.
          Toggle Pill (50×29px, ON state): background:colorActionPrimary, borderRadius:radiusFull, position:relative, width:50px, height:29px.
            Knob: 21×21px circle (#ffffff, position:absolute, right:4px — ON position).

    Bottom Spacer (flex:1).
    Tab Bar — NOTE: Preferences IS accessed from Settings (not a tab), but the design frame shows a tab bar. Include Tab Account active (Preferences is within the Account navigation flow).

    INTERACTIVE for Preferences:
    makePhoneFrame() inline. Inner screen tokens.colorGrey050.
    Segmented toggle: let activeUnit = 'km'. pointerdown on km/mi → switch active (active: colorSurfaceInverse bg + white; inactive: transparent + colorTextSecondary).
    Push Notifications toggle: let notifOn = true. pointerdown on pill: notifOn = !notifOn. ON: bg=colorActionPrimary, knob.style.right='4px'. OFF: bg=colorGrey200, knob.style.right='calc(100% - 25px)' (knob slides left).
    Return frame.

    CREATE: stories/screens/login-security.stories.js. export default { title: 'Screens/LoginSecurity' }.

    Same colorGrey050 layout. Header: "Login & Security".

    Security Section (margin:16px 16px 0). Label: "SECURITY". Card (colorSurfaceBase):
      Change Password Row: "🔑" chip (34×34, colorGrey100) + "Change Password" + "›".
      Divider.
      Biometric Login Row: "👆" chip + "Biometric Login" + Toggle (46×27px, ON colorActionPrimary, knob:19×19px right position).
      Divider.
      2FA Row: "📱" chip + "Two-Factor Authentication" + Toggle (46×27px, OFF colorGrey200, knob left position).

    Sessions Section (margin:16px 16px 0). Label: "ACTIVE SESSIONS".
      Current Device Card (background:colorGreen100, borderRadius:radiusLg, padding:12px 16px, display:flex, align-items:center, gap:12px, marginBottom:8px):
        DeviceIconChip: 38×38px, colorActionPrimary bg, "📱" icon black.
        Device Text Col (flex:1): "iPhone 16 Pro" (typeBodyMd, colorTextAccent fontWeight:600) + "Current device · Online" (typeLabelSm, colorGreen700).
        LiveDot: 8×8px circle, background:colorActionPrimary, borderRadius:50%, flex-shrink:0.
      Other Device Row (background:colorSurfaceBase, borderRadius:radiusLg, border:1px solid colorGrey100, padding:12px 16px, display:flex, align-items:center, gap:12px):
        DeviceIconChip: 38×38px, colorGrey100 bg, "💻" icon.
        Device Text Col: "MacBook Pro" + "Last active 2 days ago" colorGrey500.
        Chevron "›" colorGrey300.

    Sign Out All Devices Button (margin:16px, height:48px, full-width, background:colorSurfaceBase, border:1px solid tokens.colorGrey200, borderRadius:radiusFull, "Sign Out All Devices" colorTextSecondary, cursor:pointer).

    Bottom Spacer (flex:1). Tab Bar with Account active.

    INTERACTIVE for Login & Security:
    makePhoneFrame(). Inner screen colorGrey050.
    Biometric toggle: let bioOn = true. Independent pointerdown handler.
    2FA toggle: let twoFaOn = false. Independent handler.
    Each toggle: ON → bg=colorActionPrimary + knob right; OFF → bg=colorGrey200 + knob left.
    Return frame.

    SOURCECODE for both: _esc()/_blk(). Key literals for L&S: '#F4FFD9' (colorGreen100), '#C6FF2D' (colorActionPrimary), '#7D9220' (colorGreen700/colorTextAccent).
  </action>
  <acceptance_criteria>
    - stories/screens/preferences.stories.js exists; exits 0 on node import check
    - stories/screens/login-security.stories.js exists; exits 0 on node import check
    - preferences: contains '50' and '29' (toggle pill dimensions)
    - preferences: contains 'km' and 'mi' segmented control options
    - login-security: contains '46' and '27' (toggle dimensions — different from Preferences)
    - login-security: contains 'colorGreen100' for Current Device card
    - login-security: Biometric ON and 2FA OFF toggles are independent state variables
    - Both: PascalCase exports Default, Interactive, SourceCode
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <command>node --input-type=module --eval "import './stories/screens/settings.stories.js'; import './stories/screens/preferences.stories.js'; import './stories/screens/login-security.stories.js'" 2>&1</command>
  <expected>Exits 0 (no parse errors on all three files)</expected>
</verification>

<success_criteria>
- settings: colorGrey050 bg, section-grouped rows, Danger Zone (#FDECEC + #D64545), NO tab bar
- preferences: Regional card + Notifications toggle (50×29px, colorActionPrimary ON); segmented km/mi
- login-security: Security Card (Biometric ON, 2FA OFF — 46×27px toggles), Sessions section (green current device card with live dot)
- All Interactive: independent toggles, tab switching
- No parse errors
</success_criteria>

<output>
Create `.planning/phases/07-hifi-screens-components-expansion/07-17-SUMMARY.md` when done
</output>
