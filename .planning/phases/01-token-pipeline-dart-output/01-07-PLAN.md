---
phase: 01-token-pipeline-dart-output
plan: 07
name: TypeScript Tokens + RN Paper Theme Factory
type: execute
wave: 4
depends_on: [01-06]
files_modified:
  - lib/voltventure_tokens.ts
  - lib/voltventure_theme.ts
  - sd-transforms/ts-constants.format.mjs
  - sd-transforms/rn-paper-theme.format.mjs
  - style-dictionary.config.mjs
autonomous: true
requirements: [D-07, D-08]
revised: 2026-07-25
revision_reason: Platform changed from Flutter to React Native Paper

must_haves:
  goal: "lib/voltventure_tokens.ts and lib/voltventure_theme.ts are generated by npm run build:tokens, exports typed TypeScript constants and createVoltVentureTheme() returning an RN Paper MD3Theme, and tsc --noEmit returns 0 errors."
  truths:
    - "lib/voltventure_tokens.ts exists with AUTO-GENERATED header"
    - "lib/voltventure_tokens.ts exports camelCase typed constants (hex strings for colors, numbers for dimensions)"
    - "lib/voltventure_theme.ts exists with AUTO-GENERATED header"
    - "lib/voltventure_theme.ts exports createVoltVentureTheme() returning MD3Theme"
    - "createVoltVentureTheme() uses spread strategy: { ...MD3LightTheme, colors: { ...MD3LightTheme.colors, ...tokenOverrides } }"
    - "No placeholder values (Colors.red equivalents) anywhere"
    - "tsc --noEmit returns 0 errors (both voltventure_tokens.ts and voltventure_theme.ts)"
    - "npm run build exits 0 (validate + both TS files generated)"
    - "npm test passes all 4 unit tests (unchanged from Plan 02)"
  artifacts:
    - path: "lib/voltventure_tokens.ts"
      provides: "Typed TypeScript token constants"
      contains: "colorActionPrimary, space400, radiusXl, etc."
    - path: "lib/voltventure_theme.ts"
      provides: "React Native Paper MD3 theme factory"
      contains: "createVoltVentureTheme"
    - path: "sd-transforms/ts-constants.format.mjs"
      provides: "SD formatter that produces the TypeScript constants file"
    - path: "sd-transforms/rn-paper-theme.format.mjs"
      provides: "SD formatter that produces the RN Paper theme factory file"
  key_links:
    - from: "lib/voltventure_theme.ts"
      to: "lib/voltventure_tokens.ts"
      via: "import * as tokens from './voltventure_tokens'"
      pattern: "import.*voltventure_tokens"
    - from: "lib/voltventure_theme.ts"
      to: "react-native-paper"
      via: "import { MD3LightTheme } from 'react-native-paper'"
      pattern: "MD3LightTheme"
---

<objective>
Generate lib/voltventure_tokens.ts and lib/voltventure_theme.ts — the TypeScript token constants and React Native Paper MD3 theme factory. The tokens file exports typed camelCase constants consumed by components. The theme factory exports createVoltVentureTheme() that wires semantic color tokens into the RN Paper MD3 theme via the spread strategy (MD3LightTheme fills all required color roles; copyWith-equivalent overrides only where explicit semantic tokens exist).

Platform: React Native Paper (switched from Flutter 2026-07-25).
Done-bar: tsc --noEmit + npm run build:tokens + npm test (replaces dart analyze lib/).
</objective>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-CONTEXT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/research/PITFALLS.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/research/STACK.md

<interfaces>
<!-- TypeScript constants file structure: -->
<!--
// AUTO-GENERATED — DO NOT EDIT. Run 'npm run build:tokens' to regenerate.

// Primitive: Color
const colorGreen500 = '#C6FF2D';
const colorGrey050  = '#FAFAFA';
const colorBlack    = '#0F0F0F';
const colorWhite    = '#FFFFFF';

// Semantic: Color
export const colorActionPrimary    = colorGreen500;
export const colorActionPrimaryFg  = colorBlack;
export const colorActionSecondary  = colorBlack;      // Volt Black secondary action
export const colorActionSecondaryFg = colorWhite;
export const colorSurfaceBase      = colorWhite;
export const colorTextPrimary      = colorBlack;
export const colorTextSecondary    = colorGrey600;    // adjust to actual grey token name
export const colorBorderSubtle     = colorGrey200;    // adjust to actual grey token name

// Semantic: Spacing
export const space050 = 2;
export const space100 = 4;
export const space200 = 8;
export const space300 = 12;
export const space400 = 16;
export const space500 = 20;
export const space600 = 24;
// ... etc

// Semantic: Radius
export const radiusXs   = 8;
export const radiusSm   = 12;
export const radiusMd   = 16;
export const radiusLg   = 20;
export const radiusXl   = 28;
export const radius2xl  = 36;
export const radiusFull = 999;
-->

<!-- RN Paper theme factory structure: -->
<!--
import { MD3LightTheme } from 'react-native-paper';
import type { MD3Theme } from 'react-native-paper';
import * as tokens from './voltventure_tokens';

export function createVoltVentureTheme(): MD3Theme {
  return {
    ...MD3LightTheme,
    colors: {
      ...MD3LightTheme.colors,
      primary: tokens.colorActionPrimary,
      onPrimary: tokens.colorActionPrimaryFg,
      secondary: tokens.colorActionSecondary,
      onSecondary: tokens.colorActionSecondaryFg,
      background: tokens.colorSurfaceBase,
      surface: tokens.colorSurfaceBase,
      onSurface: tokens.colorTextPrimary,
      outline: tokens.colorBorderSubtle,
    },
  };
}
-->

<!-- RN Paper MD3 color role reference (react-native-paper v5): -->
<!-- primary, onPrimary, primaryContainer, onPrimaryContainer -->
<!-- secondary, onSecondary, secondaryContainer, onSecondaryContainer -->
<!-- tertiary, onTertiary, tertiaryContainer, onTertiaryContainer -->
<!-- error, onError, errorContainer, onErrorContainer -->
<!-- background, onBackground -->
<!-- surface, onSurface, surfaceVariant, onSurfaceVariant -->
<!-- outline, outlineVariant -->
<!-- shadow, scrim -->
<!-- inverseSurface, inverseOnSurface, inversePrimary -->
<!-- elevation: { level0..level5 } -->
<!-- All filled by MD3LightTheme spread; only override where explicit semantic tokens exist -->

<!-- TypeScript note: -->
<!-- react-native-paper must be listed in devDependencies for the build step (type checking) -->
<!-- It is a peerDependency for the consuming RN app -->
<!-- tsc --noEmit validates the theme factory types against the MD3Theme interface -->
</interfaces>
</context>

<tasks>

<task type="auto" id="1-07-01">
  <name>Task 1: TypeScript constants formatter + RN Paper theme formatter + SD platform wiring</name>
  <files>sd-transforms/ts-constants.format.mjs, sd-transforms/rn-paper-theme.format.mjs, style-dictionary.config.mjs</files>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/lib/voltventure_tokens.dart (read the existing generated Dart constants to identify what token names and values exist — the TS formatter must produce equivalent exports)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/sd-transforms/dart-constants.format.mjs (understand existing formatter pattern to follow the same structure for the TS equivalent)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/style-dictionary.config.mjs (read current config to understand existing platforms and how to add new ones)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-CONTEXT.md (token naming conventions and tier architecture)
  </read_first>
  <action>
    Step 1: Create sd-transforms/ts-constants.format.mjs.
    Export { name: 'voltventure/ts/constants', format: ({ dictionary }) => { ... } }.
    The format function returns a string — the full voltventure_tokens.ts file content.

    File header: "// AUTO-GENERATED — DO NOT EDIT. Run 'npm run build:tokens' to regenerate.\n\n"

    For each token in dictionary.allTokens:
    - Primitive tokens (depth <= 2 or category matches primitive/): emit as `const` (unexported)
    - Semantic tokens: emit as `export const`
    - Colors: emit hex string value as-is: `export const colorActionPrimary = '#C6FF2D';`
    - Dimensions: emit as JS number: `export const space400 = 16;`
    - Shadows: emit as object literal with RN shadow props
    - Name transform: use token.name (already camelCase from existing transforms in dart platform)

    Note: SD v4 with usesDtcg: true stores values in token.$value (not token.value). The transforms have already run by the time the formatter executes, so token.$value contains the resolved/transformed value.

    Step 2: Create sd-transforms/rn-paper-theme.format.mjs.
    Export { name: 'voltventure/rn/theme', format: ({ dictionary }) => { ... } }.
    The format function returns the full voltventure_theme.ts file content.

    File content:
    - Header comment: AUTO-GENERATED
    - Import: `import { MD3LightTheme } from 'react-native-paper';`
    - Import: `import type { MD3Theme } from 'react-native-paper';`
    - Import: `import * as tokens from './voltventure_tokens';`
    - Export createVoltVentureTheme() using the spread strategy from the interfaces block
    - The formatter emits static TS code that references token constants by their export name
    - Override only the color roles where explicit semantic tokens exist: primary, onPrimary, secondary, onSecondary, background, surface, onSurface, outline

    Note: The rn/theme formatter is a static emitter — it does not iterate dictionary.allTokens for the theme structure. It emits hardcoded TS code that references the token export names from voltventure_tokens.ts. This is the same approach as the Dart ThemeData formatter (static code emission, not token-driven).

    Step 3: Update style-dictionary.config.mjs.
    - Register both new formats: sd.registerFormat({ name: 'voltventure/ts/constants', format: ... }) and sd.registerFormat({ name: 'voltventure/rn/theme', format: ... })
    - Add ts/constants platform targeting lib/voltventure_tokens.ts
    - Add rn/theme platform targeting lib/voltventure_theme.ts
    - The rn/theme platform needs at least one token to trigger SD (use a filter for semantic color tokens, or pass all tokens — the formatter ignores the dictionary anyway since it emits static code)
    - Keep the existing dart/constants and js/reference platforms unchanged (Dart output can remain as a legacy artifact)

    Run npm run build:tokens and verify both lib/voltventure_tokens.ts and lib/voltventure_theme.ts are created.
  </action>
  <acceptance_criteria>
    - sd-transforms/ts-constants.format.mjs exists and exports { name: 'voltventure/ts/constants', format: ... }
    - sd-transforms/rn-paper-theme.format.mjs exists and exports { name: 'voltventure/rn/theme', format: ... }
    - style-dictionary.config.mjs registers both new formats and platforms
    - npm run build:tokens exits 0
    - lib/voltventure_tokens.ts exists after run with AUTO-GENERATED header
    - lib/voltventure_theme.ts exists after run with AUTO-GENERATED header
    - lib/voltventure_tokens.ts exports camelCase constants (e.g. colorActionPrimary, space400, radiusXl)
    - lib/voltventure_theme.ts exports createVoltVentureTheme()
    - lib/voltventure_theme.ts contains MD3LightTheme spread
    - lib/voltventure_theme.ts imports from './voltventure_tokens'
    - lib/voltventure_theme.ts does NOT contain placeholder values or Colors.red equivalents
  </acceptance_criteria>
</task>

<task type="auto" id="1-07-02">
  <name>Task 2: Final Phase 1 done-bar verification</name>
  <files>lib/voltventure_tokens.ts, lib/voltventure_theme.ts</files>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/lib/voltventure_tokens.ts (verify content before running tsc)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/lib/voltventure_theme.ts (verify content before running tsc)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-CONTEXT.md (D-08: done-bar criteria)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-VALIDATION.md (full verification map)
  </read_first>
  <action>
    Run the complete Phase 1 done-bar in sequence. Each step must pass before proceeding.

    Step 1: npm run build:validate — all validators (V2 WCAG, V3 green guard, V4 grid, V8 DTCG) pass. Fix any failures in token source or validator before proceeding.

    Step 2: npm run build:tokens — both lib/voltventure_tokens.ts and lib/voltventure_theme.ts are generated. Fix any SD build failures before proceeding.

    Step 3: tsc --noEmit (from project root, uses tsconfig.json) — type-check both generated TS files. Must return 0 errors.
    If react-native-paper types are missing: add "react-native-paper" to devDependencies in package.json and run npm install.
    Common TS issues: (a) MD3Theme import path wrong — verify react-native-paper v5 exports MD3Theme from main index; (b) color role names wrong — verify against MD3LightTheme.colors keys in current package version; (c) spread type mismatch — ensure the return type annotation matches.

    Step 4: npm test — all 4 type conversion unit tests (color, dimension, shadow, lineHeight) still pass. The platform switch does not change the transform logic — tests should pass unchanged.

    When all 4 checks pass, record the exact command outputs in the SUMMARY.
  </action>
  <acceptance_criteria>
    - npm run build:validate exits 0
    - npm run build:tokens exits 0 producing both lib/voltventure_tokens.ts and lib/voltventure_theme.ts
    - tsc --noEmit exits 0 with 0 errors (both TS files type-check clean)
    - npm test exits 0 with all 4 type conversion unit tests passing
    - Phase 1 done bar (revised for RN Paper):
      - style-dictionary build exits 0 (checked via npm run build:tokens)
      - tsc --noEmit returns 0 errors
      - WCAG contrast check passes (checked via npm run build:validate)
      - All 4 type conversion unit tests pass (checked via npm test)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase 1 done-bar (revised for RN Paper) — all 4 must be green:

1. npm run build:validate → exits 0 (WCAG, green guard, grid, DTCG checks pass)
2. npm run build:tokens → exits 0 (both TS files generated, no SD errors)
3. tsc --noEmit → 0 errors (both voltventure_tokens.ts and voltventure_theme.ts type-check clean)
4. npm test → all 4 type conversion tests pass (color, dimension, shadow, lineHeight)

Additional checks:
5. grep -c "createVoltVentureTheme" lib/voltventure_theme.ts → at least 1
6. grep -c "MD3LightTheme" lib/voltventure_theme.ts → at least 1
7. grep -c "colorActionPrimary" lib/voltventure_tokens.ts → at least 1
</verification>

<success_criteria>
- All 4 Phase 1 done-bar criteria met (revised D-08):
  - npm run build exits 0
  - tsc --noEmit returns 0 errors
  - WCAG contrast passes as part of build
  - All 4 type conversion unit tests pass via npm test
- lib/voltventure_tokens.ts exports camelCase typed TS constants
- lib/voltventure_theme.ts exports createVoltVentureTheme() using RN Paper MD3 spread strategy
- No placeholder values in generated theme
- Phase 1 is declared complete and ready for Phase 2 (Storybook) planning
</success_criteria>

<output>
Create D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-07-SUMMARY.md when done.
</output>
