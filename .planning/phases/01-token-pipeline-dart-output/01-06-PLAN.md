---
phase: 01-token-pipeline-dart-output
plan: 06
name: SD Config Wiring and Dart Constants Output
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-04, 01-05]
files_modified:
  - style-dictionary.config.mjs
  - sd-transforms/dart-constants.format.mjs
  - lib/voltventure_tokens.dart
  - generated/tokens.js
autonomous: true
requirements: [D-04, D-05, D-06, D-08]

must_haves:
  goal: "The Style Dictionary pipeline is fully wired: custom transforms registered, Dart constants formatter implemented, npm run build:tokens generates lib/voltventure_tokens.dart and generated/tokens.js — dart analyze lib/voltventure_tokens.dart exits 0."
  truths:
    - "lib/voltventure_tokens.dart exists and is generated by npm run build:tokens"
    - "lib/voltventure_tokens.dart contains the AUTO-GENERATED header comment"
    - "lib/voltventure_tokens.dart contains const Color, const double, and const List<BoxShadow> declarations"
    - "Primitive token constants are private: _colorGreen500, _colorGrey050, etc. (underscore prefix)"
    - "Semantic token constants are public: colorActionPrimary, spacer400, radiusXl, etc. (no underscore)"
    - "Every exported constant has a Dart doc comment (/// ) describing its token path and usage"
    - "lib/voltventure_tokens.dart contains Color(0xFF...) format for all colors — no hex strings"
    - "generated/tokens.js exists with camelCase JS token values for Storybook"
    - "dart analyze lib/voltventure_tokens.dart exits 0 with 0 issues"
    - "No unresolved {reference} strings appear in lib/voltventure_tokens.dart"
  artifacts:
    - path: "lib/voltventure_tokens.dart"
      provides: "All generated Dart token constants"
      contains: "Color(0xFF"
    - path: "sd-transforms/dart-constants.format.mjs"
      provides: "Custom SD formatter that produces the Dart constants file"
    - path: "generated/tokens.js"
      provides: "JS reference token object for Storybook"
  key_links:
    - from: "style-dictionary.config.mjs"
      to: "sd-transforms/color.flutter.mjs"
      via: "StyleDictionary.registerTransform()"
      pattern: "registerTransform"
    - from: "style-dictionary.config.mjs"
      to: "sd-transforms/dart-constants.format.mjs"
      via: "StyleDictionary.registerFormat()"
      pattern: "registerFormat"
    - from: "lib/voltventure_tokens.dart"
      to: "dart analyze"
      via: "dart analyze lib/ command"
      pattern: "dart analyze"
---

<objective>
Wire the complete Style Dictionary pipeline: register the 4 custom transforms from Plan 02, implement the Dart constants formatter, update style-dictionary.config.mjs to use them, and produce lib/voltventure_tokens.dart that passes dart analyze.

Purpose: This is the pipeline integration — where all pieces from Waves 1-2 combine into the actual output. The Dart constants file is the primary deliverable of the entire phase. The formatter must correctly apply private/public visibility (D-06), doc comments (D-05), and the exact Flutter type formats (Color, double, List<BoxShadow>) that dart analyze accepts.

Output: lib/voltventure_tokens.dart (generated Dart constants), generated/tokens.js (JS reference), and the updated style-dictionary.config.mjs that drives both.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-CONTEXT.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/research/ARCHITECTURE.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/research/PITFALLS.md

<interfaces>
<!-- SD v4 format registration (from RESEARCH.md Q1) -->
<!-- A custom formatter is registered before buildAllPlatforms() is called: -->
<!--
StyleDictionary.registerFormat({
  name: 'voltventure/dart/constants',
  format: ({ dictionary, file }) => {
    // dictionary.allTokens — array of all resolved token objects
    // Each token: { name, path[], $type, value (resolved), $description, isSource, ... }
    // Build and return the Dart file content as a string
  }
});
-->

<!-- Token visibility rules (D-06): -->
<!-- Primitive tokens: path[0] is 'color', 'space', 'font', 'elevation', 'border', 'radius', 'grid', 'icon' AND they live in tokens/primitive/ -->
<!-- SD token.filePath contains the source file path — use this to distinguish primitive vs semantic -->
<!-- Alternative: check token.attributes.category or path length — primitive tokens have shorter paths (color.green.500 = 3 segments) vs semantic (color.action.primary = 3, color.action.primary.fg = 4) -->
<!-- CORRECT approach: Filter on token.isSource being true AND check the source file path contains '/primitive/' -->
<!-- Primitives → prefix with underscore in Dart: _colorGreen500 -->
<!-- Semantics → no underscore: colorActionPrimary -->

<!-- Dart naming convention: -->
<!-- SD name transform 'name/camelCase' converts color.action.primary → colorActionPrimary -->
<!-- For private primitives: prepend underscore → _colorActionPrimary (but primitives use the full path: _colorGreen500) -->

<!-- Dart output format for each type: -->
<!-- Color: const Color colorActionPrimary = Color(0xFFC6FF2D); -->
<!-- double: const double space400 = 16.0; -->
<!-- List<BoxShadow>: const List<BoxShadow> elevationRaised = [BoxShadow(color: Color(0x0F0F0F0F), offset: Offset(0.0, 2.0), blurRadius: 8.0, spreadRadius: 0.0)]; -->
<!-- elevation.flat (none): const List<BoxShadow> elevationFlat = []; -->
<!-- Typography: semantic type styles are NOT emitted as constants in the tokens file — they are used in voltventure_theme.dart's TextTheme instead -->

<!-- Doc comment format (D-05): -->
<!-- /// color.action.primary — Primary CTA background. Background only — pair with colorActionPrimaryFg. -->
<!-- const Color colorActionPrimary = Color(0xFFC6FF2D); -->

<!-- File header (required): -->
<!-- // AUTO-GENERATED — DO NOT EDIT. Run 'npm run build:tokens' to regenerate. -->
<!-- // Generated by Style Dictionary v4 with VoltVenture custom transforms. -->
<!-- import 'package:flutter/material.dart'; -->

<!-- JS reference output format (for Storybook in Phase 2): -->
<!-- generated/tokens.js uses SD's built-in javascript/es6 format with name/camelCase transform -->
<!-- Export shape: export const colorActionPrimary = '#C6FF2D'; export const space400 = 16; ... -->
</interfaces>
</context>

<tasks>

<task type="auto" id="1-06-01">
  <name>Task 1: Dart constants formatter and updated SD config</name>
  <files>sd-transforms/dart-constants.format.mjs, style-dictionary.config.mjs</files>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/sd-transforms/color.flutter.mjs (export name to import: colorFlutterTransform)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/sd-transforms/dimension.double.mjs (export name: dimensionDoubleTransform)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/sd-transforms/shadow.boxShadow.mjs (export name: shadowBoxShadowTransform)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/sd-transforms/lineHeight.multiplier.mjs (export name: lineHeightMultiplierTransform)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-CONTEXT.md (D-04 single file, D-05 doc comments, D-06 private primitives)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/research/PITFALLS.md (Pitfall F1 Color ARGB, Pitfall 5 double units, shadow BoxShadow)
  </read_first>
  <action>
    Create sd-transforms/dart-constants.format.mjs: export a formatter config object { name: 'voltventure/dart/constants', format: ({ dictionary }) => { ... } }. The format function receives the SD dictionary object with .allTokens array and must return a string (the full contents of voltventure_tokens.dart).

    File header: emit "// AUTO-GENERATED — DO NOT EDIT. Run 'npm run build:tokens' to regenerate.\n// Generated by Style Dictionary v4 + VoltVenture custom Dart transforms.\nimport 'package:flutter/material.dart';\n\n".

    Token categorization: Determine if a token is primitive by checking if the resolved token's filePath includes 'primitive'. Primitive tokens get an underscore-prefixed Dart name; semantic tokens do not. Apply name/camelCase transform result for the base name, then prefix _ for primitives.

    Emit tokens by section with section comments (// — Primitive: Color —, // — Semantic: Color —, etc.). Process tokens in this order: primitive colors, semantic colors, primitive spacing (doubles), semantic spacing if any, primitive radius (doubles), primitive elevation (BoxShadow lists), primitive border (doubles), primitive grid (doubles), primitive iconography (doubles).

    For each token: first emit the doc comment: "/// {token.path.join('.')} — {token.$description ?? 'No description'}". Then emit the Dart constant. Type mapping: $type 'color' → const Color {dartName} = {transformedValue};. $type 'dimension' → const double {dartName} = {transformedValue};. $type 'shadow' with value 'none' → const List<BoxShadow> {dartName} = [];. $type 'shadow' with object value → const List<BoxShadow> {dartName} = [{boxShadowString}];. $type 'typography' → SKIP (handled in voltventure_theme.dart, not constants file). fontFamily tokens (string $value) → SKIP (handled directly in ThemeData formatter). grid.columns (number, no $type) → const int {dartName} = {value};.

    Update style-dictionary.config.mjs: Import all 4 transforms and the dart-constants format. Before creating the StyleDictionary instance, call StyleDictionary.registerTransform() for each of the 4 transforms. Call StyleDictionary.registerFormat({ ...dartConstantsFormat }). Update the dart platform: transforms array to ['name/camelCase', 'voltventure/color/flutter', 'voltventure/dimension/double', 'voltventure/shadow/boxShadow'], format: 'voltventure/dart/constants'. Keep the js/reference platform using built-in 'javascript/es6' with 'name/camelCase' and 'color/hex' transforms. The dart/theme platform remains a stub (Plan 07 will wire it).

    Run npm run build:tokens and check that lib/voltventure_tokens.dart is generated.
  </action>
  <acceptance_criteria>
    - sd-transforms/dart-constants.format.mjs exists and exports a format config with name 'voltventure/dart/constants'
    - style-dictionary.config.mjs imports all 4 transforms from sd-transforms/ and registers them
    - style-dictionary.config.mjs registers the 'voltventure/dart/constants' format
    - npm run build:tokens exits 0
    - lib/voltventure_tokens.dart exists after running npm run build:tokens
    - lib/voltventure_tokens.dart first line is "// AUTO-GENERATED — DO NOT EDIT"
    - lib/voltventure_tokens.dart contains "import 'package:flutter/material.dart';"
    - lib/voltventure_tokens.dart contains at least one private constant starting with underscore (e.g., const Color _colorGreen500)
    - lib/voltventure_tokens.dart contains at least one public semantic constant (e.g., const Color colorActionPrimary)
    - generated/tokens.js exists after running npm run build:tokens
  </acceptance_criteria>
</task>

<task type="auto" id="1-06-02">
  <name>Task 2: Verify dart analyze + unresolved reference check</name>
  <files>lib/voltventure_tokens.dart</files>
  <read_first>
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/lib/voltventure_tokens.dart (generated in Task 1 — read the actual output to diagnose issues)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/research/PITFALLS.md (Pitfall F1: Color ARGB, Pitfall 5: dimension double)
    - D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-CONTEXT.md (D-08: done bar requires dart analyze 0 issues)
  </read_first>
  <action>
    Run dart analyze lib/ and inspect the output. If there are errors, diagnose and fix the formatter in sd-transforms/dart-constants.format.mjs. Common issues to check and fix:

    1. Color format: verify every color constant uses Color(0xFFRRGGBB) or Color(0xAARRGGBB) for alpha-included colors. A Color(0xC6FF2D) without the FF prefix is a syntax that compiles but produces wrong color — search for Color(0x without the FF prefix.

    2. Double format: verify dimension constants are N.0 (16.0 not 16, not '16.0' with quotes). The Dart const double requires a literal double; writing 16 (integer literal) in a const double declaration causes a Dart analyzer warning in strict mode.

    3. BoxShadow: verify the syntax is BoxShadow(color: Color(0x...), offset: Offset(x.0, y.0), blurRadius: b.0, spreadRadius: s.0) with no trailing commas that break Dart syntax.

    4. List<BoxShadow>: verify the const keyword is present. Dart requires `const List<BoxShadow>` not just `List<BoxShadow>` for compile-time constants.

    5. Doc comments: verify /// comments are on the line immediately before the const declaration, not separated by blank lines (blank lines between /// and const break Dart doc association).

    Run the unresolved reference check: node -v scripts/check-unresolved.mjs if it exists, or run inline: node -e "import('node:fs').then(({default:fs})=>{const f=fs.readFileSync('lib/voltventure_tokens.dart','utf8');const matches=f.match(/\{[a-z]/g);if(matches){console.error('UNRESOLVED REFERENCES:',matches);process.exit(1);}else console.log('OK: no unresolved references');})"

    Update scripts/validate-tokens.mjs to add the V1 post-build unresolved reference check as an active validator (not a stub). This check reads lib/voltventure_tokens.dart after it exists and fails if it finds any {color. or {space. strings.

    Iterate on the formatter until dart analyze lib/ returns "No issues found!" and the unresolved reference check passes.
  </action>
  <acceptance_criteria>
    - dart analyze lib/ exits 0 with output "No issues found!" (or equivalent zero-issue message)
    - lib/voltventure_tokens.dart contains no raw hex strings (grep for '"#' in the dart file returns 0 matches)
    - lib/voltventure_tokens.dart contains no unresolved reference strings (grep for '{color.' or '{space.' returns 0 matches)
    - lib/voltventure_tokens.dart contains Color(0xFF as substring (verifying ARGB prefix is present)
    - All doc comments use /// prefix (triple-slash Dart doc style)
    - Private primitive constants start with underscore: grep for 'const Color _' returns at least 1 match
    - Public semantic constants have no underscore: grep for 'const Color colorAction' returns at least 1 match
    - npm run build exits 0 (build:validate + build:tokens both succeed)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SD formatter → lib/ write | The formatter writes to lib/voltventure_tokens.dart; malformed output fails dart analyze but is safe |
| generated/ → Storybook | tokens.js is read-only build artifact; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | lib/voltventure_tokens.dart (generated file) | mitigate | AUTO-GENERATED header discourages manual edits; Plan 06 V1 check catches unresolved refs; dart analyze catches type errors |
| T-06-02 | Information Disclosure | None — generated Dart constants are public design values | accept | No PII, no credentials in token constants |
| T-06-03 | Tampering | Incorrect Color ARGB prefix | mitigate | Acceptance criteria assert Color(0xFF presence; dart analyze would catch type error if format were wrong |
</threat_model>

<verification>
1. npm run build exits 0 (both validate and build:tokens)
2. dart analyze lib/ → "No issues found!" (exactly 0 issues)
3. grep -c "Color(0xFF" lib/voltventure_tokens.dart → at least 5 (one per color primitive)
4. grep -c "const double" lib/voltventure_tokens.dart → at least 11 (spacing steps)
5. grep -c "List<BoxShadow>" lib/voltventure_tokens.dart → at least 4 (elevation levels)
6. grep -c "///" lib/voltventure_tokens.dart → at least 20 (doc comments on each constant)
7. head -1 lib/voltventure_tokens.dart → "// AUTO-GENERATED — DO NOT EDIT"
8. grep -v '^#' lib/voltventure_tokens.dart | grep -c "{color\." → 0 (no unresolved refs)
</verification>

<success_criteria>
- dart analyze lib/ returns 0 issues (D-08 partial — voltventure_tokens.dart passes)
- lib/voltventure_tokens.dart contains private primitives (underscore) and public semantics (D-06)
- Every public constant has a Dart doc comment (D-05)
- No raw hex strings or unresolved references in generated file
- generated/tokens.js exists for Phase 2 Storybook consumption
- npm run build exits 0 end-to-end
</success_criteria>

<output>
Create D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/01-token-pipeline-dart-output/01-06-SUMMARY.md when done.
</output>
