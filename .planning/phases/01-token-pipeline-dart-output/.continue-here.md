---
context: phase
phase: 01-token-pipeline-dart-output
task: 2
total_tasks: 2
plan: 06
status: in_progress
last_updated: 2026-07-24T07:48:58Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: JS-in-Dart — `lib/voltventure_tokens.dart` and `lib/voltventure_theme.dart` contain JavaScript `export const` content (SD built-in `javascript/es6` format), NOT Dart code. The SD config is not routing the dart platform through the custom `voltventure/dart/constants` formatter. Fix the config wiring before running any `dart analyze` or testing.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong format in dart platform | SD config used `javascript/es6` (or default) for the dart platform output, writing JS content to `.dart` files | blocking | Before running `npm run build:tokens`, read `style-dictionary.config.mjs` and verify the dart platform entry explicitly sets `format: 'voltventure/dart/constants'` — not a built-in SD format |
| Assume generated file is correct | Plan 06 was WIP-committed but `dart analyze` was never confirmed clean | advisory | Always run `dart analyze lib/` after build and read the output before claiming a plan complete |

_The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Paused mid-Plan 01-06 (SD Config Wiring and Dart Constants Output), Task 2 (Verify dart analyze + unresolved reference check). Context exhausted at ~79% during Plan 06 execution.

CRITICAL: `lib/voltventure_tokens.dart` and `lib/voltventure_theme.dart` both contain JavaScript-format content (`export const borderWidthNone = 0;` etc.) — they are named `.dart` but hold JS output. The `dart-constants.format.mjs` formatter exists in `sd-transforms/` and was committed, but the SD config is not routing the dart platform through it. Plan 06 Task 1 is incomplete.

Plan 01-07 (ThemeData Factory) has NOT been started — `lib/voltventure_theme.dart` exists but contains JS content from the wrong SD formatter.
</current_state>

<completed_work>

Completed Plans (01-01 through 01-05 — all have SUMMARY.md):
- Plan 01-01: Project Scaffold — Done
- Plan 01-02: SD Transforms + Unit Tests — Done
- Plan 01-03: Primitive Tokens (8 categories) — Done
- Plan 01-04: Semantic Tokens — Done
- Plan 01-05: Build Validators (WCAG, green guard, 4pt grid, DTCG) — Done

Plan 01-06 — Partial:
- Task 1: dart-constants.format.mjs exists in sd-transforms/ and was committed (WIP: ce7f018). style-dictionary.config.mjs was updated but the dart platform format wiring is BROKEN (outputs JS to .dart files). Task 1 is NOT complete.
- Task 2: Not reached — dart analyze has not been run successfully.
</completed_work>

<remaining_work>

Plan 01-06 — Task 1 (fix) and Task 2:
- Fix style-dictionary.config.mjs: verify dart platform uses `format: 'voltventure/dart/constants'` (the custom formatter, not any built-in). Re-run `npm run build:tokens`. Verify `lib/voltventure_tokens.dart` starts with `// AUTO-GENERATED` and contains `const Color`, `const double`, `const List<BoxShadow>`.
- Task 2: Run `dart analyze lib/` — iterate until 0 issues. Run unresolved reference check. Ensure private primitives (`_colorGreen500`) and public semantics (`colorActionPrimary`) are correct.
- Write `01-06-SUMMARY.md` when done.

Plan 01-07 — ThemeData Factory (Wave 4, depends on 01-06):
- Task 0 (checkpoint): Verify `GoogleFonts.manjari()` availability in google_fonts ^6.2.0
- Task 1: Create `sd-transforms/dart-theme.format.mjs`, wire dart/theme platform in SD config, generate `lib/voltventure_theme.dart`
- Task 2: Final Phase 1 done-bar verification — `npm run build:validate` + `npm run build:tokens` + `dart analyze lib/` (0 issues) + `npm test` (4 unit tests pass)
- Write `01-07-SUMMARY.md` when done.
</remaining_work>

<decisions_made>

- Platform: Flutter (not React Native) — locked
- SD v4 with W3C DTCG JSON source — locked
- dart-constants formatter: custom (no community one found) — locked
- Dart output: single `voltventure_tokens.dart` (not per-category barrel) — locked
- Primitive tokens private (`_colorGreen500`); semantic tokens public (`colorActionPrimary`) — locked
- ColorScheme: `ColorScheme.fromSeed(seedColor: colorActionPrimary).copyWith(...)` — no red placeholders (D-07) — locked
- Phase 1 done bar: `dart analyze lib/` + `npm run build` + 4 unit tests (no device required) — locked
</decisions_made>

<blockers>
- BLOCKING: `lib/voltventure_tokens.dart` and `lib/voltventure_theme.dart` contain JS content (wrong SD formatter). Must fix `style-dictionary.config.mjs` dart platform wiring before Plan 06 can complete.
- Uncommitted: `.planning/STATE.md` (modified), `voltventure-foundations (1).html` (untracked — ignore for now, unrelated to pipeline work)
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-token-pipeline-dart-output/01-06-PLAN.md` — exact Task 1 fix steps and acceptance criteria
2. `sd-transforms/dart-constants.format.mjs` — verify the formatter exists and exports the correct `name: 'voltventure/dart/constants'`
3. `style-dictionary.config.mjs` — identify why the dart platform is using the wrong format
4. `.planning/phases/01-token-pipeline-dart-output/01-CONTEXT.md` — D-04 through D-08 decisions that govern the formatter
5. `.planning/research/PITFALLS.md` — Pitfall F1 (Color ARGB), Pitfall 5 (dimension double), shadow BoxShadow

## Infrastructure State
- Node.js/npm: project in `D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/`
- `npm run build:tokens` — currently generates JS content to `.dart` files (broken)
- `npm run build:validate` — last run clean in Plan 05 (validators still pass)
- `npm test` — 4 unit tests passing as of Plan 02 (unchanged)
- Dart SDK: available (≥3.19 confirmed during scaffold)
- Flutter: available (≥3.19 confirmed during scaffold)

<context>
Context exhausted at ~79% through phase execution (after 5 of 7 plans complete). Plan 06 was the wave-3 pipeline integration plan — the most complex plan in the phase. The dart-constants formatter was written and committed, but SD config wiring didn't connect it to the dart platform output. The generated `.dart` files in `lib/` contain JS instead of Dart code. This is a single config fix (changing the `format:` field in the dart platform entry in `style-dictionary.config.mjs`) but requires careful reading of the current config and formatter export name before patching.

After Plan 06 is fixed and complete, Plan 07 (ThemeData Factory) is the final wave — it has a human-verify checkpoint for `GoogleFonts.manjari()` availability before auto tasks run.
</context>

<next_action>
Start with: Read `style-dictionary.config.mjs` to identify the dart platform format field, then read `sd-transforms/dart-constants.format.mjs` to confirm its exported name. Fix the mismatch, run `npm run build:tokens`, and verify `lib/voltventure_tokens.dart` starts with `// AUTO-GENERATED` and contains Dart `const Color` declarations.
</next_action>
