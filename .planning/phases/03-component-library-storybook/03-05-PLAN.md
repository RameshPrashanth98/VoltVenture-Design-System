---
phase: 03-component-library-storybook
plan: 05
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - stories/screens/splash.stories.js
  - stories/screens/onboarding-1.stories.js
  - stories/screens/registration.stories.js
  - stories/screens/login.stories.js
autonomous: true
requirements:
  - D-02
  - D-03
  - D-05

must_haves:
  truths:
    - "Splash screen story exists with single Default export at 393x852px canvas"
    - "Onboarding1 screen story exists with single Default export"
    - "Registration screen story exists with single Default export"
    - "Login screen story exists with TWO exports: PhoneTab and EmailTab (not two files)"
    - "All 4 screen stories use flex column layout with colorSurfaceBase background"
    - "All 4 files import tokens from ../../generated/tokens.js"
    - "No hexToRgba helper needed — no elevation shadows in auth screens"
    - "Named exports are PascalCase"
  artifacts:
    - path: "stories/screens/splash.stories.js"
      provides: "Splash screen story"
      exports: ["Default"]
    - path: "stories/screens/onboarding-1.stories.js"
      provides: "Onboarding1 screen story"
      exports: ["Default"]
    - path: "stories/screens/registration.stories.js"
      provides: "Registration screen story"
      exports: ["Default"]
    - path: "stories/screens/login.stories.js"
      provides: "Login screen story (two Hi-Fi frames yfZaz + TS9Td → one file)"
      exports: ["PhoneTab", "EmailTab"]
  key_links:
    - from: "stories/screens/login.stories.js"
      to: "03-RESEARCH.md Section 1"
      via: "Login frame yfZaz (Phone tab) and TS9Td (duplicate/Email tab) → one story file"
      pattern: "PhoneTab|EmailTab"
---

<objective>
Author the four authentication-flow screen stories: Splash, Onboarding1, Registration, and Login. All use flex-column layout (no absolute positioning). The Login screen is a special case: two Hi-Fi frames in the wireframe (yfZaz = Phone tab, TS9Td = Email tab duplicate) map to ONE story file with TWO named exports using a helper function.

Purpose: These are the first 4 of 9 screen stories in the Screens/ section. They establish the non-map screen layout pattern (393px canvas, flex column, status bar header, scrollable content). Parallel to 03-06 (KYC screens) and 03-07 (map screens).
Output: 4 .stories.js files in stories/screens/
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-RESEARCH.md

<interfaces>
<!-- Screen root element — non-map screens (flex column layout) -->
<!-- Analog: stories/grid.stories.js lines 17-26 (read this file for the 393px canvas pattern) -->
Root div style:
  width:393px
  min-height:852px
  display:flex
  flex-direction:column
  background:${tokens.colorSurfaceBase}
  position:relative
  overflow:hidden
  box-sizing:border-box

Import path (two levels up from stories/screens/):
  import * as tokens from '../../generated/tokens.js';

<!-- Key tokens for auth screens -->
colorSurfaceBase     = #ffffff   (screen background)
colorTextPrimary     = #0f0f0f   (primary text, headings)
colorTextSecondary   = #808080   (subtitle, placeholder, step labels)
colorActionPrimary   = #c6ff2d   (green: loader bar fill, Next button, pagination active dot)
colorGrey200         = #ebebeb   (pagination inactive dots, disabled elements)
colorGrey300         = #c9c9c9   (splash background pattern dots)

typeDisplayXl        composite   (splash brand name: 40px/Manjari/700)
typeDisplayMd        composite   (onboarding headline: 28px/Manjari/700)
typeHeadingLg        composite   (registration page title: 20px/Inter/600)
typeHeadingMd        composite   (login screen header: 17px/Inter/600)
typeBodyMd           composite   (body text, social button labels: 15px/Inter/400)
typeBodySm           composite   (captions, terms text: 13px/Inter/400)
typeLabelMd          composite   (status bar time: 13px/Inter/600)
typeLabelSm          composite   (status icons: 11px/Inter/500)

space100=4  space200=8  space300=12  space400=16  space600=24  space800=32  space1200=48
radiusFull=999

<!-- StatusBar inline pattern (no import of component — inline the markup) -->
StatusBar HTML (light surface):
  <div style="width:393px;height:44px;display:flex;align-items:center;
    justify-content:space-between;padding:0 ${tokens.space400}px;
    background:${tokens.colorSurfaceBase};box-sizing:border-box;">
    <span style="font-family:'${tokens.typeLabelMd.fontFamily}',sans-serif;
      font-size:${tokens.typeLabelMd.fontSize}px;font-weight:${tokens.typeLabelMd.fontWeight};
      color:${tokens.colorTextPrimary};">9:41</span>
    <span style="font-size:${tokens.typeLabelSm.fontSize}px;color:${tokens.colorTextPrimary};
      letter-spacing:2px;">▲ WiFi ■</span>
  </div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Splash + Onboarding1 screen stories</name>
  <files>stories/screens/splash.stories.js, stories/screens/onboarding-1.stories.js</files>
  <read_first>
    - `stories/grid.stories.js` — analog: 393px fixed-width canvas, single named export; read full file
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — sections "splash.stories.js" and "onboarding-1.stories.js" for layout structure
    - `.planning/phases/03-component-library-storybook/03-RESEARCH.md` Section 1 — Splash frame O94n2 and Onboarding 1 frame WSGRc content descriptions
  </read_first>
  <action>
    Create `stories/screens/splash.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Screens/Splash' };`
    - Export `Default`: root div (`width:393px`, `min-height:852px`, `display:flex`, `flex-direction:column`, `align-items:center`, `justify-content:center`, `background:${tokens.colorSurfaceBase}`, `position:relative`, `overflow:hidden`, `box-sizing:border-box`). Content:
      - Background pattern layer: absolute-positioned div (`position:absolute`, `inset:0`) with repeating-dot pattern approximation — use `background:radial-gradient(circle, ${tokens.colorGrey300} 1px, transparent 1px)`, `background-size:24px 24px`.
      - Content wrapper (position:relative, z-index:1, text-align:center, padding:space400):
        - Logo badge: 64x64px circle div, `background:${tokens.colorTextPrimary}`, `border-radius:${tokens.radiusFull}px`, inline-flex, centered, contains "V" letter in white font
        - Brand name: `typeDisplayXl` composite (40px/Manjari/700), `color:${tokens.colorTextPrimary}`, text "VoltVenture", `margin-top:${tokens.space300}px`
        - Tagline: `typeBodyMd` composite, `color:${tokens.colorTextSecondary}`, text "Ride. Discover. Sustain.", `margin-top:${tokens.space200}px`
        - Loader track: flex row, `width:120px`, `height:4px`, `background:${tokens.colorGrey200}`, `border-radius:${tokens.radiusFull}px`, `margin:${tokens.space800}px auto 0`. Inner progress fill: `width:40%`, `height:100%`, `background:${tokens.colorActionPrimary}`, `border-radius:${tokens.radiusFull}px`.
        - Version label: `typeLabelSm` composite, `color:${tokens.colorTextSecondary}`, text "v1.0.0", `margin-top:${tokens.space400}px`

    Create `stories/screens/onboarding-1.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Screens/Onboarding1' };`
    - Export `Default`: root div (`width:393px`, `min-height:852px`, `display:flex`, `flex-direction:column`, `background:${tokens.colorSurfaceBase}`, `overflow:hidden`, `box-sizing:border-box`). Content (top to bottom):
      - StatusBar: inline the light-surface markup from the interfaces section above
      - Skip link row: `display:flex`, `justify-content:flex-end`, `padding:${tokens.space200}px ${tokens.space400}px`. Child: skip link span "Skip" in `typeBodyMd` composite, `color:${tokens.colorTextSecondary}`
      - Illustration placeholder: `flex:1`, `background:${tokens.colorGrey200}`, `display:flex`, `align-items:center`, `justify-content:center`. Inner text "[ Illustration ]" in `typeBodySm` composite, `color:${tokens.colorTextSecondary}`
      - Pagination dots row: `display:flex`, `justify-content:center`, `gap:${tokens.space200}px`, `padding:${tokens.space300}px`. 3 dot divs: first (active) `width:24px`, `height:8px`, `border-radius:${tokens.radiusFull}px`, `background:${tokens.colorActionPrimary}`; second and third `width:8px`, `height:8px`, `border-radius:${tokens.radiusFull}px`, `background:${tokens.colorGrey200}`
      - Headline: `padding:0 ${tokens.space400}px`, `typeDisplayMd` composite (28px/Manjari/700), `color:${tokens.colorTextPrimary}`, text "Ride green, explore more"
      - Subtext: `padding:${tokens.space200}px ${tokens.space400}px`, `typeBodyMd` composite, `color:${tokens.colorTextSecondary}`, text "Unlock sustainable rides across your city."
      - Next button: `margin:${tokens.space400}px ${tokens.space400}px ${tokens.space800}px`, green pill button (`background:${tokens.colorActionPrimary}`, `color:${tokens.colorTextPrimary}`, `border-radius:${tokens.radiusFull}px`, `min-height:${tokens.space1200}px`, `border:none`, `cursor:pointer`, `display:flex`, `align-items:center`, `justify-content:center`, `gap:${tokens.space200}px`), font from `typeHeadingSm` composite. Label: "Next →"
  </action>
  <verify>
    <automated>
      node -e "
        Promise.all([
          import('./stories/screens/splash.stories.js'),
          import('./stories/screens/onboarding-1.stories.js'),
        ]).then(([sp, ob]) => {
          if (!sp.Default) throw new Error('Splash missing Default');
          if (!ob.Default) throw new Error('Onboarding1 missing Default');
          if (!sp.Default().includes('VoltVenture')) throw new Error('Splash missing brand name');
          if (!ob.Default().includes('Ride green')) throw new Error('Onboarding1 missing headline');
          console.log('splash and onboarding-1 OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/screens/splash.stories.js` exists
    - File contains: `export const Default`
    - File contains: `title: 'Screens/Splash'`
    - File contains: `tokens.typeDisplayXl.fontFamily` or `.fontSize` (composite)
    - File contains: `tokens.colorActionPrimary` (loader progress fill)
    - File contains: `width:393px` and `min-height:852px` on root div
    - `stories/screens/onboarding-1.stories.js` exists
    - File contains: `export const Default`
    - File contains: `title: 'Screens/Onboarding1'`
    - File contains: `tokens.typeDisplayMd` (headline composite)
    - File contains: `tokens.colorActionPrimary` (active pagination dot)
    - Both files contain: `import * as tokens from '../../generated/tokens.js'`
    - Neither file contains `hexToRgba` (not needed for auth screens)
  </acceptance_criteria>
  <done>Splash story: centered layout with background pattern, logo badge, brand name, tagline, loader track, version. Onboarding1 story: status bar, skip link, illustration placeholder, pagination dots, headline, subtext, next button.</done>
</task>

<task type="auto">
  <name>Task 2: Registration + Login screen stories</name>
  <files>stories/screens/registration.stories.js, stories/screens/login.stories.js</files>
  <read_first>
    - `stories/color.stories.js` — analog for login (multiple named exports per file using helper fn); read lines 18–70
    - `stories/grid.stories.js` — analog for registration (single export, 393px canvas); read full file
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — sections "registration.stories.js" and "login.stories.js" for exact layout structure
    - `.planning/phases/03-component-library-storybook/03-RESEARCH.md` Section 1 — Registration frame Y9ojN and Login frames yfZaz / TS9Td content descriptions
    - Note on Login duplicate: frames yfZaz and TS9Td are structural duplicates (side-by-side design review copies). Map to ONE file with PhoneTab and EmailTab exports using helper function.
  </read_first>
  <action>
    Create `stories/screens/registration.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Screens/Registration' };`
    - Export `Default`: root div (`width:393px`, `min-height:852px`, `display:flex`, `flex-direction:column`, `background:${tokens.colorSurfaceBase}`, `box-sizing:border-box`, `padding:${tokens.space400}px`). Content (top to bottom):
      - StatusBar inline markup (light surface)
      - Logo row: centered row, `margin:${tokens.space800}px 0 ${tokens.space400}px`. Pill div (`background:${tokens.colorTextPrimary}`, `border-radius:${tokens.radiusFull}px`, 40x40px inline-flex centered) with "V" in white
      - Page title: `typeHeadingLg` composite, `color:${tokens.colorTextPrimary}`, text "Create your account", `text-align:center`
      - Subtitle: `typeBodyMd` composite, `color:${tokens.colorTextSecondary}`, text "Join VoltVenture and ride sustainably.", `text-align:center`, `margin-bottom:${tokens.space600}px`
      - Apple button: green pill button (`background:${tokens.colorActionPrimary}`, `color:${tokens.colorTextPrimary}`, `border-radius:${tokens.radiusFull}px`, `min-height:${tokens.space1200}px`, `width:100%`, `border:none`, `cursor:pointer`, `box-sizing:border-box`, `margin-bottom:${tokens.space300}px`), font from `typeBodyMd` composite. Label "  Continue with Apple"
      - Google button: white pill button (`background:${tokens.colorSurfaceBase}`, `color:${tokens.colorTextPrimary}`, `border:1px solid ${tokens.colorBorderSubtle}`, same sizing as Apple button, `margin-bottom:${tokens.space400}px`). Label "G  Continue with Google"
      - OR divider: flex row with two `colorBorderSubtle` lines flanking "OR" text in `typeLabelSm` composite, `color:${tokens.colorTextSecondary}`, `margin-bottom:${tokens.space400}px`
      - WhatsApp phone button: green pill button (same style as Apple button), label "📱 Continue with WhatsApp"
      - Email link: ghost-style button (`background:none`, `border:none`, `color:${tokens.colorTextPrimary}`, `text-decoration:underline`, `cursor:pointer`, `width:100%`, `margin-top:${tokens.space200}px`), font `typeBodyMd`. Label "Use email instead"
      - Sign-in anchor: center-aligned text `typeBodySm` composite, `color:${tokens.colorTextSecondary}`, `margin-top:${tokens.space400}px`. Text: "Already have an account? Sign in"
      - Terms row: `display:flex`, `gap:${tokens.space200}px`, `align-items:flex-start`, `margin-top:${tokens.space400}px`. Checkbox placeholder (16x16px `border:1px solid ${tokens.colorBorderSubtle}`, `border-radius:4px`, `flex-shrink:0`) + terms text in `typeBodySm`, `color:${tokens.colorTextSecondary}`. Text: "I agree to the Terms of Service and Privacy Policy"

    Create `stories/screens/login.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Screens/Login' };`
    - Define helper function `loginScreen(activeTab)` that returns a full 393px screen HTML string. The `activeTab` parameter is 'phone' or 'email' and controls which segment of the toggle appears active.
    - Screen content (top to bottom):
      - StatusBar inline markup (light surface)
      - Header row: back arrow "←" + title "Sign in" in `typeHeadingMd` composite, `margin-bottom:${tokens.space600}px`, `padding:${tokens.space400}px`
      - Apple button (same as Registration — green pill, `margin-bottom:${tokens.space300}px`)
      - Google button (same as Registration — white pill with border, `margin-bottom:${tokens.space400}px`)
      - OR divider (same inline pattern as Registration)
      - SegmentedToggle: outer pill `background:${tokens.colorGrey100}`, `border-radius:${tokens.radiusFull}px`, `padding:4px`, `display:flex`, `gap:4px`, `margin:${tokens.space400}px 0`. Phone segment: active when activeTab === 'phone' (`background:${tokens.colorActionPrimary}`, `color:${tokens.colorTextPrimary}`), inactive (`background:transparent`, `color:${tokens.colorTextSecondary}`). Email segment: inverse. Both in `typeHeadingSm` composite.
      - PhoneInput row (shown in both variants for simplicity): `background:${tokens.colorGrey050}`, `border-radius:${tokens.radiusSm}px`, `display:flex`, `align-items:center`, `min-height:${tokens.space1200}px`, `padding:0 ${tokens.space400}px`, `margin-bottom:${tokens.space400}px`. Content: "+91" + divider + placeholder/email hint in `typeBodyLg` composite
      - Continue button: green pill button, label "Continue", `margin-bottom:${tokens.space400}px`
      - Sign-up link: center-aligned, `typeBodySm` composite, `color:${tokens.colorTextSecondary}`. Text: "Don't have an account? Sign up"
    - Export `PhoneTab = () => loginScreen('phone')`
    - Export `EmailTab = () => loginScreen('email')`

    NOTE: Login is ONE file with TWO exports — this is the correct mapping for the two Hi-Fi frames (yfZaz = Phone tab, TS9Td = Email tab).
  </action>
  <verify>
    <automated>
      node -e "
        Promise.all([
          import('./stories/screens/registration.stories.js'),
          import('./stories/screens/login.stories.js'),
        ]).then(([reg, login]) => {
          if (!reg.Default) throw new Error('Registration missing Default');
          if (!login.PhoneTab || !login.EmailTab) throw new Error('Login missing PhoneTab or EmailTab');
          const loginHtml = login.PhoneTab();
          if (!loginHtml.includes('393px')) throw new Error('Login missing 393px width');
          console.log('registration and login OK');
          console.log('Login exports:', Object.keys(login).filter(k=>k!=='default').join(', '));
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/screens/registration.stories.js` exists
    - File contains: `export const Default`
    - File contains: `title: 'Screens/Registration'`
    - File contains: `width:393px` and `min-height:852px`
    - File contains: `tokens.colorActionPrimary` (Apple/WhatsApp button bg)
    - File contains: `tokens.colorBorderSubtle` (Google button border, OR divider)
    - `stories/screens/login.stories.js` exists
    - File contains: `export const PhoneTab`, `export const EmailTab`
    - File does NOT contain `export const Default` (Login has only PhoneTab and EmailTab)
    - File contains: `title: 'Screens/Login'`
    - File contains: `tokens.colorGrey100` (toggle outer bg) and `tokens.colorActionPrimary` (active segment)
    - File contains: `tokens.colorGrey050` (phone input bg)
    - Both files: `import * as tokens from '../../generated/tokens.js'`
    - Neither file imports hexToRgba (not needed — no elevation in auth screens)
  </acceptance_criteria>
  <done>Registration story: full flow from status bar through social auth, OR divider, WhatsApp/email buttons, sign-in anchor, terms. Login story: ONE file with PhoneTab and EmailTab exports via helper function; both show the sign-in flow with segmented toggle state difference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static story files | HTML template literal strings; no user input, no auth surface, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | story HTML | accept | Static dev documentation; no real auth tokens, no PII, sample text only |
| T-03-05-SC | Tampering | npm/pip/cargo installs | accept | No new installs; phase uses only existing packages |
</threat_model>

<verification>
After both tasks:
1. `grep "title:" stories/screens/splash.stories.js stories/screens/onboarding-1.stories.js stories/screens/registration.stories.js stories/screens/login.stories.js` — 4 matches with correct titles
2. `grep "width:393px" stories/screens/splash.stories.js stories/screens/onboarding-1.stories.js stories/screens/registration.stories.js stories/screens/login.stories.js` — 4 matches
3. `grep "export const PhoneTab\|export const EmailTab" stories/screens/login.stories.js` — 2 matches
4. `grep "export const Default" stories/screens/login.stories.js` — 0 matches (login has no Default export)
</verification>

<success_criteria>
- 4 screen story files created in stories/screens/
- All import from ../../generated/tokens.js
- Login is a single file (login.stories.js) with PhoneTab and EmailTab exports — NOT two separate files
- 393px canvas width on all screen roots
- No hexToRgba helper in any of these 4 files (auth screens have no elevation tokens)
- Typography tokens accessed as composite objects
</success_criteria>

<output>
Create `.planning/phases/03-component-library-storybook/03-05-SUMMARY.md` when done
</output>
