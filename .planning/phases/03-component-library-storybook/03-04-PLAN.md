---
phase: 03-component-library-storybook
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - stories/components/tab-bar.stories.js
  - stories/components/bottom-card.stories.js
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04

must_haves:
  truths:
    - "TabBar story exists with RideActive, DiscoverActive, WalletActive, AccountActive variants"
    - "BottomCard story exists with BikeSelection and WalkProgress variants"
    - "Both files contain hexToRgba and shadowFromToken helpers copied from elevation.stories.js"
    - "TabBar uses elevationFloating for box-shadow"
    - "BottomCard uses elevationRaised for box-shadow"
    - "Both files import from ../../generated/tokens.js"
    - "Typography composite token access is correct throughout"
  artifacts:
    - path: "stories/components/tab-bar.stories.js"
      provides: "TabBar component story"
      exports: ["RideActive", "DiscoverActive", "WalletActive", "AccountActive"]
    - path: "stories/components/bottom-card.stories.js"
      provides: "BottomCard component story"
      exports: ["BikeSelection", "WalkProgress"]
  key_links:
    - from: "stories/components/tab-bar.stories.js"
      to: "generated/tokens.js"
      via: "shadowFromToken(tokens.elevationFloating)"
      pattern: "elevationFloating"
    - from: "stories/components/bottom-card.stories.js"
      to: "generated/tokens.js"
      via: "shadowFromToken(tokens.elevationRaised)"
      pattern: "elevationRaised"
---

<objective>
Author the two most complex component stories: TabBar (4 active-state variants, elevation shadow, array-map pattern) and BottomCard (2 structurally distinct layouts, elevation shadow). Both require the hexToRgba and shadowFromToken helpers copied verbatim from stories/elevation.stories.js — the helper is NOT exported from that file, so it must be duplicated.

Purpose: TabBar appears in HomeMap and NavigateToBike screens; BottomCard appears in NavigateToBike and WalkingDirections. Screen story authors in Wave 3 (plans 03-07) will reference these components inline. The elevation shadow pattern established here is also reused in the map screen stories.
Output: 2 .stories.js files in stories/components/
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md

<interfaces>
<!-- hexToRgba + shadowFromToken helper — COPY VERBATIM from stories/elevation.stories.js -->
<!-- DO NOT import from elevation.stories.js — the helper is not exported -->
function hexToRgba(hex8) {
  const r = parseInt(hex8.slice(1, 3), 16);
  const g = parseInt(hex8.slice(3, 5), 16);
  const b = parseInt(hex8.slice(5, 7), 16);
  const a = (parseInt(hex8.slice(7, 9), 16) / 255).toFixed(2);
  return 'rgba(' + r + ', ' + g + ', ' + b + ', ' + a + ')';
}
function shadowFromToken(token) {
  if (token === 'none') return 'none';
  return token.offsetX + 'px ' + token.offsetY + 'px ' + token.blur + 'px ' + token.spread + 'px ' + hexToRgba(token.color);
}

<!-- Elevation token structure (from generated/tokens.js) -->
elevationFloating  — { offsetX, offsetY, blur, spread, color: '#RRGGBBAA' }
elevationRaised    — { offsetX, offsetY, blur, spread, color: '#RRGGBBAA' }

<!-- TabBar tokens -->
colorSurfaceBase     = #ffffff   (bar background)
colorTextPrimary     = #0f0f0f   (active tab pill fill — black circle)
colorTextOnInverse   = #ffffff   (active tab icon + label — white on black circle)
colorGrey200         = #ebebeb   (inactive tab circle)
colorTextSecondary   = #808080   (inactive tab icon + label)
elevationFloating    — object    (tab bar shadow)
radiusFull           = 999       (active tab pill)
typeLabelSm          composite   (tab labels: 11px/Inter/500 — use .fontSize, .fontWeight, .fontFamily)
space100             = 4
space200             = 8
space400             = 16
space500             = 20

<!-- BottomCard tokens -->
colorSurfaceBase     = #ffffff   (card background)
colorTextPrimary     = #0f0f0f   (primary text)
colorTextSecondary   = #808080   (secondary labels: Distance, ETA)
colorBorderSubtle    = #ebebeb   (card divider)
colorGrey100         = #f5f5f5   (distance badge bg)
colorGrey700         = #4a4a4a   (distance badge text)
colorGreen100        = #f4ffd9   (bike chip background)
colorGreen700        = #7d9220   (bike chip icon + text)
colorActionPrimary   = #c6ff2d   (CTA button background)
elevationRaised      — object    (card shadow)
radiusLg             = 20        (card top border-radius)
radiusSm             = 12        (bike thumbnail border-radius)
radiusXs             = 8         (distance badge, bike chip border-radius)
radiusFull           = 999       (CTA button pill)
typeHeadingMd        composite   (card title, data values: 17px/Inter/600)
typeHeadingSm        composite   (CTA label: 15px/Inter/600)
typeBodySm           composite   (secondary labels: 13px/Inter/400)
typeLabelSm          composite   (badge labels: 11px/Inter/500)
space300             = 12
space400             = 16
space1200            = 48        (CTA button min-height)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabBar story</name>
  <files>stories/components/tab-bar.stories.js</files>
  <read_first>
    - `stories/elevation.stories.js` — read full file; extract hexToRgba and shadowFromToken helper functions verbatim (lines 7–30 approximately); understand the array-map pattern used for the elevation card grid
    - `stories/iconography.stories.js` — read lines 1–60 for array/map pattern with dynamic list rendering
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — section "tab-bar.stories.js" for exact markup
  </read_first>
  <action>
    Create `stories/components/tab-bar.stories.js`:

    1. Import: `import * as tokens from '../../generated/tokens.js';`
    2. Default export: `export default { title: 'Components/TabBar' };`
    3. Copy hexToRgba and shadowFromToken helpers verbatim from `stories/elevation.stories.js`. Add a comment above them: `// Copied from stories/elevation.stories.js — helper not exported from that file`.
    4. Define a constant `const TABS = ['Ride', 'Discover', 'Wallet', 'Account'];`
    5. Define helper function `tabBar(activeTab)` that returns an HTML string:
       - Root div: `width:393px`, `background:${tokens.colorSurfaceBase}`, `display:flex`, `align-items:center`, `padding:${tokens.space200}px ${tokens.space400}px ${tokens.space500}px`, `box-shadow:${shadowFromToken(tokens.elevationFloating)}`, `box-sizing:border-box`.
       - Inside: map over TABS array. For each tab label, build a flex-column div (`flex:1`, `display:flex`, `flex-direction:column`, `align-items:center`, `gap:${tokens.space100}px`). Inside: (a) a circle div (width 48px, height 32px, `border-radius:${tokens.radiusFull}px`, `background:${isActive ? tokens.colorTextPrimary : tokens.colorGrey200}`, `display:flex`, `align-items:center`, `justify-content:center`) containing an icon placeholder span (font-size 14px, color: active = `tokens.colorTextOnInverse`, inactive = `tokens.colorTextSecondary`, text "●"); (b) label span in `typeLabelSm` composite (access .fontSize, .fontWeight, .fontFamily), color: active = `tokens.colorTextPrimary`, inactive = `tokens.colorTextSecondary`.
       - Use `.map(...).join('')` to concatenate tab divs.
    6. Export `RideActive = () => tabBar('Ride')`
    7. Export `DiscoverActive = () => tabBar('Discover')`
    8. Export `WalletActive = () => tabBar('Wallet')`
    9. Export `AccountActive = () => tabBar('Account')`
  </action>
  <verify>
    <automated>
      node -e "
        import('./stories/components/tab-bar.stories.js').then(m => {
          ['RideActive','DiscoverActive','WalletActive','AccountActive'].forEach(k => {
            if (!m[k]) throw new Error('TabBar missing: ' + k);
          });
          const html = m.RideActive();
          if (!html.includes('elevationFloating') && !html.includes('rgba(')) throw new Error('TabBar missing elevation shadow');
          console.log('tab-bar OK, exports:', Object.keys(m).filter(k => k !== 'default').join(', '));
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/components/tab-bar.stories.js` exists
    - File contains: `export const RideActive`, `export const DiscoverActive`, `export const WalletActive`, `export const AccountActive`
    - File contains: `title: 'Components/TabBar'`
    - File contains: `function hexToRgba` and `function shadowFromToken`
    - File contains: `shadowFromToken(tokens.elevationFloating)`
    - File contains: `tokens.colorTextPrimary` (active tab bg), `tokens.colorGrey200` (inactive tab bg)
    - File contains: `tokens.typeLabelSm.fontSize` or `.fontWeight` (composite access)
    - File contains: `import * as tokens from '../../generated/tokens.js'`
    - File does NOT import from `elevation.stories.js` (helper must be inlined, not imported)
  </acceptance_criteria>
  <done>TabBar story exists with 4 active-tab variants, hexToRgba + shadowFromToken helpers inlined, elevation shadow applied, array-map pattern for tab rendering, composite typography access.</done>
</task>

<task type="auto">
  <name>Task 2: BottomCard story</name>
  <files>stories/components/bottom-card.stories.js</files>
  <read_first>
    - `stories/elevation.stories.js` — read full file; extract hexToRgba and shadowFromToken helper functions verbatim; understand the structural complexity pattern
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — section "bottom-card.stories.js" for exact BikeSelection and WalkProgress markup
    - `stories/components/tab-bar.stories.js` — just completed in Task 1; confirm helper copy pattern
  </read_first>
  <action>
    Create `stories/components/bottom-card.stories.js`:

    1. Import: `import * as tokens from '../../generated/tokens.js';`
    2. Default export: `export default { title: 'Components/BottomCard' };`
    3. Copy hexToRgba and shadowFromToken helpers verbatim from `stories/elevation.stories.js`. Same comment: `// Copied from stories/elevation.stories.js — helper not exported from that file`.

    4. Export `BikeSelection`: a card div with `width:393px`, `background:${tokens.colorSurfaceBase}`, `border-radius:${tokens.radiusLg}px ${tokens.radiusLg}px 0 0`, `padding:${tokens.space400}px`, `box-shadow:${shadowFromToken(tokens.elevationRaised)}`, `box-sizing:border-box`. Children:
       - Top row (`display:flex`, `align-items:center`, `gap:${tokens.space300}px`, `margin-bottom:${tokens.space400}px`):
         - Bike thumbnail placeholder: 64x64px div, `background:${tokens.colorGrey100}`, `border-radius:${tokens.radiusSm}px`, `flex-shrink:0`
         - Text column (`flex:1`):
           - Bike name div: `font-family:'${tokens.typeHeadingMd.fontFamily}',sans-serif`, `font-size:${tokens.typeHeadingMd.fontSize}px`, `font-weight:${tokens.typeHeadingMd.fontWeight}`, `color:${tokens.colorTextPrimary}`, text "VoltBike VV-042"
           - Distance badge: inline-block div, `background:${tokens.colorGrey100}`, `color:${tokens.colorGrey700}`, `padding:2px ${tokens.space200}px`, `border-radius:${tokens.radiusXs}px`, `font-size:${tokens.typeLabelSm.fontSize}px`, `margin-top:4px`, text "120m away"
       - CTA button: `width:100%`, `min-height:${tokens.space1200}px`, `background:${tokens.colorActionPrimary}`, `color:${tokens.colorTextPrimary}`, `border:none`, `border-radius:${tokens.radiusFull}px`, font from `typeHeadingSm` composite, `cursor:pointer`, label "Get Directions"

    5. Export `WalkProgress`: same card wrapper (same width, bg, border-radius, padding, shadow). Children:
       - Data row (`display:flex`, `gap:${tokens.space400}px`, `align-items:flex-start`, `margin-bottom:${tokens.space400}px`):
         - Distance column: label span "Distance" in `typeBodySm` composite, `color:${tokens.colorTextSecondary}`; value div "350m" in `typeHeadingMd` composite, `color:${tokens.colorTextPrimary}`
         - ETA column: label span "ETA" same style; value "4 min" same style
         - Bike chip (`margin-left:auto`, `display:inline-flex`, `align-items:center`, `gap:4px`, `background:${tokens.colorGreen100}`, `padding:4px ${tokens.space200}px`, `border-radius:${tokens.radiusXs}px`): bike emoji "🚲" `color:${tokens.colorGreen700}`, `font-size:12px`; label "VV-042" in `typeLabelSm` composite, `color:${tokens.colorGreen700}`
       - CTA button: same as BikeSelection but label "I've Arrived"
  </action>
  <verify>
    <automated>
      node -e "
        import('./stories/components/bottom-card.stories.js').then(m => {
          if (!m.BikeSelection || !m.WalkProgress) throw new Error('BottomCard missing export');
          const html = m.BikeSelection();
          if (!html.includes('rgba(')) throw new Error('BottomCard missing elevation shadow (hexToRgba not working)');
          if (!html.includes('Get Directions')) throw new Error('BottomCard BikeSelection missing CTA text');
          const html2 = m.WalkProgress();
          if (!html2.includes('I') || !html2.includes('Arrived')) throw new Error('BottomCard WalkProgress missing CTA text');
          console.log('bottom-card OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/components/bottom-card.stories.js` exists
    - File contains: `export const BikeSelection`, `export const WalkProgress`
    - File contains: `title: 'Components/BottomCard'`
    - File contains: `function hexToRgba` and `function shadowFromToken`
    - File contains: `shadowFromToken(tokens.elevationRaised)`
    - File contains: `tokens.colorGreen100` and `tokens.colorGreen700` (bike chip)
    - File contains: `tokens.radiusLg` (card top border-radius)
    - File contains: `tokens.typeHeadingMd.fontSize` or `.fontWeight` (composite access)
    - File contains: `import * as tokens from '../../generated/tokens.js'`
    - `BikeSelection()` HTML output contains "Get Directions"
    - `WalkProgress()` HTML output contains "Arrived"
  </acceptance_criteria>
  <done>BottomCard story exists with BikeSelection and WalkProgress exports, hexToRgba + shadowFromToken inlined, elevationRaised shadow applied, both structural layouts complete with correct tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static story files | HTML template literal strings; no user input, no auth, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | story HTML | accept | Static dev documentation; no PII, no runtime data |
| T-03-04-SC | Tampering | npm/pip/cargo installs | accept | No new installs; phase uses only existing packages |
</threat_model>

<verification>
After both tasks complete:
1. `grep "function hexToRgba\|function shadowFromToken" stories/components/tab-bar.stories.js` — 2 matches
2. `grep "function hexToRgba\|function shadowFromToken" stories/components/bottom-card.stories.js` — 2 matches
3. `grep "elevationFloating" stories/components/tab-bar.stories.js` — 1 match
4. `grep "elevationRaised" stories/components/bottom-card.stories.js` — 1 match
5. `grep -r "from '../../generated/tokens.js'" stories/components/tab-bar.stories.js stories/components/bottom-card.stories.js` — 2 matches
</verification>

<success_criteria>
- stories/components/tab-bar.stories.js and bottom-card.stories.js exist
- hexToRgba + shadowFromToken inlined in both files (not imported)
- Elevation shadows applied: elevationFloating on TabBar, elevationRaised on BottomCard
- All named exports PascalCase
- Composite token access throughout (no bare type token usage)
- Together with 03-02 and 03-03, all 11 component stories are authored after Wave 2 completes
</success_criteria>

<output>
Create `.planning/phases/03-component-library-storybook/03-04-SUMMARY.md` when done
</output>
