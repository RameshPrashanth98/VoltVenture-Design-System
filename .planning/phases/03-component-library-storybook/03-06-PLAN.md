---
phase: 03-component-library-storybook
plan: 06
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - stories/screens/id-scan.stories.js
  - stories/screens/facial-scan.stories.js
autonomous: true
requirements:
  - D-02
  - D-03
  - D-05

must_haves:
  truths:
    - "IdScan screen story exists with single Default export on dark (#1a1a1a) background"
    - "FacialScan screen story exists with single Default export on dark background"
    - "Both files contain alpha fills hardcoded with inline comments"
    - "Both files use flex-column layout with dark colorGrey900 background"
    - "Both files show: dark top nav, step label + ProgressStrip, camera viewport, TrustPanel"
    - "All named exports are PascalCase"
  artifacts:
    - path: "stories/screens/id-scan.stories.js"
      provides: "IdScan KYC screen story"
      exports: ["Default"]
    - path: "stories/screens/facial-scan.stories.js"
      provides: "FacialScan KYC screen story"
      exports: ["Default"]
  key_links:
    - from: "stories/screens/id-scan.stories.js"
      to: "generated/tokens.js"
      via: "colorGrey900 dark screen bg + alpha fills"
      pattern: "colorGrey900"
    - from: "stories/screens/facial-scan.stories.js"
      to: "generated/tokens.js"
      via: "colorGrey900 dark screen bg"
      pattern: "colorGrey900"
---

<objective>
Author the two KYC verification screen stories: IdScan (Step 1) and FacialScan (Step 2). Both are full-dark screens (colorGrey900 background) with camera viewports, dark top navs with alpha-fill button backgrounds, a ProgressStrip showing the current step, and a TrustPanel bottom sheet. Alpha fills (#FFFFFF22, #FFFFFF18, #00000088) are NOT in the token system and must be hardcoded with comments.

Purpose: These complete the KYC screens (6 of 9 screen stories) and demonstrate the dark-surface screen pattern. They run in parallel with 03-05 and 03-07 in Wave 3.
Output: 2 .stories.js files in stories/screens/
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-RESEARCH.md

<interfaces>
<!-- Screen root element — KYC dark screens -->
Root div style:
  width:393px
  min-height:852px
  display:flex
  flex-direction:column
  background:${tokens.colorGrey900}   /* #1a1a1a — dark background */
  overflow:hidden
  box-sizing:border-box

Import path (two levels up):
  import * as tokens from '../../generated/tokens.js';

<!-- Alpha fills — NOT in token system — MUST hardcode with comments -->
Top nav button bg:   rgba(255,255,255,0.13)  /* #FFFFFF22 — design alpha fill, no token */
Shield badge bg:     rgba(255,255,255,0.09)  /* #FFFFFF18 — design alpha fill, no token */
Instructions banner: rgba(0,0,0,0.53)        /* #00000088 — design alpha fill, no token */

<!-- Key tokens for KYC screens -->
colorGrey900         = #1a1a1a   (screen background, camera viewport bg)
colorTextOnInverse   = #ffffff   (text/icons on dark surface)
colorTextSecondary   = #808080   (step label text, reassurance text)
colorSurfaceBase     = #ffffff   (ProgressStrip active segment, TrustPanel CTA button)
colorTextPrimary     = #0f0f0f   (TrustPanel CTA button text — black)
colorActionPrimary   = #c6ff2d   (facial scan arc / active scan line approximation)

typeLabelMd          composite   (step "Step 1 of 2" label: 13px/Inter/600)
typeLabelSm          composite   (ProgressStrip step sub-labels: 11px/Inter/500)
typeHeadingSm        composite   (TrustPanel trust label, CTA: 15px/Inter/600)
typeBodySm           composite   (TrustPanel reassurance text: 13px/Inter/400)
typeHeadingMd        composite   (top nav close "✕" icon: 17px/Inter/600)

space100=4  space200=8  space300=12  space400=16  space600=24  space800=32  space1200=48
radiusFull=999  radiusXs=8  radiusXl=28

<!-- ProgressStrip inline markup pattern (Step1Active or Step2Active) -->
/* Alpha: rgba(255,255,255,0.20) from #FFFFFF33 for inactive segments */
<div style="display:flex;gap:${tokens.space200}px;">
  <div style="flex:1;height:4px;border-radius:${tokens.radiusXs}px;background:${tokens.colorSurfaceBase};"></div>    <!-- active -->
  <div style="flex:1;height:4px;border-radius:${tokens.radiusXs}px;background:rgba(255,255,255,0.20);"></div>  <!-- inactive — #FFFFFF33 -->
</div>

<!-- TrustPanel inline markup pattern -->
<div style="background:${tokens.colorGrey900};border-radius:${tokens.radiusXl}px ${tokens.radiusXl}px 0 0;
  padding:${tokens.space600}px ${tokens.space400}px ${tokens.space800}px;">
  <div style="width:56px;height:56px;border-radius:${tokens.radiusFull}px;
    background:rgba(255,255,255,0.09);/* #FFFFFF18 */
    display:flex;align-items:center;justify-content:center;margin-bottom:${tokens.space400}px;">🛡</div>
  <div style="...typeHeadingSm...color:${tokens.colorTextOnInverse};">Secure Identity Scan</div>
  <div style="...typeBodySm...color:${tokens.colorTextSecondary};">Your data is encrypted...</div>
  <button style="...colorSurfaceBase bg...colorTextPrimary text...">[CTA Label]</button>
</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IdScan screen story</name>
  <files>stories/screens/id-scan.stories.js</files>
  <read_first>
    - `stories/elevation.stories.js` — analog: dark surface context; read lines 1–80
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — section "id-scan.stories.js" for exact layout structure (top nav, camera viewport, guide frame, TrustPanel)
    - `.planning/phases/03-component-library-storybook/03-RESEARCH.md` Section 1 — ID Scan frame f6zx5 content description
  </read_first>
  <action>
    Create `stories/screens/id-scan.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Screens/IdScan' };`
    - Comment block at top of file listing all alpha fills used:
      `// Alpha fills — NOT in token system (design-layer values):`
      `// #FFFFFF22 = rgba(255,255,255,0.13) — top nav button background`
      `// #FFFFFF18 = rgba(255,255,255,0.09) — shield badge background in TrustPanel`
      `// #00000088 = rgba(0,0,0,0.53)       — instructions banner background`
      `// #FFFFFF33 = rgba(255,255,255,0.20) — ProgressStrip inactive segment`

    - Export `Default`: root div (`width:393px`, `min-height:852px`, `display:flex`, `flex-direction:column`, `background:${tokens.colorGrey900}`, `overflow:hidden`, `box-sizing:border-box`). Content top to bottom:
      1. Dark top nav (`display:flex`, `justify-content:space-between`, `align-items:center`, `padding:${tokens.space400}px`, `padding-top:52px` — accounts for status bar area):
         - Close button: 40x40px circle (`border-radius:${tokens.radiusFull}px`, `background:rgba(255,255,255,0.13)` with comment `/* #FFFFFF22 */`, `display:flex`, `align-items:center`, `justify-content:center`), text "✕" `color:${tokens.colorTextOnInverse}`, `font-size:${tokens.typeHeadingMd.fontSize}px`
         - Flashlight toggle button: same style as close button, text "⚡" or "🔦"
      2. Step label + ProgressStrip section (`padding:${tokens.space300}px ${tokens.space400}px`):
         - Step label: "Step 1 of 2" in `typeLabelMd` composite, `color:${tokens.colorTextSecondary}`, `margin-bottom:${tokens.space200}px`
         - ProgressStrip: flex row with 2 segments (Step 1 active: `background:${tokens.colorSurfaceBase}`; Step 2 inactive: `background:rgba(255,255,255,0.20)` with comment `/* #FFFFFF33 */`), each `flex:1`, `height:4px`, `border-radius:${tokens.radiusXs}px`
      3. Camera viewport (`flex:1`, `position:relative`, `background:#111111`, `display:flex`, `align-items:center`, `justify-content:center`):
         - ID card guide frame: centered inner div (`width:280px`, `height:180px`, `border:2px dashed rgba(255,255,255,0.60)`, `border-radius:${tokens.radiusXs}px`, `position:relative`)
         - Instructions banner: absolute positioned pill (`position:absolute`, `bottom:${tokens.space300}px`, `left:50%`, `transform:translateX(-50%)`, `background:rgba(0,0,0,0.53)` with comment `/* #00000088 */`, `border-radius:${tokens.radiusFull}px`, `padding:${tokens.space200}px ${tokens.space400}px`), text "Position your ID card within the frame" in `typeLabelSm` composite, `color:${tokens.colorTextOnInverse}`
      4. TrustPanel (IdScan variant): dark bottom-sheet div (`background:${tokens.colorGrey900}`, `border-radius:${tokens.radiusXl}px ${tokens.radiusXl}px 0 0`, `padding:${tokens.space600}px ${tokens.space400}px ${tokens.space800}px`):
         - Shield badge: 56x56px circle, `background:rgba(255,255,255,0.09)` with comment `/* #FFFFFF18 */`, `border-radius:${tokens.radiusFull}px`, inline-flex centered, "🛡" text, `margin-bottom:${tokens.space400}px`
         - Trust label: "Secure Identity Scan" in `typeHeadingSm` composite, `color:${tokens.colorTextOnInverse}`, `margin-bottom:${tokens.space200}px`
         - Reassurance text: "Your data is encrypted and never shared." in `typeBodySm` composite, `color:${tokens.colorTextSecondary}`, `margin-bottom:${tokens.space600}px`
         - CTA button: `width:100%`, `min-height:${tokens.space1200}px`, `background:${tokens.colorSurfaceBase}`, `color:${tokens.colorTextPrimary}`, `border:none`, `border-radius:${tokens.radiusFull}px`, font from `typeHeadingSm` composite, `cursor:pointer`, label "Scan My ID"
  </action>
  <verify>
    <automated>
      node -e "
        import('./stories/screens/id-scan.stories.js').then(m => {
          if (!m.Default) throw new Error('IdScan missing Default');
          const html = m.Default();
          if (!html.includes('393px')) throw new Error('IdScan missing 393px width');
          if (!html.includes('rgba(255,255,255,0.13)')) throw new Error('IdScan missing top nav alpha fill');
          if (!html.includes('rgba(0,0,0,0.53)')) throw new Error('IdScan missing banner alpha fill');
          if (!html.includes('Scan My ID')) throw new Error('IdScan missing CTA label');
          console.log('id-scan OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/screens/id-scan.stories.js` exists
    - File contains: `export const Default`
    - File contains: `title: 'Screens/IdScan'`
    - File contains: `rgba(255,255,255,0.13)` with comment referencing `#FFFFFF22` (top nav buttons)
    - File contains: `rgba(255,255,255,0.09)` with comment referencing `#FFFFFF18` (shield badge)
    - File contains: `rgba(0,0,0,0.53)` with comment referencing `#00000088` (instructions banner)
    - File contains: `rgba(255,255,255,0.20)` with comment referencing `#FFFFFF33` (progress strip inactive)
    - File contains: `tokens.colorGrey900` (screen bg)
    - File contains: `tokens.colorSurfaceBase` (TrustPanel CTA, ProgressStrip active)
    - File contains: `width:393px` and `min-height:852px`
    - File contains: `import * as tokens from '../../generated/tokens.js'`
    - File does NOT contain `function hexToRgba` (no elevation tokens needed)
    - `Default()` HTML output contains "Scan My ID"
  </acceptance_criteria>
  <done>IdScan story: dark full-screen layout with top nav (alpha fill buttons), step 1 progress strip, camera viewport with ID card guide frame + instructions banner, and TrustPanel (Scan My ID CTA). All 4 alpha fills hardcoded with comments.</done>
</task>

<task type="auto">
  <name>Task 2: FacialScan screen story</name>
  <files>stories/screens/facial-scan.stories.js</files>
  <read_first>
    - `stories/screens/id-scan.stories.js` — just created in Task 1; FacialScan follows the same dark-surface structure. Read to ensure consistent pattern.
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — section "facial-scan.stories.js" for differences from IdScan (oval face guide, flip-camera toggle, Step 2 active)
    - `.planning/phases/03-component-library-storybook/03-RESEARCH.md` Section 1 — FacialScan frame llnIt content description (oval face guide, scan progress arc)
  </read_first>
  <action>
    Create `stories/screens/facial-scan.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Screens/FacialScan' };`
    - Same alpha fill comment block as id-scan.stories.js.

    - Export `Default`: same dark root div as IdScan. Content differences from IdScan:
      1. Dark top nav: close button (same "✕" with `rgba(255,255,255,0.13)` bg) + flip-camera toggle ("🔄" or "⟳" text) instead of flashlight.
      2. Step label + ProgressStrip: "Step 2 of 2" label. ProgressStrip: BOTH segments active (`background:${tokens.colorSurfaceBase}`) — step 2 means both are lit.
      3. Camera viewport: same `flex:1`, `position:relative`, `background:#111111`. Different guide: oval face guide instead of ID card frame. Create oval as a div with `width:200px`, `height:250px`, `border-radius:50%`, `border:2px solid ${tokens.colorActionPrimary}`, `position:relative`. Above guide (inside viewport) in absolute position: a scan progress arc approximation — a partial ring div (`width:200px`, `height:250px`, `border-radius:50%`, `border:2px dashed rgba(255,255,255,0.30)`, positioned slightly offset). Instructions banner: same `rgba(0,0,0,0.53)` pill with text "Center your face in the oval".
      4. TrustPanel (FacialScan variant): same dark panel structure as IdScan. CTA label: "Start Face Scan". Trust label: "Facial Recognition". Reassurance text: "Your biometric data is processed locally."

    Ensure all four alpha fills appear in this file with the same comments as id-scan.stories.js.
  </action>
  <verify>
    <automated>
      node -e "
        import('./stories/screens/facial-scan.stories.js').then(m => {
          if (!m.Default) throw new Error('FacialScan missing Default');
          const html = m.Default();
          if (!html.includes('393px')) throw new Error('FacialScan missing 393px');
          if (!html.includes('rgba(255,255,255,0.13)')) throw new Error('FacialScan missing top nav alpha');
          if (!html.includes('Start Face Scan')) throw new Error('FacialScan missing CTA label');
          if (!html.includes('border-radius:50%')) throw new Error('FacialScan missing oval guide');
          console.log('facial-scan OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/screens/facial-scan.stories.js` exists
    - File contains: `export const Default`
    - File contains: `title: 'Screens/FacialScan'`
    - File contains: `rgba(255,255,255,0.13)` with comment (top nav buttons)
    - File contains: `rgba(255,255,255,0.09)` with comment (shield badge)
    - File contains: `rgba(0,0,0,0.53)` with comment (instructions banner)
    - File contains: `border-radius:50%` (oval face guide)
    - File contains: `tokens.colorActionPrimary` (oval guide border color)
    - File contains: `tokens.colorGrey900` (screen bg)
    - File contains: `width:393px` and `min-height:852px`
    - File contains: `import * as tokens from '../../generated/tokens.js'`
    - `Default()` HTML output contains "Start Face Scan"
    - ProgressStrip in FacialScan has BOTH segments as `tokens.colorSurfaceBase` (step 2 = both active)
  </acceptance_criteria>
  <done>FacialScan story: same dark-surface structure as IdScan with oval face guide, flip-camera toggle, step 2 progress (both segments active), and TrustPanel with "Start Face Scan" CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static story files | HTML template literal strings; no biometric data, no live camera, no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | story HTML | accept | Static documentation; screen stories show placeholder content only, no real biometric or identity data |
| T-03-06-SC | Tampering | npm/pip/cargo installs | accept | No new installs; phase uses only existing packages |
</threat_model>

<verification>
After both tasks:
1. `grep -c "rgba(" stories/screens/id-scan.stories.js` — at least 4 rgba() calls (4 alpha fills)
2. `grep -c "rgba(" stories/screens/facial-scan.stories.js` — at least 4 rgba() calls
3. `grep "FFFFFF22\|FFFFFF18\|00000088\|FFFFFF33" stories/screens/id-scan.stories.js` — at least 4 comment references
4. `grep "FFFFFF22\|FFFFFF18\|00000088" stories/screens/facial-scan.stories.js` — at least 3 comment references
5. `grep "Step 2\|border-radius:50%" stories/screens/facial-scan.stories.js` — 2 matches
</verification>

<success_criteria>
- 2 KYC screen story files created in stories/screens/
- Both import from ../../generated/tokens.js
- All alpha fills hardcoded with comments referencing original design hex values
- IdScan: ID card guide frame (rectangular dashed border), step 1 active in ProgressStrip
- FacialScan: oval face guide (border-radius:50%), step 2 active in ProgressStrip (both segments lit)
- No hexToRgba/shadowFromToken helper (no elevation tokens in KYC screens)
- Typography composite access throughout
</success_criteria>

<output>
Create `.planning/phases/03-component-library-storybook/03-06-SUMMARY.md` when done
</output>
