---
phase: 03-component-library-storybook
plan: 08
type: execute
wave: 4
depends_on:
  - "03-05"
  - "03-06"
  - "03-07"
files_modified: []
autonomous: false
requirements:
  - D-06

must_haves:
  truths:
    - "npm run build:tokens exits 0 — tokens are freshly generated"
    - "npm run build-storybook exits 0 — storybook-static/ directory is produced"
    - "Exactly 11 component story files exist in stories/components/"
    - "Exactly 9 screen story files exist in stories/screens/"
    - "All 20 story files have correct titles and PascalCase named exports"
    - "CI (GitHub Actions storybook.yml) passes on push"
  artifacts:
    - path: "storybook-static/"
      provides: "Built Storybook output (confirms all 20 stories compile)"
    - path: "stories/components/"
      provides: "11 component story files"
      contains: "status-bar, button, social-auth-buttons, or-divider, phone-input, segmented-toggle, progress-strip, trust-panel, tab-bar, map-pin, bottom-card"
    - path: "stories/screens/"
      provides: "9 screen story files"
      contains: "splash, onboarding-1, registration, login, id-scan, facial-scan, home-map, navigate-to-bike, walking-directions"
  key_links:
    - from: "npm run build-storybook"
      to: "stories/components/*.stories.js + stories/screens/*.stories.js"
      via: ".storybook/main.js glob discovery"
      pattern: "storybook-static/index.html"
---

<objective>
Phase 3 done-bar verification. Run the full build pipeline (build:tokens then build-storybook), verify all 20 story files exist with correct names, check the glob discovery worked for both new subdirectories, and confirm CI passes.

Purpose: This is the quality gate for Phase 3. All 20 new story files must be authored, token-imported, and successfully compiled into the Storybook static output before Phase 3 is marked complete. A human checkpoint confirms the visual output looks correct.
Output: storybook-static/ directory with all 20 new stories compiled
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: File count audit + token rebuild</name>
  <files></files>
  <read_first>
    - `.planning/phases/03-component-library-storybook/03-RESEARCH.md` Section 7 — full file list (11 components + 9 screens)
    - All 11 SUMMARY.md files from plans 03-01 through 03-07 to confirm all plans completed
  </read_first>
  <action>
    Step 1 — Verify all 20 story files exist and have correct names. Run these checks:

    Components (11 expected):
    - stories/components/status-bar.stories.js
    - stories/components/button.stories.js
    - stories/components/social-auth-buttons.stories.js
    - stories/components/or-divider.stories.js
    - stories/components/phone-input.stories.js
    - stories/components/segmented-toggle.stories.js
    - stories/components/progress-strip.stories.js
    - stories/components/trust-panel.stories.js
    - stories/components/tab-bar.stories.js
    - stories/components/map-pin.stories.js
    - stories/components/bottom-card.stories.js

    Screens (9 expected):
    - stories/screens/splash.stories.js
    - stories/screens/onboarding-1.stories.js
    - stories/screens/registration.stories.js
    - stories/screens/login.stories.js
    - stories/screens/id-scan.stories.js
    - stories/screens/facial-scan.stories.js
    - stories/screens/home-map.stories.js
    - stories/screens/navigate-to-bike.stories.js
    - stories/screens/walking-directions.stories.js

    If ANY file is missing: stop and report which files are absent. Do not proceed to the build until all 20 files exist.

    Step 2 — Verify all story files have correct import paths:
    Run: `grep -rL "from '../../generated/tokens.js'" stories/components/ stories/screens/`
    Expected output: empty (all files contain the correct import). If any file is listed, fix its import path.

    Step 3 — Verify all named exports are PascalCase:
    Run: `grep -r "^export const [a-z]" stories/components/ stories/screens/`
    Expected output: empty (no lowercase exports). If any are found, rename them.

    Step 4 — Regenerate tokens to ensure generated/tokens.js is current:
    Run: `npm run build:tokens`
    Expected: exits 0. If it fails, diagnose and fix before proceeding.
  </action>
  <verify>
    <automated>
      cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && ls stories/components/*.stories.js stories/screens/*.stories.js | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - Component file count: `ls stories/components/*.stories.js | wc -l` outputs 11
    - Screen file count: `ls stories/screens/*.stories.js | wc -l` outputs 9
    - `grep -rL "from '../../generated/tokens.js'" stories/components/ stories/screens/` outputs nothing (all files have correct import)
    - `grep -r "^export const [a-z]" stories/components/ stories/screens/` outputs nothing (no lowercase exports)
    - `npm run build:tokens` exits 0
    - `generated/tokens.js` was modified within the last 5 minutes (fresh regeneration confirmed)
  </acceptance_criteria>
  <done>All 20 story files confirmed present with correct names, correct import paths, PascalCase exports, and freshly built token file.</done>
</task>

<task type="auto">
  <name>Task 2: Full Storybook build</name>
  <files></files>
  <read_first>
    - `.storybook/main.js` — confirm glob still covers stories/**/*.stories.js (unchanged from 03-01)
    - `package.json` — confirm build-storybook script exists and is unchanged from Phase 2
  </read_first>
  <action>
    Run the full Storybook static build:
    ```
    npm run build-storybook
    ```

    Expected: exits 0. The storybook-static/ directory is created (or updated).

    If build fails, diagnose:
    - Parse error in a story file → open the file, fix the syntax, re-run
    - Token import error → check that `npm run build:tokens` was run first (Task 1)
    - Glob not picking up new files → check .storybook/main.js stories array (should have ** glob from 03-01)
    - Missing export error (Storybook 10 enforcement) → ensure all exports are PascalCase functions

    After successful build, verify storybook-static/ output:
    - `ls storybook-static/` — directory exists with index.html and asset files
    - `ls storybook-static/*.html | wc -l` — at least 1 (index.html present)

    Also verify the build included the new story categories by checking for story entries:
    - `grep -r "Components" storybook-static/ --include="*.json" | head -5` — should find Component story references
    - `grep -r "Screens" storybook-static/ --include="*.json" | head -5` — should find Screen story references

    Note: If the json grep finds nothing (Storybook 10 may not produce story-catalog json in that format), the index.html existence is sufficient — story content is bundled in JS.
  </action>
  <verify>
    <automated>
      cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && npm run build-storybook 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run build-storybook` exits 0
    - `storybook-static/` directory exists
    - `storybook-static/index.html` file exists
    - Build output (stdout/stderr) contains no ERROR lines about missing exports or import failures
    - Build duration completes (no infinite hang — if it hangs >120s, kill and investigate)
  </acceptance_criteria>
  <done>npm run build-storybook exits 0. storybook-static/ directory produced with index.html. All 20 new story files were compiled without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 3 Storybook has been built. 11 component stories (Components/ section) and 9 screen stories (Screens/ section) have been authored as HTML/CSS template functions using VoltVenture design tokens. The static build completed successfully.
  </what-built>
  <how-to-verify>
    1. Run Storybook locally: `npm run storybook`
    2. Open http://localhost:6006 in your browser
    3. Check the sidebar — you should see THREE top-level sections:
       - Foundation/ (existing — 8 stories: Color, Typography, Spacing, Radius, Elevation, Border, Grid, Iconography)
       - Components/ (NEW — 11 stories)
       - Screens/ (NEW — 9 stories)
    4. Click through each Components/ story and verify:
       - StatusBar: LightSurface (white bg, dark icons) and DarkSurface (transparent on dark)
       - Button: Primary (green), Secondary (black), Ghost (underline link), Disabled (grey)
       - SocialAuthButtons: AppleButton (green), GoogleButton (white with border)
       - OrDivider: horizontal line with "OR" text
       - PhoneInput: Default (placeholder) and Filled (country code + number)
       - SegmentedToggle: PhoneActive and EmailActive (pill toggle)
       - ProgressStrip: Step1Active and Step2Active (on dark background)
       - TrustPanel: IdScan and FacialScan (dark panel with shield + CTA)
       - TabBar: RideActive, DiscoverActive, WalletActive, AccountActive (4 tabs)
       - MapPin: RangePin (pill with ⚡) and SelectedPin (badge with pulse ring)
       - BottomCard: BikeSelection (thumbnail + CTA) and WalkProgress (data + arrived CTA)
    5. Click through each Screens/ story and verify:
       - Splash: centered layout, brand name, loader bar visible
       - Onboarding1: status bar, skip link, illustration placeholder, pagination, next button
       - Registration: logo, social auth buttons, OR divider, WhatsApp + email options, terms
       - Login — PhoneTab: header, social auth, toggle (Phone active), phone input, Continue
       - Login — EmailTab: same as PhoneTab but Email segment highlighted in toggle
       - IdScan: dark screen, top nav, step 1 progress, camera viewport, "Scan My ID" CTA
       - FacialScan: dark screen, step 2 progress (both segments lit), oval guide, "Start Face Scan" CTA
       - HomeMap: grey map bg, search bar, bike range pins, tab bar (Ride active), Scan QR card
       - NavigateToBike: grey map, dashed route line, ETA badge, cancel button, bike card
       - WalkingDirections: grey map, route lines, turn card (dark), recenter FAB, walk progress card
    6. Confirm no visual errors (blank white exports, missing imports, console errors)
    7. Confirm Foundation/ section is unmodified (all 8 existing stories still show correctly)
  </how-to-verify>
  <resume-signal>Type "approved" if all 20 stories render correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output | storybook-static/ is a static HTML/JS bundle served locally — no server-side execution |
| CI | GitHub Actions runs npm ci + build:tokens + build-storybook — no secrets in story files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Information Disclosure | storybook-static/ | accept | Build output is developer documentation; contains no PII, secrets, or auth tokens |
| T-03-08-02 | Denial of Service | build-storybook hanging | accept | Build timeout set by developer; if hung >120s, kill and investigate story parse errors |
| T-03-08-SC | Tampering | npm/pip/cargo installs | accept | No new installs; build uses locked node_modules from Phase 2 |
</threat_model>

<verification>
Phase 3 done-bar (from D-06):
1. `npm run build:tokens` exits 0 — confirmed in Task 1
2. `npm run build-storybook` exits 0 — confirmed in Task 2
3. `ls stories/components/*.stories.js | wc -l` → 11
4. `ls stories/screens/*.stories.js | wc -l` → 9
5. `ls storybook-static/index.html` — file exists
6. Human visual verification approved (Task 3 checkpoint)
</verification>

<success_criteria>
- npm run build:tokens exits 0 (token pipeline still healthy)
- npm run build-storybook exits 0 (all 20 new stories compile without errors)
- storybook-static/ directory produced with index.html
- 11 component story files exist in stories/components/ with correct names
- 9 screen story files exist in stories/screens/ with correct names
- Human verification approved: all 20 stories render correctly, Foundation/ section unmodified
- CI (GitHub Actions storybook.yml) passes on push — existing workflow validates the same build
- Phase 3 complete — ready to plan Phase 4 (React Native Paper App Integration)
</success_criteria>

<output>
Create `.planning/phases/03-component-library-storybook/03-08-SUMMARY.md` when done

Update STATE.md:
- status: "Phase 3 complete — all 20 stories built and verified"
- phase: "Phase 4 — React Native Paper App Integration"
- stopped_at: "Inter-phase boundary; Phase 4 ready to plan"
- completed_phases: 3
- completed_plans: 20
- percent: 75 (3 of 4 phases)
</output>
