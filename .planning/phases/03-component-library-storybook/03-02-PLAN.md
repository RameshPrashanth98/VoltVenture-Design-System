---
phase: 03-component-library-storybook
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - stories/components/status-bar.stories.js
  - stories/components/button.stories.js
  - stories/components/social-auth-buttons.stories.js
  - stories/components/or-divider.stories.js
  - stories/components/phone-input.stories.js
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04

must_haves:
  truths:
    - "StatusBar component story exists with LightSurface and DarkSurface variants"
    - "Button component story exists with Primary, Secondary, Ghost, and Disabled variants"
    - "SocialAuthButtons story exists with AppleButton and GoogleButton variants"
    - "OrDivider story exists with Default variant"
    - "PhoneInput story exists with Default and Filled variants"
    - "All 5 files import tokens from ../../generated/tokens.js with explicit .js extension"
    - "All named exports are PascalCase"
    - "Typography tokens are accessed as composite objects (.fontSize, .fontWeight, .fontFamily, .lineHeight)"
  artifacts:
    - path: "stories/components/status-bar.stories.js"
      provides: "StatusBar component story"
      exports: ["LightSurface", "DarkSurface"]
    - path: "stories/components/button.stories.js"
      provides: "Button component story"
      exports: ["Primary", "Secondary", "Ghost", "Disabled"]
    - path: "stories/components/social-auth-buttons.stories.js"
      provides: "SocialAuthButtons component story"
      exports: ["AppleButton", "GoogleButton"]
    - path: "stories/components/or-divider.stories.js"
      provides: "OrDivider component story"
      exports: ["Default"]
    - path: "stories/components/phone-input.stories.js"
      provides: "PhoneInput component story"
      exports: ["Default", "Filled"]
  key_links:
    - from: "stories/components/*.stories.js"
      to: "generated/tokens.js"
      via: "import * as tokens from '../../generated/tokens.js'"
      pattern: "import \\* as tokens from '\\.\\./\\.\\./generated/tokens\\.js'"
---

<objective>
Author the five simplest component stories: StatusBar, Button, SocialAuthButtons, OrDivider, and PhoneInput. These are atomic/stateless components — each export is a self-contained HTML snippet. No elevation helpers needed.

Purpose: These 5 components appear across 2–9 Hi-Fi screens. Authoring them first (Wave 2) establishes the component story pattern for Wave 2 parallel plans 03-03 and 03-04, and provides building blocks that screen story authors (Wave 3) can reference inline.
Output: 5 .stories.js files in stories/components/
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md

<interfaces>
<!-- Token names from generated/tokens.js — verified values from RESEARCH.md Section 3 -->
<!-- Import path for stories/components/ files: two levels up -->
import * as tokens from '../../generated/tokens.js';

<!-- Color tokens (exact names) -->
colorActionPrimary       = #c6ff2d   (electric green — Button Primary bg, Apple Button bg)
colorActionSecondary     = #0f0f0f   (volt black — Button Secondary bg)
colorSurfaceBase         = #ffffff   (white — Google Button bg, Card bg)
colorTextPrimary         = #0f0f0f   (black — text on light surfaces, on green bg)
colorTextOnInverse       = #ffffff   (white — text on dark/black surfaces)
colorTextSecondary       = #808080   (grey — placeholder, OR text, inactive)
colorTextDisabled        = #c9c9c9   (disabled state text)
colorTextAccent          = use colorActionPrimary or colorTextPrimary for Ghost — per design no accent token; Ghost uses underline + colorTextPrimary
colorBorderSubtle        = #ebebeb   (card dividers, Google button border, OR divider lines)
colorGrey050             = #fafafa   (phone input bg in design uses $vv-grey-050)
colorGrey100             = #f5f5f5   (input bg fallback)
colorGrey200             = #ebebeb   (disabled bg)

<!-- Spacing tokens (JS number values, no units) -->
space100 = 4    space200 = 8    space300 = 12   space400 = 16
space600 = 24   space800 = 32   space1200 = 48

<!-- Radius tokens -->
radiusSm   = 12    radiusFull = 999

<!-- Typography composite objects — ALWAYS access sub-properties, never use bare -->
typeHeadingSm  — { fontSize: 15, fontWeight: 600, fontFamily: 'Inter', lineHeight: 20, letterSpacing: 0 }
typeBodyMd     — { fontSize: 15, fontWeight: 400, fontFamily: 'Inter', lineHeight: 22, letterSpacing: 0 }
typeBodyLg     — { fontSize: 17, fontWeight: 400, fontFamily: 'Inter', lineHeight: 24, letterSpacing: 0 }
typeLabelSm    — { fontSize: 11, fontWeight: 500, fontFamily: 'Inter', lineHeight: 16, letterSpacing: 0 }
typeLabelMd    — { fontSize: 13, fontWeight: 600, fontFamily: 'Inter', lineHeight: 18, letterSpacing: 0 }

<!-- Border tokens -->
borderWidthHairline = 1   (px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusBar + Button stories</name>
  <files>stories/components/status-bar.stories.js, stories/components/button.stories.js</files>
  <read_first>
    - `stories/border.stories.js` — analog for status-bar (single-export, simple flex-row doc story); read full file
    - `stories/radius.stories.js` — analog for button (multi-export named variants); read full file
    - `stories/typography.stories.js` — lines 8–20 for composite token access pattern (.fontSize, .fontFamily etc.)
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — sections "status-bar.stories.js" and "button.stories.js" for exact markup patterns
  </read_first>
  <action>
    Create `stories/components/status-bar.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/StatusBar' };`
    - Export `LightSurface`: a 393px-wide, 44px-tall flex row div with `justify-content:space-between`, `align-items:center`, `padding:0 ${tokens.space400}px`, `background:${tokens.colorSurfaceBase}`. Left child: time text "9:41" in `typeLabelMd` (access `.fontFamily`, `.fontSize`, `.fontWeight`), color `colorTextPrimary`. Right child: unicode approximation of status icons "▲ WiFi ■" in `typeLabelSm.fontSize`, color `colorTextPrimary`, `letter-spacing:2px`.
    - Export `DarkSurface`: same structure but `background:transparent` and a wrapper div with `background:${tokens.colorGrey900}` for contrast context. Time text and icons use `color:${tokens.colorTextOnInverse}` (white).
    - `box-sizing:border-box` on the root div.

    Create `stories/components/button.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/Button' };`
    - Export `Primary`: a `<button>` with `background:${tokens.colorActionPrimary}`, `color:${tokens.colorTextPrimary}`, `padding:${tokens.space400}px ${tokens.space600}px`, `border-radius:${tokens.radiusFull}px`, `min-height:${tokens.space1200}px`, `width:100%`, `border:none`, `cursor:pointer`, `display:inline-flex`, `align-items:center`, `justify-content:center`, `box-sizing:border-box`. Font from `typeHeadingSm` composite. Label text: "Book a Ride".
    - Export `Secondary`: same structure but `background:${tokens.colorActionSecondary}` and `color:${tokens.colorTextOnInverse}`. Label text: "Continue".
    - Export `Ghost`: `<button>` with `background:none`, `border:none`, `color:${tokens.colorTextPrimary}`, `text-decoration:underline`, `cursor:pointer`, same font from `typeHeadingSm`. Label text: "Sign in instead". No fixed width — inline display.
    - Export `Disabled`: same as Primary but `background:${tokens.colorGrey200}`, `color:${tokens.colorTextDisabled}`, `cursor:not-allowed`, add HTML attribute `disabled`. Label text: "Book a Ride".

    Both files: no elevation helpers needed. No hexToRgba. No fenced code blocks in actions.
  </action>
  <verify>
    <automated>
      node -e "
        import('./stories/components/status-bar.stories.js').then(m => {
          if (!m.LightSurface) throw new Error('missing LightSurface');
          if (!m.DarkSurface) throw new Error('missing DarkSurface');
          const html = m.LightSurface();
          if (!html.includes('colorSurfaceBase') && !html.includes('#ffffff') && !html.includes('tokens')) throw new Error('no token reference in LightSurface');
          console.log('status-bar OK');
        });
        import('./stories/components/button.stories.js').then(m => {
          ['Primary','Secondary','Ghost','Disabled'].forEach(k => { if (!m[k]) throw new Error('missing ' + k); });
          console.log('button OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/components/status-bar.stories.js` exists
    - File contains: `export const LightSurface`, `export const DarkSurface`
    - File contains: `import * as tokens from '../../generated/tokens.js'`
    - File contains: `title: 'Components/StatusBar'`
    - File accesses typography as `.fontSize` or `.fontFamily` (not bare `tokens.typeBodyMd`)
    - `stories/components/button.stories.js` exists
    - File contains: `export const Primary`, `export const Secondary`, `export const Ghost`, `export const Disabled`
    - File contains: `tokens.colorActionPrimary`
    - File contains: `tokens.radiusFull`
    - File contains: `tokens.space1200`
    - File contains: `tokens.typeHeadingSm.fontSize` (composite access)
    - File contains: `title: 'Components/Button'`
  </acceptance_criteria>
  <done>StatusBar story has LightSurface and DarkSurface exports. Button story has Primary, Secondary, Ghost, Disabled exports. Both files import from ../../generated/tokens.js and use composite token access for typography.</done>
</task>

<task type="auto">
  <name>Task 2: SocialAuthButtons + OrDivider + PhoneInput stories</name>
  <files>stories/components/social-auth-buttons.stories.js, stories/components/or-divider.stories.js, stories/components/phone-input.stories.js</files>
  <read_first>
    - `stories/radius.stories.js` — analog for social-auth-buttons (multi-export, named variants)
    - `stories/border.stories.js` — analog for or-divider (single-export, simple element)
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — sections "social-auth-buttons.stories.js", "or-divider.stories.js", "phone-input.stories.js" for exact markup patterns
    - `stories/components/button.stories.js` — read the completed Task 1 file; social auth buttons reuse the same pill-button pattern
  </read_first>
  <action>
    Create `stories/components/social-auth-buttons.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/SocialAuthButtons' };`
    - Export `AppleButton`: pill `<button>` with `background:${tokens.colorActionPrimary}`, `color:${tokens.colorTextPrimary}`, `display:inline-flex`, `align-items:center`, `justify-content:center`, `gap:${tokens.space200}px`, `padding:${tokens.space400}px ${tokens.space600}px`, `border-radius:${tokens.radiusFull}px`, `min-height:${tokens.space1200}px`, `width:100%`, `border:none`, `cursor:pointer`, `box-sizing:border-box`. Font from `typeBodyMd` composite. Label: " Continue with Apple" (include unicode apple symbol  or text "Apple" — use text "Apple" if symbol unavailable).
    - Export `GoogleButton`: same structure but `background:${tokens.colorSurfaceBase}`, `color:${tokens.colorTextPrimary}`, `border:${tokens.borderWidthHairline}px solid ${tokens.colorBorderSubtle}`. Label: "G Continue with Google".

    Create `stories/components/or-divider.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/OrDivider' };`
    - Export `Default`: a flex-row div with `display:flex`, `align-items:center`, `gap:${tokens.space300}px`, `padding:0 ${tokens.space400}px`. Children: (1) flex-1 div with `height:${tokens.borderWidthHairline}px`, `background:${tokens.colorBorderSubtle}`; (2) span with "OR" text in `typeLabelSm` composite, `color:${tokens.colorTextSecondary}`; (3) same flex-1 div again.

    Create `stories/components/phone-input.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/PhoneInput' };`
    - Export `Default` (empty/placeholder state): a flex-row div with `display:flex`, `align-items:center`, `background:${tokens.colorGrey050}`, `border-radius:${tokens.radiusSm}px`, `padding:0 ${tokens.space400}px`, `min-height:${tokens.space1200}px`, `box-sizing:border-box`, `gap:${tokens.space300}px`. Children: (1) span "+91" in `typeBodyLg` composite, `color:${tokens.colorTextPrimary}`; (2) divider div `width:${tokens.borderWidthHairline}px`, `height:20px`, `background:${tokens.colorBorderSubtle}`; (3) span "Mobile number" (placeholder) in `typeBodyLg` composite, `color:${tokens.colorTextSecondary}`.
    - Export `Filled`: same structure but last child span shows "98765 43210" in `color:${tokens.colorTextPrimary}` (not secondary).
  </action>
  <verify>
    <automated>
      node -e "
        Promise.all([
          import('./stories/components/social-auth-buttons.stories.js'),
          import('./stories/components/or-divider.stories.js'),
          import('./stories/components/phone-input.stories.js'),
        ]).then(([sa, od, pi]) => {
          if (!sa.AppleButton || !sa.GoogleButton) throw new Error('SocialAuthButtons missing export');
          if (!od.Default) throw new Error('OrDivider missing Default');
          if (!pi.Default || !pi.Filled) throw new Error('PhoneInput missing export');
          console.log('All 3 stories OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/components/social-auth-buttons.stories.js` exists
    - File contains: `export const AppleButton`, `export const GoogleButton`
    - File contains: `title: 'Components/SocialAuthButtons'`
    - File contains: `tokens.colorActionPrimary` (Apple bg) and `tokens.colorSurfaceBase` (Google bg)
    - File contains: `tokens.radiusFull` and `tokens.borderWidthHairline`
    - `stories/components/or-divider.stories.js` exists
    - File contains: `export const Default`
    - File contains: `title: 'Components/OrDivider'`
    - File contains: `tokens.colorBorderSubtle` and `tokens.colorTextSecondary`
    - `stories/components/phone-input.stories.js` exists
    - File contains: `export const Default`, `export const Filled`
    - File contains: `title: 'Components/PhoneInput'`
    - File contains: `tokens.colorGrey050` and `tokens.typeBodyLg.fontSize`
    - All 3 files contain: `import * as tokens from '../../generated/tokens.js'`
  </acceptance_criteria>
  <done>SocialAuthButtons, OrDivider, and PhoneInput stories exist with correct exports, titles, token usage, and import paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static story files | HTML template literal strings; no user input, no runtime data fetch, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | story HTML output | accept | Stories are static dev documentation; no PII, no secrets, no live data |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | No new installs; existing package-lock.json unchanged |
</threat_model>

<verification>
After both tasks complete, run from project root:
1. `node -e "import('./stories/components/status-bar.stories.js').then(m => console.log(Object.keys(m)))"` — outputs ["default","LightSurface","DarkSurface"]
2. `node -e "import('./stories/components/button.stories.js').then(m => console.log(Object.keys(m)))"` — outputs ["default","Primary","Secondary","Ghost","Disabled"]
3. `grep -r "import \* as tokens from '../../generated/tokens.js'" stories/components/` — 5 matches
4. `grep -r "\.fontSize" stories/components/status-bar.stories.js stories/components/button.stories.js` — at least 1 match per file (composite token access confirmed)
</verification>

<success_criteria>
- 5 story files exist in stories/components/
- All 5 files import from `../../generated/tokens.js` with explicit .js extension
- All named exports are PascalCase
- Typography tokens accessed as composite objects (e.g. `tokens.typeHeadingSm.fontSize`)
- No elevation helpers (hexToRgba, shadowFromToken) in these 5 files — not needed
- npm run build-storybook would include all 5 stories (verified in Plan 03-08)
</success_criteria>

<output>
Create `.planning/phases/03-component-library-storybook/03-02-SUMMARY.md` when done
</output>
