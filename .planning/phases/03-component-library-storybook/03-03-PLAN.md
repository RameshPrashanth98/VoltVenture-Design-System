---
phase: 03-component-library-storybook
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - stories/components/segmented-toggle.stories.js
  - stories/components/progress-strip.stories.js
  - stories/components/trust-panel.stories.js
  - stories/components/map-pin.stories.js
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04

must_haves:
  truths:
    - "SegmentedToggle story exists with PhoneActive and EmailActive variants"
    - "ProgressStrip story exists with Step1Active and Step2Active variants on a dark background"
    - "TrustPanel story exists with IdScan and FacialScan variants"
    - "MapPin story exists with RangePin and SelectedPin variants"
    - "All 4 files import from ../../generated/tokens.js"
    - "Alpha fill values (#FFFFFF33, #FFFFFF18) are hardcoded with inline comments — no missing token reference"
    - "Typography tokens accessed as composite objects"
  artifacts:
    - path: "stories/components/segmented-toggle.stories.js"
      provides: "SegmentedToggle component story"
      exports: ["PhoneActive", "EmailActive"]
    - path: "stories/components/progress-strip.stories.js"
      provides: "ProgressStrip component story"
      exports: ["Step1Active", "Step2Active"]
    - path: "stories/components/trust-panel.stories.js"
      provides: "TrustPanel component story"
      exports: ["IdScan", "FacialScan"]
    - path: "stories/components/map-pin.stories.js"
      provides: "MapPin component story"
      exports: ["RangePin", "SelectedPin"]
  key_links:
    - from: "stories/components/progress-strip.stories.js"
      to: "generated/tokens.js"
      via: "import + colorGrey900 for dark bg context"
      pattern: "colorGrey900"
    - from: "stories/components/trust-panel.stories.js"
      to: "generated/tokens.js"
      via: "colorGrey900 dark surface + colorSurfaceBase CTA"
      pattern: "colorGrey900"
---

<objective>
Author four medium-complexity component stories: SegmentedToggle (state-driven via helper fn), ProgressStrip (alpha fills on dark bg), TrustPanel (dark bottom-sheet panel), and MapPin (pill + pulse ring). All use the helper-function pattern from Phase 2 spacing/radius stories.

Purpose: These 4 components power the KYC screens (ID Scan, Facial Scan) and the Login screen. They are authored in Wave 2 parallel to 03-02 and 03-04, converging in Wave 3 screen stories.
Output: 4 .stories.js files in stories/components/
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-PATTERNS.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md

<interfaces>
<!-- Token names — verified from generated/tokens.js (RESEARCH.md Section 3) -->
import * as tokens from '../../generated/tokens.js';

<!-- For SegmentedToggle -->
colorGrey100         = #f5f5f5   (outer pill background)
colorActionPrimary   = #c6ff2d   (active segment background)
colorTextPrimary     = #0f0f0f   (active tab label color — black on green)
colorTextSecondary   = #808080   (inactive tab label color)
radiusFull           = 999       (pill border-radius)
typeHeadingSm        composite   (tab labels: 15px/Inter/600 — use .fontSize, .fontWeight, .fontFamily)
space200             = 8         (segment vertical padding)
space400             = 16        (segment horizontal padding)

<!-- For ProgressStrip -->
colorGrey900         = #1a1a1a   (dark container background)
colorSurfaceBase     = #ffffff   (active segment color — white on dark)
radiusXs             = 8         (segment border-radius)
typeLabelSm          composite   (step label: 11px/Inter/500 — use .fontSize, .fontWeight, .fontFamily)
colorTextSecondary   = #808080   (step label color)
space300             = 12
space400             = 16
space200             = 8
/* ALPHA FILL — NOT a token: #FFFFFF33 = rgba(255,255,255,0.20) — inactive progress segment */

<!-- For TrustPanel -->
colorGrey900         = #1a1a1a   (panel background)
colorTextOnInverse   = #ffffff   (trust label + icons on dark surface)
colorTextSecondary   = #808080   (reassurance text)
colorSurfaceBase     = #ffffff   (CTA button background — white button on dark panel)
colorTextPrimary     = #0f0f0f   (CTA button text — black)
radiusFull           = 999       (CTA button pill)
radiusXl             = 28        (panel top border-radius for bottom-sheet shape)
typeHeadingSm        composite   (trust label + CTA label: 15px/Inter/600)
typeBodySm           composite   (reassurance text: 13px/Inter/400)
space400             = 16
space600             = 24
space800             = 32
space1200            = 48        (CTA button min-height)
/* ALPHA FILL — NOT a token: #FFFFFF18 = rgba(255,255,255,0.09) — shield badge background */

<!-- For MapPin -->
colorGrey900         = #1a1a1a   (RangePin background)
colorTextOnInverse   = #ffffff   (RangePin text)
colorActionPrimary   = #c6ff2d   (zap icon color in RangePin; pulse ring tint in SelectedPin)
colorSurfaceBase     = #ffffff   (SelectedPin badge background)
colorTextPrimary     = #0f0f0f   (SelectedPin label text)
radiusFull           = 999       (both pin shapes are pills)
typeLabelSm          composite   (pin label: 11px/Inter/500)
space100             = 4
space200             = 8
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SegmentedToggle + ProgressStrip stories</name>
  <files>stories/components/segmented-toggle.stories.js, stories/components/progress-strip.stories.js</files>
  <read_first>
    - `stories/spacing.stories.js` — analog: helper function + multi-export pattern; read lines 5–55
    - `stories/radius.stories.js` — analog: multi-export named variants; read lines 1–40
    - `stories/elevation.stories.js` — analog for ProgressStrip: dark background context; read lines 1–60
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — sections "segmented-toggle.stories.js" and "progress-strip.stories.js" for exact markup patterns
  </read_first>
  <action>
    Create `stories/components/segmented-toggle.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/SegmentedToggle' };`
    - Define a helper function `toggle(activeTab)` that accepts 'phone' or 'email' as the active segment. The function returns an HTML string of: outer pill div with `display:inline-flex`, `background:${tokens.colorGrey100}`, `border-radius:${tokens.radiusFull}px`, `padding:4px`, `gap:4px`. Inside: two segment divs ("Phone" and "Email"). Active segment: `background:${tokens.colorActionPrimary}`, `color:${tokens.colorTextPrimary}`. Inactive segment: `background:transparent`, `color:${tokens.colorTextSecondary}`. Both segments: `padding:${tokens.space200}px ${tokens.space400}px`, `border-radius:${tokens.radiusFull}px`, font from `typeHeadingSm` composite (access .fontFamily, .fontSize, .fontWeight).
    - Export `PhoneActive = () => toggle('phone')`
    - Export `EmailActive = () => toggle('email')`

    Create `stories/components/progress-strip.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/ProgressStrip' };`
    - Add comment at top of file: `// Alpha fill — not in token system: #FFFFFF33 = rgba(255,255,255,0.20) — inactive segment`
    - Define a helper function `strip(activeStep)` that returns an HTML string. Root div: `background:${tokens.colorGrey900}`, `padding:${tokens.space300}px ${tokens.space400}px`. Inner flex row with `display:flex`, `gap:${tokens.space200}px`. Two segment divs: `flex:1`, `height:4px`, `border-radius:${tokens.radiusXs}px`. Active (step number <= activeStep): `background:${tokens.colorSurfaceBase}`. Inactive: `background:rgba(255,255,255,0.20)` with inline comment `/* #FFFFFF33 — design alpha fill, no token */`.
    - Export `Step1Active = () => strip(1)` — segment 1 white, segment 2 dim
    - Export `Step2Active = () => strip(2)` — both segments white (step 2 active = step 1 completed)
  </action>
  <verify>
    <automated>
      node -e "
        Promise.all([
          import('./stories/components/segmented-toggle.stories.js'),
          import('./stories/components/progress-strip.stories.js'),
        ]).then(([st, ps]) => {
          if (!st.PhoneActive || !st.EmailActive) throw new Error('SegmentedToggle missing export');
          if (!ps.Step1Active || !ps.Step2Active) throw new Error('ProgressStrip missing export');
          const html = ps.Step1Active();
          if (!html.includes('rgba(255,255,255,0.20)')) throw new Error('ProgressStrip missing alpha fill');
          console.log('segmented-toggle and progress-strip OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/components/segmented-toggle.stories.js` exists
    - File contains: `export const PhoneActive`, `export const EmailActive`
    - File contains: `title: 'Components/SegmentedToggle'`
    - File contains: `tokens.colorGrey100` (outer pill bg) and `tokens.colorActionPrimary` (active segment)
    - File contains: `tokens.typeHeadingSm.fontSize` or `.fontWeight` (composite access)
    - `stories/components/progress-strip.stories.js` exists
    - File contains: `export const Step1Active`, `export const Step2Active`
    - File contains: `title: 'Components/ProgressStrip'`
    - File contains: `rgba(255,255,255,0.20)` with a comment referencing `#FFFFFF33`
    - File contains: `tokens.colorGrey900` (dark container bg)
    - File contains: `tokens.colorSurfaceBase` (active segment color)
  </acceptance_criteria>
  <done>SegmentedToggle and ProgressStrip stories exist with correct exports, helper functions, token usage, and hardcoded alpha fill with comment.</done>
</task>

<task type="auto">
  <name>Task 2: TrustPanel + MapPin stories</name>
  <files>stories/components/trust-panel.stories.js, stories/components/map-pin.stories.js</files>
  <read_first>
    - `stories/elevation.stories.js` — analog for TrustPanel: dark surface, local constant data; read lines 1–80
    - `stories/radius.stories.js` — analog for MapPin: multi-export, pill shapes; read lines 1–40
    - `.planning/phases/03-component-library-storybook/03-PATTERNS.md` — sections "trust-panel.stories.js" and "map-pin.stories.js" for exact markup patterns
  </read_first>
  <action>
    Create `stories/components/trust-panel.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/TrustPanel' };`
    - Comment at top: `// Alpha fill — not in token system: #FFFFFF18 = rgba(255,255,255,0.09) — shield badge background`
    - Define helper function `panel(ctaLabel)` that returns HTML. Root div: `background:${tokens.colorGrey900}`, `border-radius:${tokens.radiusXl}px ${tokens.radiusXl}px 0 0`, `padding:${tokens.space600}px ${tokens.space400}px ${tokens.space800}px`. Children: (1) shield badge circle (56x56px, `border-radius:${tokens.radiusFull}px`, `background:rgba(255,255,255,0.09)` with comment `/* #FFFFFF18 — design alpha fill, no token */`, centered flex, "🛡" unicode); (2) trust label div in `typeHeadingSm` composite, `color:${tokens.colorTextOnInverse}`, text "Secure Identity Scan", `margin-bottom:${tokens.space200}px`; (3) reassurance text div in `typeBodySm` composite, `color:${tokens.colorTextSecondary}`, text "Your data is encrypted and never shared.", `margin-bottom:${tokens.space600}px`; (4) CTA button full-width, `background:${tokens.colorSurfaceBase}`, `color:${tokens.colorTextPrimary}`, `border:none`, `border-radius:${tokens.radiusFull}px`, `min-height:${tokens.space1200}px`, `cursor:pointer`, font from `typeHeadingSm` composite, label from `ctaLabel` param.
    - Export `IdScan = () => panel('Scan My ID')`
    - Export `FacialScan = () => panel('Start Face Scan')`

    Create `stories/components/map-pin.stories.js`:
    - Import: `import * as tokens from '../../generated/tokens.js';`
    - Default export: `export default { title: 'Components/MapPin' };`
    - Export `RangePin`: inline-flex div with `display:inline-flex`, `align-items:center`, `gap:${tokens.space100}px`, `background:${tokens.colorGrey900}`, `color:${tokens.colorTextOnInverse}`, `padding:${tokens.space100}px ${tokens.space200}px`, `border-radius:${tokens.radiusFull}px`. Children: zap icon span `color:${tokens.colorActionPrimary}`, `font-size:12px`, text "⚡"; label span in `typeLabelSm` composite, text "0.3 km".
    - Export `SelectedPin`: position:relative container div (64x64px, inline-flex, centered). Two children: (1) pulse ring absolute div (56x56px, `border-radius:${tokens.radiusFull}px`, `background:rgba(198,255,45,0.20)` — #c6ff2d at 20% opacity approximation for actionPrimary tint); (2) pin badge relative div with `background:${tokens.colorSurfaceBase}`, `border-radius:${tokens.radiusFull}px`, `padding:${tokens.space100}px ${tokens.space200}px`, `display:inline-flex`, `gap:${tokens.space100}px`, `align-items:center`. Badge children: bike emoji "🚲" at 12px, label span "VV-042" in `typeLabelSm` composite, `color:${tokens.colorTextPrimary}`.
  </action>
  <verify>
    <automated>
      node -e "
        Promise.all([
          import('./stories/components/trust-panel.stories.js'),
          import('./stories/components/map-pin.stories.js'),
        ]).then(([tp, mp]) => {
          if (!tp.IdScan || !tp.FacialScan) throw new Error('TrustPanel missing export');
          if (!mp.RangePin || !mp.SelectedPin) throw new Error('MapPin missing export');
          const tpHtml = tp.IdScan();
          if (!tpHtml.includes('rgba(255,255,255,0.09)')) throw new Error('TrustPanel missing alpha fill');
          console.log('trust-panel and map-pin OK');
        });
      " 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `stories/components/trust-panel.stories.js` exists
    - File contains: `export const IdScan`, `export const FacialScan`
    - File contains: `title: 'Components/TrustPanel'`
    - File contains: `rgba(255,255,255,0.09)` with comment referencing `#FFFFFF18`
    - File contains: `tokens.colorGrey900` (panel bg), `tokens.colorSurfaceBase` (CTA button bg)
    - File contains: `tokens.radiusXl` (panel top corners), `tokens.radiusFull` (CTA pill)
    - File contains: `tokens.typeHeadingSm.fontSize` or `.fontWeight` (composite access)
    - `stories/components/map-pin.stories.js` exists
    - File contains: `export const RangePin`, `export const SelectedPin`
    - File contains: `title: 'Components/MapPin'`
    - File contains: `tokens.colorActionPrimary` (zap icon color)
    - File contains: `tokens.colorGrey900` (RangePin bg)
    - File contains: `tokens.typeLabelSm.fontSize` or `.fontWeight` (composite access)
    - Both files contain: `import * as tokens from '../../generated/tokens.js'`
  </acceptance_criteria>
  <done>TrustPanel story has IdScan and FacialScan exports with dark surface, alpha fill comment, and correct token usage. MapPin story has RangePin and SelectedPin exports with correct pill + pulse ring layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static story files | HTML template literal strings; no user input, no network, no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | story HTML | accept | Static dev documentation; no PII, no secrets |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No new installs; phase uses only existing packages |
</threat_model>

<verification>
After both tasks complete:
1. `grep -l "export const PhoneActive\|export const EmailActive" stories/components/segmented-toggle.stories.js` — 1 match
2. `grep "rgba(255,255,255,0.20)" stories/components/progress-strip.stories.js` — 1 match with FFFFFF33 comment
3. `grep "rgba(255,255,255,0.09)" stories/components/trust-panel.stories.js` — 1 match with FFFFFF18 comment
4. `grep -r "import \* as tokens from '../../generated/tokens.js'" stories/components/segmented-toggle.stories.js stories/components/progress-strip.stories.js stories/components/trust-panel.stories.js stories/components/map-pin.stories.js` — 4 matches
</verification>

<success_criteria>
- 4 story files created in stories/components/
- All 4 import from ../../generated/tokens.js
- Alpha fills hardcoded with inline comments (no missing token stub references)
- No elevation helpers needed in this plan (tab-bar and bottom-card handle that in 03-04)
- Typography tokens accessed as composite objects throughout
</success_criteria>

<output>
Create `.planning/phases/03-component-library-storybook/03-03-SUMMARY.md` when done
</output>
