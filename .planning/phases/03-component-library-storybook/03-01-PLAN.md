---
phase: 03-component-library-storybook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stories/components/.gitkeep
  - stories/screens/.gitkeep
  - .storybook/main.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-06

must_haves:
  truths:
    - "stories/components/ directory exists and is tracked by git"
    - "stories/screens/ directory exists and is tracked by git"
    - "Storybook main.js glob covers stories/components/ and stories/screens/ subdirectories"
    - "A stub story placed in stories/components/ is discovered by Storybook without touching main.js"
  artifacts:
    - path: "stories/components/.gitkeep"
      provides: "components directory placeholder"
    - path: "stories/screens/.gitkeep"
      provides: "screens directory placeholder"
    - path: ".storybook/main.js"
      provides: "Storybook config — glob verified or updated to cover subdirs"
  key_links:
    - from: ".storybook/main.js"
      to: "stories/components/*.stories.js"
      via: "glob pattern ../stories/**/*.stories.js"
      pattern: "stories/\\*\\*/\\*\\.stories\\.js"
---

<objective>
Prepare the Storybook project for Phase 3 component and screen story authoring. Verify that the existing story glob covers subdirectories without modification, then create the two new story directories. If the glob does NOT cover subdirs, update main.js with explicit globs — but do not change main.js unnecessarily.

Purpose: Wave 2 and Wave 3 plans depend on stories/components/ and stories/screens/ existing AND being discovered by Storybook. This plan gates the rest of the phase.
Output: Two new directories, confirmed glob coverage, ROADMAP already updated (done in planning session — no task needed here).
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/03-component-library-storybook/03-RESEARCH.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/PROJECT.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md

<interfaces>
<!-- From .storybook/main.js (Phase 2) — current config -->
Current stories glob (Phase 2 output):
  stories: ['../stories/**/*.stories.js']

fast-glob semantics: ** matches zero or more path segments.
- '../stories/**/*.stories.js' DOES match '../stories/components/button.stories.js'
- '../stories/**/*.stories.js' DOES match '../stories/screens/home-map.stories.js'
- '../stories/**/*.stories.js' DOES match '../stories/color.stories.js' (zero intermediate dirs)

This means NO main.js change is needed if the existing glob is exactly '../stories/**/*.stories.js'.
If main.js uses a different pattern (e.g. '../stories/*.stories.js' without **), update it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create story subdirectories</name>
  <files>stories/components/.gitkeep, stories/screens/.gitkeep</files>
  <read_first>
    - `.storybook/main.js` — read the exact stories glob string before deciding if update is needed
    - `stories/elevation.stories.js` — confirm current stories/ layout and file-naming convention
  </read_first>
  <action>
    Create two new directories under stories/:
    - `stories/components/` with an empty `.gitkeep` file so git tracks the directory
    - `stories/screens/` with an empty `.gitkeep` file

    Both directories must exist as real filesystem paths (not just tracked by concept). The .gitkeep files are zero-byte files — write an empty string.

    Do NOT create any story files in this task — that is Wave 2/3 work.
    Do NOT modify any existing stories/ files.
  </action>
  <verify>
    <automated>
      cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && ls stories/components/.gitkeep stories/screens/.gitkeep
    </automated>
  </verify>
  <done>Both .gitkeep files exist on disk. stories/components/ and stories/screens/ directories are present.</done>
</task>

<task type="auto">
  <name>Task 2: Verify glob coverage and update main.js if needed</name>
  <files>.storybook/main.js</files>
  <read_first>
    - `.storybook/main.js` — read the full file; extract the exact `stories:` array value
    - `.planning/phases/03-component-library-storybook/03-RESEARCH.md` Section 2 — glob analysis and fallback recommendation
  </read_first>
  <action>
    Step 1: Read .storybook/main.js and extract the stories glob string.

    Step 2: Evaluate the glob against fast-glob ** semantics:
    - If stories array contains `'../stories/**/*.stories.js'` (with **): NO change needed. The glob already covers subdirectories per D-02 research finding. Document this in a code comment on the stories line: `// covers stories/, stories/components/, stories/screens/ via ** glob`
    - If stories array contains `'../stories/*.stories.js'` (without **): UPDATE main.js — replace with `'../stories/**/*.stories.js'` and add the same comment.
    - If stories array has any other pattern: apply judgment to ensure it recursively covers all three directories.

    Step 3: Create a temporary stub file at `stories/components/glob-check.stories.js` with content:
    ```
    export default { title: 'Components/GlobCheck' };
    export const Pass = () => '<p>glob ok</p>';
    ```
    Then run `npm run build-storybook -- --quiet 2>&1 | tail -20` to confirm the build includes the new file without errors.
    After confirming (exit code 0 or story count includes GlobCheck), delete `stories/components/glob-check.stories.js`.

    Step 4: If the build fails because the glob did NOT pick up the stub, update main.js to add explicit globs:
    ```js
    stories: [
      '../stories/**/*.stories.js',
      '../stories/components/**/*.stories.js',
      '../stories/screens/**/*.stories.js',
    ],
    ```
    Then re-run the stub build test to confirm it passes. Delete the stub file once confirmed.

    Per D-02: Do not change the Storybook framework, do not add addons, do not change any other main.js setting.
  </action>
  <verify>
    <automated>
      cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('.storybook/main.js').then(m => { const s = JSON.stringify(m.default.stories); console.log('stories glob:', s); if (!s.includes('**')) process.exit(1); })"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.storybook/main.js` stories array contains `**` in the glob pattern
    - `stories/components/glob-check.stories.js` does NOT exist (stub was deleted)
    - `npm run build-storybook` exits 0 (or the stub-test build exited 0 before cleanup)
    - No addons, framework, or other main.js settings were altered
  </acceptance_criteria>
  <done>
    Glob verified to cover stories/components/ and stories/screens/. main.js updated only if required. Stub file created, tested, and deleted. Storybook can discover story files in both new subdirectories.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem | Story files are static HTML template literals — no user input, no auth, no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | .storybook/main.js | accept | File is developer-controlled config; no user input path; changes are git-tracked |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Phase 3 (per D-02 and Section 9 of RESEARCH.md); existing lockfile unchanged |
</threat_model>

<verification>
After both tasks complete:
1. `ls stories/components/ stories/screens/` — both directories exist
2. `.storybook/main.js` stories glob contains `**`
3. No `glob-check.stories.js` stub remains
4. `npm run build-storybook` exits 0 with storybook-static/ produced (the stub-test build serves as this verification)
</verification>

<success_criteria>
- stories/components/ and stories/screens/ directories exist on disk
- Storybook glob confirmed to cover both new subdirectories
- main.js changed only if glob was insufficient; existing Foundation stories unaffected
- Wave 2 and Wave 3 plans can run immediately: no further setup needed
</success_criteria>

<output>
Create `.planning/phases/03-component-library-storybook/03-01-SUMMARY.md` when done
</output>
