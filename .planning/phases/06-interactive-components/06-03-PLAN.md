---
phase: 06-interactive-components
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - stories/components/button.stories.js
  - stories/components/social-auth-buttons.stories.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "Button Interactive export shows all 4 variants (Primary, Secondary, Ghost, Disabled) in one phone frame"
    - "Primary button responds to pointerdown (darken + scale) and pointerup/pointerleave (restore)"
    - "Secondary button responds to pointerdown (darken + scale) and pointerup/pointerleave (restore)"
    - "Ghost button responds to pointerdown (opacity 0.6) and pointerup/pointerleave (opacity 1)"
    - "Disabled button has pointer-events:none and no press state"
    - "SocialAuthButtons Interactive shows Apple and Google buttons with independent press opacity effects"
    - "All existing static exports retained"
  artifacts:
    - path: "stories/components/button.stories.js"
      provides: "Interactive export — 4 button variants with press state"
      contains: "export const Interactive"
    - path: "stories/components/social-auth-buttons.stories.js"
      provides: "Interactive export — Apple + Google buttons with pointerdown opacity"
      contains: "export const Interactive"
  key_links:
    - from: "Primary button element"
      to: "pointerdown → #a8de1a + scale(0.97)"
      via: "addEventListener('pointerdown')"
      pattern: "addEventListener.*pointerdown"
    - from: "Ghost button element"
      to: "pointerdown → opacity 0.6"
      via: "addEventListener('pointerdown')"
      pattern: "opacity.*0\\.6"
---

<objective>
Add the `Interactive` named export to button.stories.js and social-auth-buttons.stories.js.
These are the press-state components: pointer events change visual state (scale + color for buttons,
opacity for social auth) and restore on release.

Purpose: Deliver the first genuinely interactive exports — components that visually respond to touch input.
Output: 2 modified story files with `Interactive` as their first named export, event-wired via addEventListener.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-01-SUMMARY.md

<interfaces>
<!-- Exact interaction state table (from UI-SPEC.md) — implement these values verbatim -->

Button (Primary):
  pointerdown:  bg → tokens.colorGreen600 (#a8de1a), transform → scale(0.97)
  pointerup/pointerleave: bg → tokens.colorActionPrimary (#c6ff2d), transform → scale(1)
  CSS transition on element: 'transform 100ms ease, background-color 100ms ease'
  label: "Book a Ride"
  text color: tokens.colorTextPrimary (#0f0f0f)

Button (Secondary):
  pointerdown:  bg → tokens.colorGrey800 (#2f2f2f), transform → scale(0.97)
  pointerup/pointerleave: bg → tokens.colorActionSecondary (#0f0f0f), transform → scale(1)
  CSS transition: 'transform 100ms ease, background-color 100ms ease'
  label: "Continue"
  text color: tokens.colorTextOnInverse (#ffffff)

Button (Ghost):
  pointerdown:  opacity → 0.6
  pointerup/pointerleave: opacity → 1
  CSS transition: 'opacity 100ms ease'
  label: "Sign in instead"
  text color: tokens.colorTextPrimary (#0f0f0f)
  border: 1px solid tokens.colorBorderSubtle (#ebebeb)

Button (Disabled):
  pointer-events: none  (no interaction — static)
  bg: tokens.colorGrey200 (#ebebeb)
  label: "Book a Ride"
  text color: tokens.colorTextDisabled (#c9c9c9)

SocialAuthButtons (Apple):
  pointerdown:  opacity → 0.7
  pointerup/pointerleave: opacity → 1
  CSS transition: 'opacity 100ms ease'
  bg: tokens.colorActionPrimary (#c6ff2d)   [Apple button uses electric green bg]
  label: "Continue with Apple"
  icon: Apple logo unicode &#63743; (or inline SVG stub)

SocialAuthButtons (Google):
  pointerdown:  opacity → 0.7
  pointerup/pointerleave: opacity → 1
  bg: tokens.colorSurfaceBase (#ffffff)
  border: 1px solid tokens.colorBorderSubtle (#ebebeb)
  label: "Continue with Google"

Common button styles:
  border-radius: tokens.radiusFull (999)px
  height: tokens.space1200 (48)px
  width: 100% of container
  font-size: tokens.typeHeadingSm.fontSize (15)px
  font-weight: tokens.typeHeadingSm.fontWeight (600)
  line-height: tokens.typeHeadingSm.lineHeight (20)px
  font-family: Inter, sans-serif
  cursor: pointer
  border: none (except Ghost and Google)

<!-- makePhoneFrame() helper — same inline-copy spec as Plan 02 -->
function makePhoneFrame() {
  // outer: width:402px, height:874px, background:#0f0f0f, border-radius:44px, padding:6px,
  //        box-sizing:border-box, position:relative, overflow:hidden, display:inline-block
  // inner screen: width:100%, height:100%, background:#ffffff, border-radius:38px,
  //               overflow:hidden, position:relative, display:flex, flex-direction:column
  // status bar: flex-shrink:0, height:54px, background:#0f0f0f, display:flex,
  //             align-items:center, justify-content:space-between, padding:0 20px
  //   left child: "9:41" — Inter 15px 600 #ffffff
  //   right child: "&#9646; WiFi &#9650;" — Inter 11px #ffffff
  // returns { frame, screen }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Button — Interactive export with 4-variant press states</name>
  <files>stories/components/button.stories.js</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
  </read_first>
  <action>
    Prepend to `button.stories.js`, after the `import` and `export default` lines:

    1. The `makePhoneFrame()` inline helper (same spec as Plan 02 interfaces block).

    2. The `Interactive` named export, structured as:
       - Opens with `/* @storybook/html-vite — returns DOM element */` comment (D-10).
       - Calls `makePhoneFrame()` to get `{ frame, screen }`.
       - Creates a content area div appended to `screen`: `flex:1; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:16px; padding:0 16px; box-sizing:border-box`.
       - Creates 4 button elements via `document.createElement('button')` — one for each variant (Primary, Secondary, Ghost, Disabled). Do NOT use innerHTML to create button elements that need event listeners — create them with createElement so listeners can be attached.
       - Each button: `width:100%; height:48px; border-radius:999px; font-family:Inter,sans-serif; font-size:15px; font-weight:600; line-height:20px; cursor:pointer; border:none`.
       - Apply variant-specific initial styles (background, color, border, pointer-events) per the interfaces table above.
       - Apply CSS transition to each interactive button via `.style.transition`.
       - Wire `pointerdown`, `pointerup`, `pointerleave` listeners to Primary, Secondary, Ghost buttons using `addEventListener`. Each listener directly mutates `.style.backgroundColor`, `.style.transform`, or `.style.opacity` on the button element — DO NOT rebuild innerHTML on state change (destroys listeners).
       - Disabled button: set `style.pointerEvents = 'none'` — no event listeners attached.
       - Append all 4 buttons to the content area, append content area to `screen`.
       - Returns `frame`.

    The existing `Primary`, `Secondary`, `Ghost`, `Disabled`, and `SourceCode` exports remain after `Interactive`, unchanged.

    ANTI-PATTERN TO AVOID: Do not set `innerHTML` on the content area after attaching event listeners — this destroys all listeners. Set innerHTML (if used) only BEFORE calling addEventListener, and prefer createElement for elements that need listeners.
  </action>
  <acceptance_criteria>
    - `button.stories.js` contains `export const Interactive =` before `export const Primary`
    - File contains `/* @storybook/html-vite — returns DOM element */`
    - File contains `function makePhoneFrame()`
    - File contains `document.createElement('button')` (at least one — for button variants)
    - File contains `addEventListener('pointerdown'` (at least one)
    - File contains `addEventListener('pointerup'` (at least one)
    - File contains `addEventListener('pointerleave'` (at least one)
    - File contains `scale(0.97)` (Primary/Secondary press transform)
    - File contains `opacity.*0\.6` matched by regex — Ghost press state
    - File contains `pointerEvents.*none` or `pointer-events.*none` — Disabled button
    - File contains `width:402px` and `height:874px`
    - File contains `return frame`
    - `export const Primary` still exists (not deleted)
    - `export const Secondary` still exists
    - `export const Ghost` still exists
    - `export const Disabled` still exists
    - `export const SourceCode` still exists
    - File does NOT contain `onclick=` attributes
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('./stories/components/button.stories.js').then(m => { const el = m.Interactive(); const btns = el.querySelectorAll('button'); console.log('frame tag:', el?.tagName, 'buttons:', btns.length); }).catch(e => console.error(e))"</automated>
  </verify>
  <done>
    `Interactive` is the first named export returning a DIV frame. `el.querySelectorAll('button')` returns 4 elements. All 5 existing exports (Primary, Secondary, Ghost, Disabled, SourceCode) still present.
  </done>
</task>

<task type="auto">
  <name>Task 2: SocialAuthButtons — Interactive export with independent press opacity</name>
  <files>stories/components/social-auth-buttons.stories.js</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/social-auth-buttons.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
  </read_first>
  <action>
    Prepend to `social-auth-buttons.stories.js`, after the `import` and `export default` lines:

    1. The `makePhoneFrame()` inline helper.

    2. The `Interactive` named export:
       - Opens with `/* @storybook/html-vite — returns DOM element */` comment.
       - Calls `makePhoneFrame()` to get `{ frame, screen }`.
       - Creates a content area div: `flex:1; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:12px; padding:0 16px; box-sizing:border-box`.
       - Creates 2 button elements via `document.createElement('button')`:
         - Apple button: bg `tokens.colorActionPrimary` (#c6ff2d), text `tokens.colorTextPrimary` (#0f0f0f), label "Continue with Apple", Apple icon prefix `\u{F8FF}` or `&#63743;`.
         - Google button: bg `tokens.colorSurfaceBase` (#ffffff), text `tokens.colorTextPrimary` (#0f0f0f), border `1px solid ${tokens.colorBorderSubtle}`, label "Continue with Google".
         - Both buttons: `width:100%; height:48px; border-radius:999px; font-family:Inter,sans-serif; font-size:15px; font-weight:600; cursor:pointer; transition:opacity 100ms ease`.
       - Wire `pointerdown`, `pointerup`, `pointerleave` on EACH button independently:
         - pointerdown → `.style.opacity = '0.7'`
         - pointerup / pointerleave → `.style.opacity = '1'`
       - Append both buttons to content area, append to `screen`.
       - Returns `frame`.

    Existing `AppleButton`, `GoogleButton`, and `SourceCode` exports remain after `Interactive`, untouched.
  </action>
  <acceptance_criteria>
    - `social-auth-buttons.stories.js` contains `export const Interactive =` before `export const AppleButton`
    - File contains `function makePhoneFrame()`
    - File contains `document.createElement('button')` (at least 2 occurrences for the two buttons)
    - File contains `addEventListener('pointerdown'`
    - File contains `opacity.*0\.7` matched by regex — press state
    - File contains `width:402px` and `height:874px`
    - File contains `return frame`
    - `export const AppleButton` still exists
    - `export const GoogleButton` still exists
    - `export const SourceCode` still exists
    - File does NOT contain `onclick=` attributes
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('./stories/components/social-auth-buttons.stories.js').then(m => { const el = m.Interactive(); const btns = el.querySelectorAll('button'); console.log('frame tag:', el?.tagName, 'buttons:', btns.length); }).catch(e => console.error(e))"</automated>
  </verify>
  <done>
    `Interactive` is the first named export returning a DIV frame. `el.querySelectorAll('button')` returns 2 elements. `AppleButton`, `GoogleButton`, `SourceCode` still present.
  </done>
</task>

</tasks>

<threat_model>
Threat surface: None. Plan 03 modifies only Storybook story files (stories/components/*.stories.js).
These are development-time documentation files with no backend, no authentication, no user data,
no network requests, and no production deployment surface. Event listeners are scoped to the
Storybook canvas iframe. No OWASP top-10 applicable.
</threat_model>

<verification>
Both files: `export const Interactive =` appears before first existing export.
Node import confirms each Interactive() returns a DIV with the expected number of button children.
Event listener wiring verified by presence of `addEventListener` calls (not onclick= attributes).
All existing exports retained.
</verification>

<success_criteria>
Button and SocialAuthButtons story files each have `Interactive` as their first named export.
Button Interactive contains 4 createElement button children with press-state listeners on Primary, Secondary, Ghost.
SocialAuthButtons Interactive contains 2 createElement button children with opacity press listeners.
All existing static exports retained in both files.
</success_criteria>

<output>
Create `.planning/phases/06-interactive-components/06-03-SUMMARY.md` when done.
</output>
