---
phase: 06-interactive-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - D-01
  - D-04

must_haves:
  truths:
    - "The existing Storybook build exits 0 before any Phase 6 changes land"
    - "The makePhoneFrame() helper contract is documented for Wave 2/3 executors"
  artifacts:
    - path: "storybook-static/"
      provides: "Baseline build output confirming pre-Phase-6 clean state"
  key_links: []
---

<objective>
Verify the baseline Storybook build is clean before any story files are touched in Phase 6.
If the build fails now, it must be fixed before Wave 2 plans run.

Purpose: Prevent Wave 2/3 executors from inheriting a broken build and misattributing failures to their edits.
Output: Confirmed-clean `storybook-static/` from `npm run build-storybook`.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline build verification</name>
  <files>none — read-only verification</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.storybook/main.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/tab-bar.stories.js
  </read_first>
  <action>
    Run the build pipeline in sequence:
    1. `npm run build:tokens` from the project root — regenerates `generated/tokens.js`.
    2. After build:tokens, run `git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts` to revert the lib/ mutation (per the persistent lib/ mutation warning — SD regenerates these files on every build:tokens run, dropping manual overrides).
    3. `npm run build-storybook` — must exit 0 with `storybook-static/` produced.

    If either command fails, fix the underlying issue before marking this plan complete. Do NOT proceed to Wave 2 plans with a broken baseline.

    Capture the build output's final line (e.g. "info => Output directory: storybook-static") as proof of success.
  </action>
  <acceptance_criteria>
    - `npm run build-storybook` exits 0
    - `storybook-static/index.html` exists (confirms full output was written)
    - `git status lib/` shows lib/voltventure_theme.ts and lib/voltventure_tokens.ts with NO modifications (reverted after build:tokens — manual overrides preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && npm run build-storybook 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build-storybook` exits 0. `storybook-static/` directory exists and contains `index.html`.
    `lib/voltventure_theme.ts` and `lib/voltventure_tokens.ts` are NOT in `git status` as modified (reverted after build:tokens).
  </done>
</task>

</tasks>

<threat_model>
Threat surface: None. Plan 01 runs only build commands and reads existing files. No story files are
modified. No backend, no auth, no user data. No OWASP top-10 applicable.
</threat_model>

<verification>
`npm run build-storybook` exits 0 with storybook-static/ present.
`git status` shows no unexpected modifications to lib/ files.
</verification>

<success_criteria>
Storybook builds cleanly from the current main branch. Wave 2 and Wave 3 plans can proceed.
</success_criteria>

<output>
Create `.planning/phases/06-interactive-components/06-01-SUMMARY.md` when done.
</output>
