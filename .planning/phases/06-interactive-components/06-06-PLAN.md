---
phase: 06-interactive-components
plan: 06
type: execute
wave: 3
depends_on:
  - "06-01"
files_modified:
  - stories/components/tab-bar.stories.js
  - stories/components/bottom-card.stories.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "TabBar Interactive export is positioned at bottom of phone frame via position:absolute; bottom:0; width:100%"
    - "Clicking any of the 4 tabs updates the active indicator circle and label color"
    - "BottomCard Interactive export is positioned at bottom of phone frame via position:absolute; bottom:0; width:100%"
    - "Clicking the drag handle toggles card height between 120px (collapsed) and 320px (expanded) via height transition"
    - "TabBar and BottomCard files retain their existing hexToRgba and shadowFromToken inline helpers"
    - "All existing static exports retained in both files"
  artifacts:
    - path: "stories/components/tab-bar.stories.js"
      provides: "Interactive export — 4-tab bottom nav with active state switching"
      contains: "export const Interactive"
    - path: "stories/components/bottom-card.stories.js"
      provides: "Interactive export — expand/collapse card with height transition"
      contains: "export const Interactive"
  key_links:
    - from: "TabBar screen div"
      to: "tabBar element at bottom:0"
      via: "position:relative on screen + position:absolute on tabBar"
      pattern: "position:absolute.*bottom:0|bottom:0.*position:absolute"
    - from: "drag handle click"
      to: "card height transition 120px ↔ 320px"
      via: "addEventListener click on handle"
      pattern: "120px|320px"
---

<objective>
Add the `Interactive` named export to tab-bar.stories.js and bottom-card.stories.js.
Both components must be `position:absolute; bottom:0` within the phone frame's screen div,
which requires `position:relative` on the screen div (confirmed in the makePhoneFrame() spec).

The existing `hexToRgba` and `shadowFromToken` helpers are already inline in both files —
they must be retained and reused in the Interactive exports.

Purpose: Deliver the bottom-pinned interactive stories — the most layout-sensitive components.
Output: 2 modified story files with bottom-pinned, event-wired Interactive exports.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-01-SUMMARY.md

<interfaces>
<!-- TabBar interaction contract (UI-SPEC.md + RESEARCH.md) -->

NOTE — Tab label discrepancy:
  - Existing static exports use: ['Ride', 'Discover', 'Wallet', 'Account']
  - UI-SPEC.md copywriting contract says: "Home", "Ride", "Rewards", "Profile"
  - RESOLUTION: Use UI-SPEC.md copywriting — "Home", "Ride", "Rewards", "Profile" — as it represents
    the approved design contract for Phase 6. The static exports use the wireframe draft labels.
    The Interactive export implements the UI-SPEC values.

State: let activeTab = 'Ride'  (initial)

Tab array: ['Home', 'Ride', 'Rewards', 'Profile']

Per tab visual:
  active:
    indicator circle: 32px × 32px, border-radius:50%, background:tokens.colorActionPrimary (#c6ff2d)
    label text: color tokens.colorTextPrimary (#0f0f0f)
  inactive:
    indicator circle: 32px × 32px, border-radius:50%, background:tokens.colorGrey200 (#ebebeb)
    label text: color tokens.colorTextSecondary (#808080)

Tab layout: each tab is a flex column (icon circle + label), flex:1, align-items:center, gap:tokens.space100 (4px)
Tab bar container: width:100%, position:absolute, bottom:0, left:0, right:0
  background: tokens.colorSurfaceBase (#ffffff)
  box-shadow: shadowFromToken(tokens.elevationFloating)  [requires hexToRgba helper]
  padding: 8px 0 12px
  display:flex

On click of any tab div:
  → activeTab = clicked label
  → loop over all tab divs, update indicator.style.background and label.style.color
  → DO NOT rebuild innerHTML (destroys all listeners)
  → Retain references to both indicatorEl and labelEl for each tab in a structure like:
    const tabEls = tabs.map(label => { indicatorEl, labelEl, containerEl })

hexToRgba and shadowFromToken helpers:
  Already exist in tab-bar.stories.js — DO NOT add duplicates.
  The Interactive export uses these existing helpers directly.

<!-- BottomCard interaction contract (UI-SPEC.md + RESEARCH.md) -->

State: let expanded = false

Card element:
  position:absolute, bottom:0, left:0, right:0 (width:100%)
  background: tokens.colorSurfaceBase (#ffffff)
  border-radius: tokens.radiusLg (20)px tokens.radiusLg (20)px 0 0  (top corners only)
  box-shadow: shadowFromToken(tokens.elevationRaised)  [requires hexToRgba helper]
  overflow: hidden
  transition: height 300ms ease
  height: '120px'  (initial, collapsed)

Drag handle:
  width:40px, height:3px, border-radius:999px, background:tokens.colorGrey200 (#ebebeb)
  margin: 12px auto 0
  cursor: pointer

BikeSelection content (shown in collapsed and expanded states):
  - Bike ID "VV-042" badge: background tokens.colorGreen100 (#f4ffd9), color tokens.colorGreen700 (#7d9220)
  - Title: "Nearby Bikes" or similar (from existing BikeSelection export — read the file)
  - Thumbnail rows (only visible when expanded, overflow:hidden clips them in collapsed state)

On click of drag handle:
  → expanded = !expanded
  → card.style.height = expanded ? '320px' : '120px'
  (Transition is already set on card element — height change animates automatically)

hexToRgba and shadowFromToken helpers:
  Already exist in bottom-card.stories.js — DO NOT add duplicates.
  The Interactive export uses these existing helpers directly.

<!-- makePhoneFrame() spec for both files -->
NOTE: The screen div MUST have position:relative for TabBar and BottomCard's position:absolute bottom:0 to work.
The makePhoneFrame() helper as specified in Plans 02-05 includes position:relative on the screen div.
Verify the screen div in your makePhoneFrame() copy has: position:relative (CRITICAL — see RESEARCH.md Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabBar — Interactive export with 4-tab active state switching</name>
  <files>stories/components/tab-bar.stories.js</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/tab-bar.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
  </read_first>
  <action>
    Read the full tab-bar.stories.js file first. It already has `hexToRgba`, `shadowFromToken`, a `TABS` const, and a `tabBar(activeTab)` string-returning function. The `Interactive` export does NOT call `tabBar()` (string return — cannot attach listeners). Build its DOM independently using the same visual design.

    Prepend to `tab-bar.stories.js`, after all existing top-level helpers (`hexToRgba`, `shadowFromToken`, `TABS`, `tabBar`), but BEFORE the first existing export (`RideActive`):

    1. The `makePhoneFrame()` inline helper. NOTE: The screen div in makePhoneFrame MUST include `position:relative` in its cssText (confirm this is present — it is critical for `position:absolute; bottom:0` to work for TabBar).

    2. The `Interactive` named export:
       - Opens with `/* @storybook/html-vite — returns DOM element */` comment.
       - State variable: `let activeTab = 'Ride'`.
       - Tab labels array: `const INTERACTIVE_TABS = ['Home', 'Ride', 'Rewards', 'Profile']` (UI-SPEC copywriting, different from existing TABS const which uses wireframe draft labels).
       - Calls `makePhoneFrame()` to get `{ frame, screen }`. Screen must have `position:relative` (prerequisite for absolute bottom positioning).
       - Creates the tab bar container div: `position:absolute; bottom:0; left:0; right:0; background:${tokens.colorSurfaceBase}; display:flex; padding:8px 0 12px; box-shadow:${shadowFromToken(tokens.elevationFloating)}`.
       - Creates 4 tab divs (one per label) via `document.createElement('div')`. For each tab, also create its `indicator` div and `labelEl` span. Store references: `const tabRefs = []`.
         - Tab div: `flex:1; display:flex; flex-direction:column; align-items:center; gap:4px; cursor:pointer`.
         - Indicator div: `width:32px; height:32px; border-radius:50%`.
         - Label span: `font-size:11px; font-weight:500; font-family:Inter,sans-serif`.
         - Initial state for 'Ride' tab: indicator bg `tokens.colorActionPrimary`, label color `tokens.colorTextPrimary`.
         - All other tabs: indicator bg `tokens.colorGrey200`, label color `tokens.colorTextSecondary`.
         - Push `{ label, indicator, labelEl, tabDiv }` into tabRefs.
       - Wire `click` on each tab div. In the handler, capture the label in closure:
         ```
         tabDiv.addEventListener('click', () => {
           activeTab = label;
           tabRefs.forEach(ref => {
             const isActive = ref.label === activeTab;
             ref.indicator.style.background = isActive ? tokens.colorActionPrimary : tokens.colorGrey200;
             ref.labelEl.style.color = isActive ? tokens.colorTextPrimary : tokens.colorTextSecondary;
           });
         });
         ```
       - Append indicators and labels to tab divs, tab divs to tab bar container, tab bar container to `screen`.
       - Returns `frame`.

    CRITICAL: Do NOT rebuild innerHTML on state change. Mutate only `.style.background` and `.style.color` via the tabRefs references.

    The existing `RideActive`, `DiscoverActive`, `WalletActive`, `AccountActive`, and `SourceCode` exports remain after `Interactive`, unchanged. The existing `TABS` const and `tabBar()` function remain untouched.
  </action>
  <acceptance_criteria>
    - `tab-bar.stories.js` contains `export const Interactive =` before `export const RideActive`
    - File contains `/* @storybook/html-vite — returns DOM element */`
    - File contains `function makePhoneFrame()`
    - File contains `position:relative` in the screen div's cssText inside makePhoneFrame (critical for absolute positioning)
    - File contains `position:absolute` applied to the tab bar container (bottom:0 placement)
    - File contains `bottom:0` in the tab bar container styles
    - File contains `['Home', 'Ride', 'Rewards', 'Profile']` or `INTERACTIVE_TABS` with these labels (UI-SPEC values)
    - File contains `tabRefs` or equivalent structure holding references to indicator and label elements
    - File contains `addEventListener.*click` on tab divs (event wiring)
    - File contains `shadowFromToken` called with `tokens.elevationFloating` (reuses existing helper)
    - File contains `width:402px` and `height:874px`
    - File contains `return frame`
    - `export const RideActive` still exists (original TABS-based export retained)
    - `export const DiscoverActive` still exists
    - `export const WalletActive` still exists
    - `export const AccountActive` still exists
    - `export const SourceCode` still exists
    - `function hexToRgba` still exists (not deleted)
    - `function shadowFromToken` still exists (not deleted)
    - File does NOT contain `onclick=` attributes
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('./stories/components/tab-bar.stories.js').then(m => { const el = m.Interactive(); const tabs = el.querySelectorAll('[style*=cursor]'); console.log('frame tag:', el?.tagName, 'clickable tabs:', tabs.length); }).catch(e => console.error(e))"</automated>
  </verify>
  <done>
    `Interactive` is the first named export returning a DIV frame. `el.querySelectorAll('[style*=cursor]')` returns at least 4 elements. All 5 existing exports retained. hexToRgba and shadowFromToken functions still present.
  </done>
</task>

<task type="auto">
  <name>Task 2: BottomCard — Interactive expand/collapse with height transition</name>
  <files>stories/components/bottom-card.stories.js</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/bottom-card.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
  </read_first>
  <action>
    Read the full bottom-card.stories.js file. It already has `hexToRgba` and `shadowFromToken` helpers inline. The `Interactive` export reuses these helpers — do NOT add duplicate copies.

    Prepend to `bottom-card.stories.js`, after all existing top-level helpers (`hexToRgba`, `shadowFromToken`) but BEFORE the first existing export (`BikeSelection`):

    1. The `makePhoneFrame()` inline helper. Verify screen div has `position:relative`.

    2. The `Interactive` named export:
       - Opens with `/* @storybook/html-vite — returns DOM element */` comment.
       - State variable: `let expanded = false`.
       - Calls `makePhoneFrame()` to get `{ frame, screen }`.
       - Creates the card div (BikeSelection variant):
         - `position:absolute; bottom:0; left:0; right:0`
         - `background:${tokens.colorSurfaceBase}` (#ffffff)
         - `border-radius:${tokens.radiusLg}px ${tokens.radiusLg}px 0 0`
         - `box-shadow:${shadowFromToken(tokens.elevationRaised)}`
         - `overflow:hidden`
         - `transition:height 300ms ease`
         - `height:120px`  (initial collapsed)
         - `box-sizing:border-box`
       - Creates the drag handle div (first child of card):
         - `width:40px; height:3px; border-radius:999px; background:${tokens.colorGrey200}; margin:12px auto 0; cursor:pointer`
       - Creates the card body content for BikeSelection variant (shown inside card, clipped by overflow:hidden at collapsed height):
         - Use innerHTML on the card body area for the static BikeSelection content (same markup as the existing `BikeSelection` export — read the file for exact markup). This is safe because no event listeners need to be on the card content — only the drag handle gets a listener.
         - Append drag handle first, then card body content to card div.
       - Wire `click` on the drag handle:
         ```
         handle.addEventListener('click', () => {
           expanded = !expanded;
           card.style.height = expanded ? '320px' : '120px';
         });
         ```
       - Append card to `screen`.
       - Returns `frame`.

    CRITICAL: `overflow:hidden` on the card + height transition is the collapse mechanism.
    The card body content can use innerHTML (it has no event listeners — only the drag handle does).
    Do NOT attach the click listener to innerHTML-created elements.

    Existing `BikeSelection`, `WalkProgress`, and `SourceCode` exports remain after `Interactive`, unchanged.
  </action>
  <acceptance_criteria>
    - `bottom-card.stories.js` contains `export const Interactive =` before `export const BikeSelection`
    - File contains `/* @storybook/html-vite — returns DOM element */`
    - File contains `function makePhoneFrame()`
    - File contains `position:relative` in screen div's cssText inside makePhoneFrame
    - File contains `position:absolute` applied to card div (bottom:0 placement)
    - File contains `bottom:0` in card styles
    - File contains `let expanded = false` (state variable)
    - File contains `transition:height 300ms ease` or `transition.*height.*300ms` on card element
    - File contains `120px` (collapsed height)
    - File contains `320px` (expanded height)
    - File contains `addEventListener.*click` on drag handle
    - File contains `shadowFromToken.*elevationRaised` or `elevationRaised` used with shadowFromToken
    - File contains `width:402px` and `height:874px`
    - File contains `return frame`
    - `export const BikeSelection` still exists
    - `export const WalkProgress` still exists
    - `export const SourceCode` still exists
    - `function hexToRgba` still exists (not deleted)
    - `function shadowFromToken` still exists (not deleted)
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('./stories/components/bottom-card.stories.js').then(m => { const el = m.Interactive(); const card = el.querySelector('[style*=absolute]'); console.log('frame tag:', el?.tagName, 'abs card found:', !!card); }).catch(e => console.error(e))"</automated>
  </verify>
  <done>
    `Interactive` is the first named export returning a DIV frame. An absolutely-positioned card div is present in the DOM. `BikeSelection`, `WalkProgress`, `SourceCode` still present. hexToRgba and shadowFromToken still present.
  </done>
</task>

</tasks>

<threat_model>
Threat surface: None. Plan 06 modifies only Storybook story files (stories/components/*.stories.js).
These are development-time documentation files with no backend, no authentication, no user data,
no network requests, and no production deployment surface. Event listeners are scoped to the
Storybook canvas iframe. No OWASP top-10 applicable.
</threat_model>

<verification>
Both files: `export const Interactive =` appears before existing first export.
Node import confirms Interactive() returns DOM elements.
TabBar: at least 4 clickable tab elements present; tab bar container has position:absolute bottom:0.
BottomCard: absolute card div present; handle has click listener (verified by presence of addEventListener call in source).
hexToRgba and shadowFromToken helpers present and unmodified in both files.
All existing exports retained.
</verification>

<success_criteria>
TabBar Interactive: 4 tabs (Home/Ride/Rewards/Profile from UI-SPEC), clicking any tab updates
indicator circle bg (#c6ff2d active, #ebebeb inactive) and label color. Tab bar pinned to bottom
of phone frame via position:absolute bottom:0; position:relative on screen div.
BottomCard Interactive: card starts collapsed (120px). Clicking drag handle expands to 320px
and back via `height 300ms ease` transition. Card pinned to bottom via position:absolute bottom:0.
All existing exports retained in both files. No duplicate hexToRgba/shadowFromToken functions added.
</success_criteria>

<output>
Create `.planning/phases/06-interactive-components/06-06-SUMMARY.md` when done.
</output>
