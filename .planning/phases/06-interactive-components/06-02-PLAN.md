---
phase: 06-interactive-components
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - stories/components/status-bar.stories.js
  - stories/components/or-divider.stories.js
  - stories/components/trust-panel.stories.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "All 3 story files export `Interactive` as their first named export"
    - "Each `Interactive` function returns a DOM element (not an HTML string)"
    - "Phone frame is 402x874px, Volt Black (#0F0F0F) bezel, 44px border-radius, 6px padding"
    - "Status bar shows '9:41' text and battery icon in every frame"
    - "All existing static exports (LightSurface, DarkSurface, Default, IdScan, FacialScan, SourceCode) are retained unchanged"
  artifacts:
    - path: "stories/components/status-bar.stories.js"
      provides: "Interactive export — phone frame where StatusBar IS the top chrome"
      contains: "export const Interactive"
    - path: "stories/components/or-divider.stories.js"
      provides: "Interactive export — OrDivider centered in phone frame, static"
      contains: "export const Interactive"
    - path: "stories/components/trust-panel.stories.js"
      provides: "Interactive export — TrustPanel static, anchored bottom of phone frame"
      contains: "export const Interactive"
  key_links:
    - from: "Interactive export"
      to: "document.createElement"
      via: "makePhoneFrame() inline helper"
      pattern: "document\\.createElement"
    - from: "makePhoneFrame"
      to: "402px / 874px dimensions"
      via: "inline cssText"
      pattern: "402px"
---

<objective>
Add the `Interactive` named export (first export in file) to the three static component stories:
StatusBar, OrDivider, and TrustPanel. These components have no interactive state per D-07 —
their Interactive export is the phone frame wrapper with the component displayed at its
context-appropriate position.

Purpose: Establish the `makePhoneFrame()` inline helper pattern that all Wave 2 and Wave 3
plans will replicate. These three files are the lowest-risk starting point.
Output: 3 modified story files, each with `Interactive` as their first named export returning a DOM element.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-01-SUMMARY.md

<interfaces>
<!-- makePhoneFrame() helper spec — inline-copy into every story file, NOT imported -->
<!-- Returns { frame, screen } where frame is the 402x874 outer div, screen is the inner white div -->

function makePhoneFrame() {
  // Outer bezel
  const frame = document.createElement('div');
  frame.style.cssText = [
    'width:402px', 'height:874px', 'background:#0f0f0f',
    'border-radius:44px', 'padding:6px', 'box-sizing:border-box',
    'position:relative', 'overflow:hidden', 'display:inline-block',
    'font-family:Inter,sans-serif'
  ].join(';');

  // Inner screen
  const screen = document.createElement('div');
  screen.style.cssText = [
    'width:100%', 'height:100%', 'background:#ffffff',
    'border-radius:38px', 'overflow:hidden', 'position:relative',
    'display:flex', 'flex-direction:column'
  ].join(';');

  // Status bar (top chrome — always present)
  const bar = document.createElement('div');
  bar.style.cssText = [
    'flex-shrink:0', 'height:54px', 'background:#0f0f0f',
    'display:flex', 'align-items:center', 'justify-content:space-between',
    'padding:0 20px', 'box-sizing:border-box'
  ].join(';');
  bar.innerHTML = '<span style="font-family:Inter,sans-serif;font-size:15px;font-weight:600;line-height:20px;color:#ffffff;">9:41</span>'
    + '<span style="font-family:Inter,sans-serif;font-size:11px;color:#ffffff;">&#9646; WiFi &#9650;</span>';

  screen.appendChild(bar);
  frame.appendChild(screen);
  return { frame, screen };
}

// Token import already present at top of each file:
// import * as tokens from '../../generated/tokens.js';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusBar — Interactive export (phone frame IS the component)</name>
  <files>stories/components/status-bar.stories.js</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/status-bar.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
  </read_first>
  <action>
    Prepend to `status-bar.stories.js`, immediately after the `import` statement and `export default` line, the following:

    1. The `makePhoneFrame()` helper function (inline-copy, per the spec in the interfaces block above — do NOT import from another file).
    2. The `Interactive` named export function, structured as follows:
       - Opens with `/* @storybook/html-vite — returns DOM element */` comment (per D-10).
       - Calls `makePhoneFrame()` to get `{ frame, screen }`.
       - The StatusBar IS the top chrome of the phone frame — the 54px black status bar bar already built inside makePhoneFrame() serves as the component display. No additional component element is added.
       - Below the status bar, create a content area div with `flex:1; display:flex; align-items:center; justify-content:center; background:#ffffff`. Add a single text span: `"Screen content"` styled with `font-family:Inter,sans-serif; font-size:13px; color:#808080` (tokens.colorTextSecondary = #808080).
       - Append the content area to `screen`.
       - Returns `frame` (the DOM element — NOT frame.outerHTML or any string).
       - No event listeners — static per D-07.

    The existing exports (`LightSurface`, `DarkSurface`, `SourceCode`) remain AFTER the `Interactive` export, untouched. Do not edit them.

    CRITICAL: `Interactive` must be declared before `LightSurface` in the file. Storybook renders exports in declaration order — Interactive first means it becomes the default view.
  </action>
  <acceptance_criteria>
    - `status-bar.stories.js` contains `export const Interactive =` before `export const LightSurface`
    - File contains `/* @storybook/html-vite — returns DOM element */` comment inside Interactive
    - File contains `function makePhoneFrame()` definition (not an import)
    - File contains `document.createElement` (proves DOM element creation)
    - File contains `width:402px` (confirms correct phone frame width, not old 393px)
    - File contains `height:874px` (correct phone frame height)
    - File contains `border-radius:44px` (correct bezel radius)
    - File contains `return frame` (not `return frame.outerHTML` or a template literal)
    - `export const LightSurface` still exists (retained, not deleted)
    - `export const DarkSurface` still exists (retained, not deleted)
    - `export const SourceCode` still exists (retained, not deleted)
    - File does NOT contain `onclick=` attributes inside any innerHTML string
    - File does NOT contain `<script` inside any innerHTML string
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('./stories/components/status-bar.stories.js').then(m => { const el = m.Interactive(); console.log('type:', typeof el, 'tag:', el?.tagName); }).catch(e => console.error(e))"</automated>
  </verify>
  <done>
    `Interactive` is the first named export. Running the export produces a DOM element (tagName: DIV). All three existing exports still present. No onclick= attributes in innerHTML strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: OrDivider — Interactive export (centered, static)</name>
  <files>stories/components/or-divider.stories.js</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/or-divider.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
  </read_first>
  <action>
    Prepend to `or-divider.stories.js`, after the `import` and `export default` lines:

    1. The `makePhoneFrame()` inline helper (same spec as above — no imports).
    2. The `Interactive` named export:
       - Opens with `/* @storybook/html-vite — returns DOM element */` comment.
       - Calls `makePhoneFrame()` to get `{ frame, screen }`.
       - Creates a content area div: `flex:1; display:flex; align-items:center; justify-content:center; padding:0 ${tokens.space400}px; box-sizing:border-box`.
       - Inside the content area, create the OrDivider markup as a DOM element. The OrDivider is two horizontal lines with "or" text between them. Use the same token values as the existing `Default` export: line color `tokens.colorBorderSubtle` (#ebebeb), "or" text color `tokens.colorTextSecondary` (#808080), font-size 13px Inter.
       - Set the content area's innerHTML to the OrDivider markup (this is allowed — no event listeners needed, so rebuilding innerHTML is safe here).
       - Append content area to `screen`.
       - Returns `frame`.
       - No event listeners — static per D-07.

    Existing `Default` and `SourceCode` exports remain after `Interactive`, untouched.
  </action>
  <acceptance_criteria>
    - `or-divider.stories.js` contains `export const Interactive =` before `export const Default`
    - File contains `function makePhoneFrame()`
    - File contains `document.createElement`
    - File contains `width:402px` and `height:874px`
    - File contains `return frame` (not `return frame.outerHTML`)
    - `export const Default` still exists
    - `export const SourceCode` still exists
    - File does NOT contain `onclick=` attributes inside innerHTML strings
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('./stories/components/or-divider.stories.js').then(m => { const el = m.Interactive(); console.log('type:', typeof el, 'tag:', el?.tagName); }).catch(e => console.error(e))"</automated>
  </verify>
  <done>
    `Interactive` is the first named export returning a DIV. `Default` and `SourceCode` still present. No event listeners (not needed).
  </done>
</task>

<task type="auto">
  <name>Task 3: TrustPanel — Interactive export (static, panel bottom-anchored)</name>
  <files>stories/components/trust-panel.stories.js</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/trust-panel.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
  </read_first>
  <action>
    Prepend to `trust-panel.stories.js`, after the `import` and `export default` lines:

    1. The `makePhoneFrame()` inline helper.
    2. The `Interactive` named export:
       - Opens with `/* @storybook/html-vite — returns DOM element */` comment.
       - Calls `makePhoneFrame()` to get `{ frame, screen }`.
       - The screen's content area (below the status bar) must have `position:relative` — this is already set by makePhoneFrame. No change needed.
       - Create the TrustPanel element: a div with `position:absolute; bottom:0; left:0; right:0`. Use the `panel('Scan My ID')` variant as the default (same markup as the existing `IdScan` export). The panel background is `tokens.colorGrey900` (#1a1a1a), text color `tokens.colorTextOnInverse` (#ffffff). Alpha fill `rgba(255,255,255,0.09)` is a non-token constant (document inline with comment `/* #FFFFFF18 — no token */`).
       - Set the TrustPanel div's innerHTML to the panel markup.
       - Append the TrustPanel div to `screen`.
       - Returns `frame`.
       - No event listeners — static per D-07.

    Existing `IdScan`, `FacialScan`, and `SourceCode` exports remain after `Interactive`, untouched.
  </action>
  <acceptance_criteria>
    - `trust-panel.stories.js` contains `export const Interactive =` before `export const IdScan`
    - File contains `function makePhoneFrame()`
    - File contains `document.createElement`
    - File contains `width:402px` and `height:874px`
    - File contains `position:absolute` (TrustPanel bottom-pinned)
    - File contains `bottom:0` (confirms bottom anchoring per D-03)
    - File contains `return frame`
    - `export const IdScan` still exists
    - `export const FacialScan` still exists
    - `export const SourceCode` still exists
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && node -e "import('./stories/components/trust-panel.stories.js').then(m => { const el = m.Interactive(); console.log('type:', typeof el, 'tag:', el?.tagName); }).catch(e => console.error(e))"</automated>
  </verify>
  <done>
    `Interactive` is the first named export returning a DIV. All three existing exports still present.
  </done>
</task>

</tasks>

<threat_model>
Threat surface: None. Plan 02 modifies only Storybook story files (stories/components/*.stories.js).
These are development-time documentation files with no backend, no authentication, no user data,
no network requests, and no production deployment surface. Event listeners are scoped to the
Storybook canvas iframe. No OWASP top-10 applicable.
</threat_model>

<verification>
All 3 files: grep confirms `export const Interactive =` appears before existing first export.
Node import test confirms each Interactive() call returns an object with tagName DIV.
Existing exports still present in each file.
</verification>

<success_criteria>
StatusBar, OrDivider, and TrustPanel story files each have `Interactive` as their first named export.
Each Interactive export returns a DOM element (not a string). Phone frame dimensions are 402x874px.
The makePhoneFrame() inline helper pattern is established for Wave 3 plans to follow.
</success_criteria>

<output>
Create `.planning/phases/06-interactive-components/06-02-SUMMARY.md` when done.
</output>
