---
phase: 06-interactive-components
plan: 07
type: execute
wave: 4
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
  - "06-05"
  - "06-06"
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-02
  - D-04
  - D-08
  - D-09

must_haves:
  truths:
    - "All 11 component story files have `export const Interactive =` as their first named export"
    - "npm run build-storybook exits 0 with storybook-static/ produced"
    - "No existing static exports were accidentally removed from any file"
    - "lib/voltventure_theme.ts and lib/voltventure_tokens.ts are clean after build:tokens revert"
  artifacts:
    - path: "storybook-static/"
      provides: "Built Storybook output — confirms all 11 Interactive exports are valid and Storybook can build"
  key_links:
    - from: "npm run build-storybook"
      to: "storybook-static/index.html"
      via: "Vite build"
      pattern: "storybook-static/index.html"
---

<objective>
Phase 6 done-bar: verify all 11 component story files have `Interactive` as their first named export,
run `npm run build-storybook` to confirm zero build errors, then confirm existing static exports
are retained, and commit the phase.

Purpose: Gate all Wave 2 and Wave 3 story edits under a single verified build before marking Phase 6 complete.
Output: Green build, human-verified Storybook canvas, git commit closing Phase 6.
</objective>

<execution_context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-RESEARCH.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/phases/06-interactive-components/06-UI-SPEC.md
</execution_context>

<context>
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
@D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification — 11 Interactive exports + build</name>
  <files>none — read-only verification + build commands</files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/status-bar.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/button.stories.js
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/stories/components/bottom-card.stories.js
  </read_first>
  <action>
    Run the following automated checks in order. If any check fails, stop and report the failure before proceeding.

    Step 1 — Verify all 11 files have `export const Interactive`:
    Run for each of the 11 story files in stories/components/:
    ```
    grep -c "export const Interactive" stories/components/status-bar.stories.js
    grep -c "export const Interactive" stories/components/button.stories.js
    grep -c "export const Interactive" stories/components/social-auth-buttons.stories.js
    grep -c "export const Interactive" stories/components/or-divider.stories.js
    grep -c "export const Interactive" stories/components/phone-input.stories.js
    grep -c "export const Interactive" stories/components/segmented-toggle.stories.js
    grep -c "export const Interactive" stories/components/progress-strip.stories.js
    grep -c "export const Interactive" stories/components/trust-panel.stories.js
    grep -c "export const Interactive" stories/components/map-pin.stories.js
    grep -c "export const Interactive" stories/components/tab-bar.stories.js
    grep -c "export const Interactive" stories/components/bottom-card.stories.js
    ```
    Each must return exactly 1. If any return 0, that plan's executor did not complete.

    Step 2 — Verify `Interactive` is the first named export in each file (not just present anywhere):
    For each file, confirm that `export const Interactive` appears before the first other `export const` (e.g., `export const Primary`, `export const LightSurface`, etc.).
    Use: `grep -n "^export const" stories/components/status-bar.stories.js | head -2` — first line must be Interactive.
    Repeat for all 11 files.

    Step 3 — Verify key static exports are retained (spot check):
    ```
    grep -c "export const LightSurface" stories/components/status-bar.stories.js        # must be 1
    grep -c "export const Primary" stories/components/button.stories.js                  # must be 1
    grep -c "export const PhoneActive" stories/components/segmented-toggle.stories.js    # must be 1
    grep -c "export const Step1Active" stories/components/progress-strip.stories.js      # must be 1
    grep -c "export const RideActive" stories/components/tab-bar.stories.js              # must be 1
    grep -c "export const BikeSelection" stories/components/bottom-card.stories.js       # must be 1
    ```

    Step 4 — Build:
    ```
    npm run build:tokens
    git restore lib/voltventure_theme.ts lib/voltventure_tokens.ts
    npm run build-storybook
    ```
    build-storybook must exit 0. storybook-static/ must be produced.

    Step 5 — Verify lib/ files clean after revert:
    `git status lib/` must show no unstaged changes to lib/ files.

    If all steps pass, proceed to the human-verify checkpoint.
  </action>
  <acceptance_criteria>
    - All 11 grep -c commands return 1
    - For each file: `grep -n "^export const" ... | head -1` shows "Interactive"
    - Spot-check grep commands each return 1 (static exports retained)
    - `npm run build-storybook` exits 0
    - `storybook-static/` directory contains `index.html`
    - `git status lib/` shows no modifications
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && for f in stories/components/*.stories.js; do count=$(grep -c "export const Interactive" "$f" 2>/dev/null || echo 0); echo "$f: $count"; done</automated>
  </verify>
  <done>
    All 11 files output "1" for Interactive export count. Build exits 0. lib/ clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Storybook canvas visual + interaction check</name>
  <what-built>
    11 Interactive exports across all component story files. Each wraps its component in a 402×874px iPhone 16 Pro phone frame (Volt Black #0F0F0F bezel, 44px border-radius, 54px status bar showing "9:41"). Components are event-wired with real JS interactivity per D-07.
  </what-built>
  <how-to-verify>
    Run: `npm run storybook` (opens at http://localhost:6006)

    For each component, navigate to Components / [name] / Interactive and verify:

    1. StatusBar — phone frame renders; status bar IS the top chrome; "Screen content" placeholder visible below; no errors in console.

    2. Button — phone frame renders; 4 button variants stacked vertically (Book a Ride primary, Continue secondary, Sign in instead ghost, disabled Book a Ride). Press and release Primary: bg darkens (#a8de1a) and scale 0.97 on press, restores on release. Same pattern for Secondary. Ghost dims on press.

    3. SocialAuthButtons — phone frame; Apple (green bg) and Google (white border) buttons centered. Press each → opacity dims to 0.7, releases back to 1.

    4. OrDivider — phone frame; "or" divider centered; no interaction; no console errors.

    5. PhoneInput — phone frame; input field visible with "+1 (___) ___-____" placeholder. Click field → green focus ring (2px #c6ff2d) and blinking cursor appear. Type digits via keyboard → display updates. Click away → focus ring disappears.

    6. SegmentedToggle — phone frame; Phone/Email toggle centered. Click Email → Email gets green bg, Phone goes white. Click Phone → restores. Toggle switches cleanly without full re-render.

    7. ProgressStrip — phone frame; 4-segment strip visible (dark background). "Step 1 of 4" label. Click "Next →" → strip advances, label updates. Click "Next →" 3 more times → reaches Step 4 of 4, Next button dims/disabled. Click "← Back" → retreats.

    8. TrustPanel — phone frame; trust panel anchored to bottom of screen; "Scan My ID" CTA; no interaction; no errors.

    9. MapPin — phone frame; two pins over gray map (#e8e8e8). Click the green SelectedPin → pulse ring animates outward and fades. Click again → animation restarts cleanly (no stuck state).

    10. TabBar — phone frame; 4 tabs (Home, Ride, Rewards, Profile) pinned to bottom. "Ride" is initially active (green circle). Click any other tab → that tab becomes active, others go inactive.

    11. BottomCard — phone frame; card docked at bottom, collapsed (shows drag handle only + top content). Click drag handle → card animates open (320px). Click again → collapses back (120px).

    Also confirm: Foundation and Screens stories in other sections still work (no regressions).
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 Interactive exports work correctly. Describe any specific failures to fix before approval.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Git commit + planning doc update</name>
  <files>
    stories/components/status-bar.stories.js
    stories/components/button.stories.js
    stories/components/social-auth-buttons.stories.js
    stories/components/or-divider.stories.js
    stories/components/phone-input.stories.js
    stories/components/segmented-toggle.stories.js
    stories/components/progress-strip.stories.js
    stories/components/trust-panel.stories.js
    stories/components/map-pin.stories.js
    stories/components/tab-bar.stories.js
    stories/components/bottom-card.stories.js
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/ROADMAP.md
    D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System/.planning/STATE.md
  </read_first>
  <action>
    Run after human approval in Task 2.

    1. Update `.planning/ROADMAP.md`: Mark Phase 6 plans as complete:
       - Replace `**Status:** Ready for planning` with `**Status:** COMPLETE (2026-08-05)`
       - Replace `**Plans:** TBD` with `**Plans:** 7 plans`
       - Add the plan list with all 7 plans checked.

    2. Update `.planning/STATE.md`:
       - Update `stopped_at` to: `Phase 6 complete — interactive components done (2026-08-05)`
       - Add Phase 6 progress line: `execute-6   [██████████] 100% — Phase 6 COMPLETE (2026-08-05)`
       - Add decision: `Phase 6 COMPLETE (2026-08-05) — Interactive exports added to all 11 component story files; 402×874 phone frame; JS event listeners via addEventListener`

    3. Stage only the story files and planning docs (do not stage storybook-static/ — it is in .gitignore):
       ```
       git add stories/components/status-bar.stories.js
       git add stories/components/button.stories.js
       git add stories/components/social-auth-buttons.stories.js
       git add stories/components/or-divider.stories.js
       git add stories/components/phone-input.stories.js
       git add stories/components/segmented-toggle.stories.js
       git add stories/components/progress-strip.stories.js
       git add stories/components/trust-panel.stories.js
       git add stories/components/map-pin.stories.js
       git add stories/components/tab-bar.stories.js
       git add stories/components/bottom-card.stories.js
       git add .planning/ROADMAP.md
       git add .planning/STATE.md
       git add .planning/phases/06-interactive-components/
       ```

    4. Commit (no Co-Authored-By per user preference in MEMORY.md):
       ```
       git commit -m "feat(06): add Interactive exports to all 11 component stories — iPhone 16 Pro phone frame + JS event listeners"
       ```

    5. Verify clean working tree: `git status` should show nothing to commit.
  </action>
  <acceptance_criteria>
    - `git log --oneline -1` shows the feat(06) commit message
    - `git status` shows clean working tree (no modified files)
    - ROADMAP.md contains `COMPLETE (2026-08-05)` for Phase 6
    - STATE.md contains `Phase 6 COMPLETE`
  </acceptance_criteria>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/5. VoltVenture app/6. Design System" && git log --oneline -1 && git status --short</automated>
  </verify>
  <done>
    git log shows feat(06) commit. git status shows clean tree. ROADMAP and STATE updated.
  </done>
</task>

</tasks>

<threat_model>
Threat surface: None. Plan 07 runs build commands, grep verification, and git operations.
No story files are modified. No backend, no auth, no user data, no production surface.
No OWASP top-10 applicable.
</threat_model>

<verification>
All 11 files: `grep -c "export const Interactive"` returns 1 for each.
Interactive is first export in each file (grep -n head -1 check).
Key static exports spot-check all return 1.
`npm run build-storybook` exits 0.
Human-verified in Storybook dev mode — all 11 interactive stories work.
Clean git commit lands on main.
</verification>

<success_criteria>
Phase 6 complete:
- All 11 component stories have `Interactive` as their first named export
- Each Interactive export returns a DOM element (not string) with a 402×874 phone frame
- JS event listeners deliver working interactivity per D-07 interaction contracts
- npm run build-storybook exits 0
- Human-verified all 11 interactions in Storybook dev mode
- Clean git commit on main branch
</success_criteria>

<output>
Create `.planning/phases/06-interactive-components/06-07-SUMMARY.md` when done.
</output>
