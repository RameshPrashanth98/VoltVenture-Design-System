---
phase: 4
plan: 2
title: "Expo App Scaffold — apps/showcase/ with metro config, navigation skeleton, PaperProvider, fonts"
wave: 2
depends_on: [1]
files_modified:
  - apps/showcase/package.json
  - apps/showcase/app.json
  - apps/showcase/tsconfig.json
  - apps/showcase/metro.config.js
  - apps/showcase/app/_layout.tsx
  - apps/showcase/app/index.tsx
  - apps/showcase/app/[item].tsx
  - apps/showcase/src/data/registry.ts
autonomous: true
requirements: [DS-SCAFFOLD]

must_haves:
  truths:
    - "npx expo start from apps/showcase/ launches Metro without module resolution errors"
    - "PaperProvider wraps the app root with createVoltVentureTheme() theme"
    - "Fonts load via useFonts() with SplashScreen.preventAutoHideAsync() hold"
    - "Home screen renders a SectionList with Components (11) and Screens (9) section headers"
    - "Tapping any list item navigates to a detail screen placeholder"
    - "TypeScript compiles without errors (npx tsc --noEmit from apps/showcase/)"
  artifacts:
    - path: "apps/showcase/package.json"
      provides: "Showcase app dependencies including workspace dep on voltventure-design-system"
      contains: "voltventure-design-system"
    - path: "apps/showcase/app/_layout.tsx"
      provides: "App root with PaperProvider + useFonts + SplashScreen"
      contains: "PaperProvider"
    - path: "apps/showcase/app/index.tsx"
      provides: "Home screen two-section list"
      contains: "SectionList"
    - path: "apps/showcase/app/[item].tsx"
      provides: "Dynamic detail screen with Preview + Code tab placeholder"
      contains: "REGISTRY"
    - path: "apps/showcase/src/data/registry.ts"
      provides: "Item registry mapping name to Preview component + sourceCode string"
      contains: "REGISTRY"
  key_links:
    - from: "apps/showcase/app/_layout.tsx"
      to: "voltventure-design-system"
      via: "import { createVoltVentureTheme }"
    - from: "apps/showcase/app/[item].tsx"
      to: "apps/showcase/src/data/registry.ts"
      via: "import { REGISTRY }"
    - from: "apps/showcase/tsconfig.json"
      to: "lib/index.ts"
      via: "compilerOptions.paths voltventure-design-system"
---

<objective>
Scaffold the complete showcase Expo app with navigation structure, font loading, theme wiring, and skeleton implementations ready for component/screen content to be filled in.

Purpose: Establishes the full app structure so Wave 2/3 component plans can write into src/components/ and src/screens/ without touching navigation or app root files.
Output: A running (but mostly empty) showcase app that Metro can start and navigate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md
@.planning/phases/04-rn-paper-showcase-app/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create apps/showcase/package.json, tsconfig.json, metro.config.js, app.json</name>
  <files>apps/showcase/package.json, apps/showcase/tsconfig.json, apps/showcase/metro.config.js, apps/showcase/app.json</files>

  <read_first>
    - package.json (root) — confirms name is now voltventure-design-system and workspaces includes apps/*
    - .planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md — Standard Stack table for exact versions; Pattern 1 for workspace:* dep; Pattern 2 for tsconfig paths
  </read_first>

  <action>
    Create apps/showcase/package.json with:
    - "name": "voltventure-showcase"
    - "version": "1.0.0"
    - "private": true
    - "main": "expo-router/entry"
    - "scripts": { "start": "expo start", "android": "expo start --android", "ios": "expo start --ios", "ts-check": "tsc --noEmit" }
    - "dependencies": voltventure-design-system workspace:*, expo ~57.0.9, react 18.3.1, react-native 0.78.0, react-native-paper ^5.15.3, expo-font ~57.0.1, expo-splash-screen ~57.0.5, expo-status-bar ~57.0.1, expo-router ~4.0.0, @expo-google-fonts/inter ^0.4.2, @expo-google-fonts/manjari ^0.4.1, @expo-google-fonts/jetbrains-mono ^0.4.1, react-native-safe-area-context ^5.8.0, react-native-screens ^4.26.2, react-native-code-highlighter ^1.3.0, react-syntax-highlighter ^16.1.1
    - "devDependencies": @types/react ^18.3.0, @types/react-syntax-highlighter ^15.5.0, typescript ~5.3.0

    Create apps/showcase/tsconfig.json extending "expo/tsconfig.base" with compilerOptions: strict true, paths { "voltventure-design-system": ["../../lib/index.ts"] }.

    Create apps/showcase/metro.config.js as a CommonJS file (not ESM — Metro config must be CJS): require expo/metro-config, call getDefaultConfig(__dirname), export the result. No custom sourceExts or watchFolders needed per RESEARCH.md Pattern 2 (SDK 57 auto-detects monorepos). Use: `const { getDefaultConfig } = require('expo/metro-config'); const config = getDefaultConfig(__dirname); module.exports = config;`

    Create apps/showcase/app.json with expo config: name "VoltVenture Showcase", slug "voltventure-showcase", version "1.0.0", scheme "voltventureshowcase", platforms ["ios","android"], sdkVersion "57.0.0".
  </action>

  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('apps/showcase/package.json','utf8')); console.log(p.dependencies['voltventure-design-system'], p.dependencies['expo'])"</automated>
  </verify>

  <acceptance_criteria>
    - apps/showcase/package.json exists; field "dependencies"."voltventure-design-system" equals "workspace:*"
    - apps/showcase/package.json lists react-native-paper, expo-font, expo-splash-screen, expo-router, all three @expo-google-fonts packages, react-native-code-highlighter, react-syntax-highlighter
    - apps/showcase/tsconfig.json exists; contains paths entry mapping "voltventure-design-system" to ["../../lib/index.ts"]
    - apps/showcase/metro.config.js exists; contains `getDefaultConfig` from expo/metro-config; is CommonJS (uses require, not import)
    - apps/showcase/app.json exists; slug is "voltventure-showcase"
  </acceptance_criteria>

  <done>Showcase package.json, tsconfig.json, metro.config.js, and app.json created with correct workspace dep and monorepo config</done>
</task>

<task type="auto">
  <name>Task 2: Create app root _layout.tsx with PaperProvider + useFonts + SplashScreen</name>
  <files>apps/showcase/app/_layout.tsx</files>

  <read_first>
    - .planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md — Pattern 3 (PaperProvider + useFonts) and Pattern 4 (exact font variable names from expo-google-fonts); the complete _layout.tsx code example in the Code Examples section
    - lib/voltventure_theme.ts — confirms function name is createVoltVentureTheme (no args)
  </read_first>

  <action>
    Create apps/showcase/app/_layout.tsx. The file must:
    - Call SplashScreen.preventAutoHideAsync() at module level (before component definition, outside any function)
    - Create theme constant at module level: `const theme = createVoltVentureTheme();`
    - Export a default function RootLayout
    - Inside RootLayout: call useFonts with { Manjari_400Regular, Manjari_700Bold, Inter_400Regular, Inter_500Medium, Inter_600SemiBold, JetBrainsMono_400Regular }
    - useEffect that calls SplashScreen.hideAsync() when loaded OR error (both conditions trigger hide)
    - Return null while !loaded && !error
    - Return PaperProvider with theme prop, wrapping a Stack with screenOptions={{ headerShown: false }}
    - Imports: useEffect from react; Stack from expo-router; PaperProvider from react-native-paper; * as SplashScreen from expo-splash-screen; useFonts + each named font export from their respective @expo-google-fonts packages; createVoltVentureTheme from voltventure-design-system
  </action>

  <verify>
    <automated>grep -c "PaperProvider" apps/showcase/app/_layout.tsx && grep -c "SplashScreen.preventAutoHideAsync" apps/showcase/app/_layout.tsx && grep -c "createVoltVentureTheme" apps/showcase/app/_layout.tsx</automated>
  </verify>

  <acceptance_criteria>
    - apps/showcase/app/_layout.tsx exists
    - Contains `SplashScreen.preventAutoHideAsync()` at module level (outside the component function)
    - Contains `const theme = createVoltVentureTheme()` at module level
    - Contains `useFonts` call with all 6 font variables (Manjari_400Regular, Manjari_700Bold, Inter_400Regular, Inter_500Medium, Inter_600SemiBold, JetBrainsMono_400Regular)
    - Contains `SplashScreen.hideAsync()` called when `loaded || error`
    - Contains `<PaperProvider theme={theme}>` wrapping `<Stack screenOptions={{ headerShown: false }} />`
    - Returns null before fonts are ready (early return guard)
  </acceptance_criteria>

  <done>App root layout created with font loading, splash screen hold, and PaperProvider theme injection</done>
</task>

<task type="auto">
  <name>Task 3: Create home screen, detail screen, and item registry skeleton</name>
  <files>apps/showcase/app/index.tsx, apps/showcase/app/[item].tsx, apps/showcase/src/data/registry.ts</files>

  <read_first>
    - .planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md — Pattern 6 (SectionList home screen), Pattern 7 (registry), the SECTIONS array with all 20 item names
  </read_first>

  <action>
    Create apps/showcase/src/data/registry.ts. This file is a SKELETON — it defines the REGISTRY type and shape, and exports an empty (or stub) REGISTRY map. Wave 2/3 plans will add real entries. Structure:
    - Import React (for ComponentType)
    - Export type RegistryEntry = { Preview: React.ComponentType; sourceCode: string; }
    - Export const REGISTRY: Record<string, RegistryEntry> = {} (empty object for now — Wave 2/3 fills it)

    Create apps/showcase/app/index.tsx with a home screen using SectionList from react-native. SECTIONS array must contain exactly:
    - Section "Components" with data: ['StatusBar', 'Button', 'SocialAuthButtons', 'OrDivider', 'PhoneInput', 'SegmentedToggle', 'ProgressStrip', 'TrustPanel', 'MapPin', 'TabBar', 'BottomCard']
    - Section "Screens" with data: ['Splash', 'Onboarding1', 'Registration', 'Login', 'IdScan', 'FacialScan', 'HomeMap', 'NavigateToBike', 'WalkingDirections']
    Use List.Subheader from react-native-paper for section headers, List.Item for rows, Divider between rows. Import useRouter from expo-router; on row press call router.push(`/${item}`). Use * as tokens from voltventure-design-system for backgroundColor on the SectionList container (tokens.colorSurfaceBase). Export default HomeScreen.

    Create apps/showcase/app/[item].tsx as a detail screen with Preview + Code tabs. Use useLocalSearchParams from expo-router to get the item name. Import REGISTRY from ../src/data/registry. If entry not found, render a Text saying "Coming soon: {item}". If found, render two tabs using a simple state-based tab switcher (not a library): two Pressable tab buttons labelled "Preview" and "Code" styled with tokens, and a content area showing either entry.Preview rendered as a component, or a CodeHighlighter with entry.sourceCode. Import CodeHighlighter from react-native-code-highlighter, atomOneDark from react-syntax-highlighter/dist/esm/styles/hljs, JetBrainsMono_400Regular fontFamily string used in textStyle. Export default ItemScreen.
  </action>

  <verify>
    <automated>grep -c "SectionList" apps/showcase/app/index.tsx && grep -c "REGISTRY" apps/showcase/app/\[item\].tsx && grep -c "RegistryEntry" apps/showcase/src/data/registry.ts</automated>
  </verify>

  <acceptance_criteria>
    - apps/showcase/app/index.tsx: SECTIONS array contains exactly 11 component names and 9 screen names as listed above
    - apps/showcase/app/index.tsx: uses List.Subheader for section headers, List.Item for rows, router.push on press
    - apps/showcase/app/[item].tsx: imports REGISTRY; uses useLocalSearchParams for item name; renders "Coming soon" when entry not found; renders Preview tab and Code tab when entry found
    - apps/showcase/app/[item].tsx: Code tab uses CodeHighlighter with atomOneDark style and JetBrainsMono_400Regular fontFamily
    - apps/showcase/src/data/registry.ts: exports REGISTRY as Record<string, RegistryEntry> and exports RegistryEntry type
    - No hardcoded hex color values in any of these three files — all colors from tokens.* or theme
  </acceptance_criteria>

  <done>Home screen SectionList, dynamic detail screen with Preview/Code tabs, and registry skeleton created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install (workspace) | Installs packages from npm registry into showcase node_modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | npm package installs | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit — all Approved |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck table present in RESEARCH.md; all packages Approved; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
After this plan executes, from the design system root:
- `npm install` — should link voltventure-design-system into apps/showcase/node_modules via workspace
- From apps/showcase/: `npx tsc --noEmit` — should exit 0 (no TypeScript errors in skeleton)
- From apps/showcase/: `npx expo start --no-dev` — Metro should start without module resolution errors
- Visually: home screen shows two sections; tapping shows "Coming soon: [name]" placeholder
</verification>

<success_criteria>
- apps/showcase/ directory exists with package.json, tsconfig.json, metro.config.js, app.json
- apps/showcase/app/_layout.tsx: PaperProvider + useFonts + SplashScreen guard
- apps/showcase/app/index.tsx: SectionList with 11 components + 9 screens
- apps/showcase/app/[item].tsx: Preview/Code tab detail screen with registry lookup
- apps/showcase/src/data/registry.ts: exports REGISTRY type and empty map
- npx tsc --noEmit exits 0 from apps/showcase/
</success_criteria>

<output>
Create `.planning/phases/04-rn-paper-showcase-app/04-02-SUMMARY.md` when done.
</output>
