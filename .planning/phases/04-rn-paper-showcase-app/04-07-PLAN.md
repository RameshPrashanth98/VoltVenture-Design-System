---
phase: 4
plan: 7
title: "Registry Merge — add all 20 entries to REGISTRY (5 remaining components + 9 screens)"
wave: 5
depends_on: [3, 4, 5, 6]
files_modified:
  - apps/showcase/src/data/registry.ts
autonomous: true
requirements: [DS-REGISTRY]

must_haves:
  truths:
    - "REGISTRY contains exactly 20 entries: 11 components + 9 screens"
    - "Every entry has a Preview ComponentType and a sourceCode string"
    - "Component names match exactly the strings in the SECTIONS array in app/index.tsx"
    - "No import errors from any missing Preview file"
  artifacts:
    - path: "apps/showcase/src/data/registry.ts"
      provides: "Complete registry with all 20 items"
      contains: "WalkingDirections"
  key_links:
    - from: "apps/showcase/app/[item].tsx"
      to: "apps/showcase/src/data/registry.ts"
      via: "REGISTRY lookup by item name from route param"
---

<objective>
Merge all component and screen Preview implementations into the single REGISTRY map. Plan 03 already added 6 component entries; this plan adds the remaining 5 components (from Plan 04) and all 9 screens (from Plans 05/06).

Purpose: After Plans 03-06 complete in parallel waves, the registry is the single integration point. This plan runs last in Wave 5 after all implementations are done.
Output: A complete registry.ts with all 20 entries; the showcase app can navigate to any item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rn-paper-showcase-app/04-03-SUMMARY.md
@.planning/phases/04-rn-paper-showcase-app/04-04-SUMMARY.md
@.planning/phases/04-rn-paper-showcase-app/04-05-SUMMARY.md
@.planning/phases/04-rn-paper-showcase-app/04-06-SUMMARY.md
</context>

<interfaces>
From apps/showcase/app/index.tsx (created in Plan 02), the SECTIONS data — the REGISTRY keys must match exactly:
Components: 'StatusBar', 'Button', 'SocialAuthButtons', 'OrDivider', 'PhoneInput', 'SegmentedToggle', 'ProgressStrip', 'TrustPanel', 'MapPin', 'TabBar', 'BottomCard'
Screens: 'Splash', 'Onboarding1', 'Registration', 'Login', 'IdScan', 'FacialScan', 'HomeMap', 'NavigateToBike', 'WalkingDirections'

From apps/showcase/src/data/registry.ts (after Plan 03):
Already has entries: StatusBar, Button, SocialAuthButtons, OrDivider, PhoneInput, SegmentedToggle
Needs entries: ProgressStrip, TrustPanel, MapPin, TabBar, BottomCard (from Plan 04 files)
            + Splash, Onboarding1, Registration, Login, IdScan (from Plan 05 files)
            + FacialScan, HomeMap, NavigateToBike, WalkingDirections (from Plan 06 files)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Complete REGISTRY with all remaining 14 entries</name>
  <files>apps/showcase/src/data/registry.ts</files>

  <read_first>
    - apps/showcase/src/data/registry.ts — read current state (has 6 entries from Plan 03); identify existing imports and REGISTRY object to append to
    - apps/showcase/src/components/ProgressStripPreview.tsx — confirm export names
    - apps/showcase/src/components/TrustPanelPreview.tsx — confirm export names
    - apps/showcase/src/components/MapPinPreview.tsx — confirm export names
    - apps/showcase/src/components/TabBarPreview.tsx — confirm export names
    - apps/showcase/src/components/BottomCardPreview.tsx — confirm export names
    - apps/showcase/src/screens/SplashPreview.tsx — confirm export names
    - apps/showcase/src/screens/Onboarding1Preview.tsx — confirm export names
    - apps/showcase/src/screens/RegistrationPreview.tsx — confirm export names
    - apps/showcase/src/screens/LoginPreview.tsx — confirm export names
    - apps/showcase/src/screens/IdScanPreview.tsx — confirm export names
    - apps/showcase/src/screens/FacialScanPreview.tsx — confirm export names
    - apps/showcase/src/screens/HomeMapPreview.tsx — confirm export names
    - apps/showcase/src/screens/NavigateToBikePreview.tsx — confirm export names
    - apps/showcase/src/screens/WalkingDirectionsPreview.tsx — confirm export names
  </read_first>

  <action>
    Read the current registry.ts carefully. It already has the RegistryEntry type, REGISTRY export, and 6 component imports + entries. Add the 14 missing entries WITHOUT removing the existing 6.

    Add imports for the 5 remaining components (from ../components/):
    - ProgressStripPreview + ProgressStripSourceCode from ../components/ProgressStripPreview
    - TrustPanelPreview + TrustPanelSourceCode from ../components/TrustPanelPreview
    - MapPinPreview + MapPinSourceCode from ../components/MapPinPreview
    - TabBarPreview + TabBarSourceCode from ../components/TabBarPreview
    - BottomCardPreview + BottomCardSourceCode from ../components/BottomCardPreview

    Add imports for all 9 screens (from ../screens/):
    - SplashPreview + SplashSourceCode from ../screens/SplashPreview
    - Onboarding1Preview + Onboarding1SourceCode from ../screens/Onboarding1Preview
    - RegistrationPreview + RegistrationSourceCode from ../screens/RegistrationPreview
    - LoginPreview + LoginSourceCode from ../screens/LoginPreview
    - IdScanPreview + IdScanSourceCode from ../screens/IdScanPreview
    - FacialScanPreview + FacialScanSourceCode from ../screens/FacialScanPreview
    - HomeMapPreview + HomeMapSourceCode from ../screens/HomeMapPreview
    - NavigateToBikePreview + NavigateToBikeSourceCode from ../screens/NavigateToBikePreview
    - WalkingDirectionsPreview + WalkingDirectionsSourceCode from ../screens/WalkingDirectionsPreview

    Add REGISTRY entries using EXACTLY these key names (case-sensitive, matches SECTIONS array):
    ProgressStrip, TrustPanel, MapPin, TabBar, BottomCard (components)
    Splash, Onboarding1, Registration, Login, IdScan, FacialScan, HomeMap, NavigateToBike, WalkingDirections (screens)

    Final REGISTRY must have exactly 20 keys total.
  </action>

  <verify>
    <automated>grep -c "Preview:" apps/showcase/src/data/registry.ts</automated>
  </verify>

  <acceptance_criteria>
    - apps/showcase/src/data/registry.ts contains exactly 20 entries (grep -c "Preview:" returns 20)
    - All 11 component keys present: StatusBar, Button, SocialAuthButtons, OrDivider, PhoneInput, SegmentedToggle, ProgressStrip, TrustPanel, MapPin, TabBar, BottomCard
    - All 9 screen keys present: Splash, Onboarding1, Registration, Login, IdScan, FacialScan, HomeMap, NavigateToBike, WalkingDirections
    - No TypeScript import errors (all import paths resolve to actual files created in Plans 03-06)
    - Key names match EXACTLY the strings in the SECTIONS data array in app/index.tsx (case-sensitive)
  </acceptance_criteria>

  <done>REGISTRY contains all 20 entries; showcase can navigate to any component or screen detail</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dynamic route lookup | REGISTRY[item] lookup from URL param — item param comes from navigation, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | Route param item lookup | accept | REGISTRY lookup returns undefined for unknown keys; [item].tsx renders "Coming soon" fallback — no crash, no code execution from unknown keys |
</threat_model>

<verification>
From apps/showcase/:
- `grep -c "Preview:" src/data/registry.ts` — must return 20
- `npx tsc --noEmit` — exits 0 (all imports resolve)
- Manually verify key list: `grep -o "'[A-Za-z]*':" src/data/registry.ts | sort` should print all 20 names
</verification>

<success_criteria>
- REGISTRY has exactly 20 entries covering all 11 components and 9 screens
- All key names match SECTIONS array strings exactly
- npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/04-rn-paper-showcase-app/04-07-SUMMARY.md` when done.
</output>
