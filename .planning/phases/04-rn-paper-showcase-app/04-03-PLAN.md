---
phase: 4
plan: 3
title: "Component Implementations — StatusBar, Button, SocialAuthButtons, OrDivider, PhoneInput, SegmentedToggle"
wave: 3
depends_on: [2]
files_modified:
  - apps/showcase/src/components/StatusBarPreview.tsx
  - apps/showcase/src/components/ButtonPreview.tsx
  - apps/showcase/src/components/SocialAuthButtonsPreview.tsx
  - apps/showcase/src/components/OrDividerPreview.tsx
  - apps/showcase/src/components/PhoneInputPreview.tsx
  - apps/showcase/src/components/SegmentedTogglePreview.tsx
  - apps/showcase/src/data/registry.ts
autonomous: true
requirements: [DS-COMPONENTS-A]

must_haves:
  truths:
    - "StatusBar, Button, SocialAuthButtons, OrDivider, PhoneInput, SegmentedToggle entries exist in REGISTRY"
    - "Button preview shows electric green contained button with Volt Black text (not white)"
    - "No hardcoded hex values in any component file — all colors from tokens.* or MD3Theme"
    - "Each file exports both a Preview component and a sourceCode string"
  artifacts:
    - path: "apps/showcase/src/components/ButtonPreview.tsx"
      provides: "Button preview with labelStyle={{ color: tokens.colorTextPrimary }}"
      exports: ["ButtonPreview", "ButtonSourceCode"]
    - path: "apps/showcase/src/components/SegmentedTogglePreview.tsx"
      provides: "SegmentedButtons from react-native-paper"
      exports: ["SegmentedTogglePreview", "SegmentedToggleSourceCode"]
  key_links:
    - from: "apps/showcase/src/data/registry.ts"
      to: "apps/showcase/src/components/*Preview.tsx"
      via: "named imports added to REGISTRY"
---

<objective>
Implement the first 6 of 11 VoltVenture components as React Native Paper compositions with Preview + sourceCode exports, and register them in the REGISTRY.

Purpose: Wave 3 (parallel) — implements component batch A. Wave 4 covers batch B. Together they populate the full component section.
Output: 6 component preview files + updated registry.ts entries for these 6 items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md
@.planning/phases/04-rn-paper-showcase-app/04-02-SUMMARY.md
@stories/components/status-bar.stories.js
@stories/components/button.stories.js
@stories/components/social-auth-buttons.stories.js
@stories/components/or-divider.stories.js
@stories/components/phone-input.stories.js
@stories/components/segmented-toggle.stories.js
</context>

<interfaces>
From apps/showcase/src/data/registry.ts (created in Plan 02):
```typescript
export type RegistryEntry = { Preview: React.ComponentType; sourceCode: string; };
export const REGISTRY: Record<string, RegistryEntry> = {};
```

From lib/voltventure_tokens.ts (key tokens for components):
- colorActionPrimary = '#c6ff2d' (electric green)
- colorTextPrimary = '#0f0f0f' (Volt Black)
- colorBorderSubtle = '#ebebeb'
- space400 = 16, space200 = 8, space600 = 24, space1200 = 48
- radiusFull = 999, radiusMd = 16
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement StatusBar, Button, SocialAuthButtons, OrDivider preview components</name>
  <files>
    apps/showcase/src/components/StatusBarPreview.tsx,
    apps/showcase/src/components/ButtonPreview.tsx,
    apps/showcase/src/components/SocialAuthButtonsPreview.tsx,
    apps/showcase/src/components/OrDividerPreview.tsx
  </files>

  <read_first>
    - stories/components/status-bar.stories.js — visual reference for StatusBar (dark icons on white)
    - stories/components/button.stories.js — visual reference: contained primary CTA + outlined secondary
    - stories/components/social-auth-buttons.stories.js — Google + Apple outlined buttons
    - stories/components/or-divider.stories.js — horizontal divider with centered "or" text
    - .planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md — Component → RN Paper Mapping table; Code Examples section (ButtonPreview full example)
  </read_first>

  <action>
    Each file follows this pattern:
    1. Named export `{Name}Preview` — a React component rendering the preview
    2. Named export `{Name}SourceCode` — a string literal (template literal, .trim()) containing the JSX source of the Preview component for display in the Code tab
    3. Import * as tokens from 'voltventure-design-system' for spacing/radius; do NOT hardcode hex values
    4. Use react-native-paper components per the Component → RN Paper Mapping in RESEARCH.md

    StatusBarPreview.tsx: Import StatusBar from expo-status-bar. Preview renders a View with backgroundColor tokens.colorSurfaceBase, padding tokens.space400, containing a StatusBar with style="dark" and a Text label "StatusBar — dark icons on light surface". No RN Paper equivalent for StatusBar itself.

    ButtonPreview.tsx: Import Button from react-native-paper. Preview renders a View (column, gap tokens.space200) with three Button variants:
    - mode="contained", labelStyle={{ color: tokens.colorTextPrimary }}, contentStyle={{ height: tokens.space1200 }}, style={{ borderRadius: tokens.radiusFull }} — label "Book a Ride"
    - mode="outlined", style={{ borderRadius: tokens.radiusFull }} — label "Cancel Ride"
    - mode="text" — label "Learn More"
    The contained button must have labelStyle={{ color: tokens.colorTextPrimary }} (Volt Black) — critical for electric green background readability. Do not omit this prop.

    SocialAuthButtonsPreview.tsx: Import Button from react-native-paper, Ionicons from @expo/vector-icons (use icon prop on Button: icon={() => <Ionicons name="logo-google" size={tokens.iconSizeMd} />} and icon={() => <Ionicons name="logo-apple" size={tokens.iconSizeMd} />}). Two Button components, mode="outlined", style={{ borderRadius: tokens.radiusFull, borderColor: tokens.colorBorderSubtle, marginBottom: tokens.space200 }}. Labels: "Continue with Google" and "Continue with Apple".

    OrDividerPreview.tsx: Import Divider, Text from react-native-paper. Preview renders a View with flexDirection row, alignItems center: a Divider with style={{ flex: 1 }}, a Text " or " with paddingHorizontal tokens.space200, another Divider flex 1.
  </action>

  <verify>
    <automated>grep -l "export.*Preview\|export.*SourceCode" apps/showcase/src/components/ButtonPreview.tsx apps/showcase/src/components/StatusBarPreview.tsx apps/showcase/src/components/OrDividerPreview.tsx apps/showcase/src/components/SocialAuthButtonsPreview.tsx | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - All 4 files exist in apps/showcase/src/components/
    - Each file exports both a Preview component and a SourceCode string
    - ButtonPreview.tsx: contained Button has labelStyle={{ color: tokens.colorTextPrimary }} — grep confirms
    - No file contains any hardcoded hex string (grep -c "#[0-9a-fA-F]" should return 0 for each file)
    - OrDividerPreview.tsx: uses Divider from react-native-paper with flex:1 on both sides
    - SocialAuthButtonsPreview.tsx: two Button mode="outlined" with Ionicons icon prop
  </acceptance_criteria>

  <done>StatusBar, Button, SocialAuthButtons, OrDivider preview files created with sourceCode exports</done>
</task>

<task type="auto">
  <name>Task 2: Implement PhoneInput, SegmentedToggle previews and register all 6 in REGISTRY</name>
  <files>
    apps/showcase/src/components/PhoneInputPreview.tsx,
    apps/showcase/src/components/SegmentedTogglePreview.tsx,
    apps/showcase/src/data/registry.ts
  </files>

  <read_first>
    - stories/components/phone-input.stories.js — visual reference: country code prefix + phone number field
    - stories/components/segmented-toggle.stories.js — visual reference: two-option toggle (e.g., "Ride" / "Package")
    - apps/showcase/src/data/registry.ts — current state (skeleton with empty REGISTRY); read before modifying
    - .planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md — Component → RN Paper Mapping for PhoneInput (TextInput.Affix) and SegmentedToggle (SegmentedButtons)
  </read_first>

  <action>
    PhoneInputPreview.tsx: Import TextInput from react-native-paper. Preview renders a View with padding tokens.space400. TextInput with mode="outlined", label="Phone number", keyboardType="phone-pad", left={<TextInput.Affix text="+1" />}. Use style={{ borderRadius: tokens.radiusSm }}.

    SegmentedTogglePreview.tsx: Import SegmentedButtons from react-native-paper. Use React.useState for selected value (default: 'ride'). SegmentedButtons with value={selected}, onValueChange={setSelected}, buttons={[{ value: 'ride', label: 'Ride' }, { value: 'package', label: 'Package' }]}. Wrap in View with padding tokens.space400.

    Update apps/showcase/src/data/registry.ts: read the current file first, then replace the empty REGISTRY with entries for the 6 components implemented in this plan. Import { StatusBarPreview, StatusBarSourceCode } from '../components/StatusBarPreview', and similarly for Button, SocialAuthButtons, OrDivider, PhoneInput, SegmentedToggle. Add entries: StatusBar, Button, SocialAuthButtons, OrDivider, PhoneInput, SegmentedToggle. Leave the rest of the REGISTRY keys for Wave 4 plan to add (ProgressStrip, TrustPanel, MapPin, TabBar, BottomCard, and all 9 screens).
  </action>

  <verify>
    <automated>grep -c "SegmentedButtons" apps/showcase/src/components/SegmentedTogglePreview.tsx && node -e "const r = require('./apps/showcase/src/data/registry.ts')" 2>&1 | grep -c "SyntaxError" || grep -c "StatusBar\|Button\|SocialAuth\|OrDivider\|PhoneInput\|SegmentedToggle" apps/showcase/src/data/registry.ts</automated>
  </verify>

  <acceptance_criteria>
    - apps/showcase/src/components/PhoneInputPreview.tsx: uses TextInput mode="outlined" with TextInput.Affix text="+1"
    - apps/showcase/src/components/SegmentedTogglePreview.tsx: uses SegmentedButtons from react-native-paper with useState
    - apps/showcase/src/data/registry.ts: contains 6 import statements (one per component) and 6 REGISTRY entries: StatusBar, Button, SocialAuthButtons, OrDivider, PhoneInput, SegmentedToggle
    - No hardcoded hex values in PhoneInputPreview.tsx or SegmentedTogglePreview.tsx
  </acceptance_criteria>

  <done>PhoneInput and SegmentedToggle previews created; REGISTRY updated with all 6 component entries</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @expo/vector-icons | Bundled with Expo SDK — no separate install; used for social auth button icons |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | PhoneInput visual demo | accept | PhoneInput is a static visual demo; no actual phone data submitted or stored |
</threat_model>

<verification>
From apps/showcase/:
- `grep -rn "#[0-9a-fA-F]\{6\}" src/components/ButtonPreview.tsx` — should return 0 matches (no hardcoded hex)
- `grep "colorTextPrimary" src/components/ButtonPreview.tsx` — should return 1 match (labelStyle override)
- `grep "StatusBar\|Button\|OrDivider\|PhoneInput\|SegmentedToggle" src/data/registry.ts` — should return 6+ matches
- `npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- 6 component preview files created in apps/showcase/src/components/
- Each file has named Preview export and SourceCode string export
- No hardcoded hex values in any component file
- ButtonPreview has labelStyle={{ color: tokens.colorTextPrimary }} on contained button
- REGISTRY updated with 6 entries for this batch
</success_criteria>

<output>
Create `.planning/phases/04-rn-paper-showcase-app/04-03-SUMMARY.md` when done.
</output>
