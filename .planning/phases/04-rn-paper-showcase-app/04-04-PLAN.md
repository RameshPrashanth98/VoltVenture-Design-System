---
phase: 4
plan: 4
title: "Component Implementations — ProgressStrip, TrustPanel, MapPin, TabBar, BottomCard"
wave: 3
depends_on: [2]
files_modified:
  - apps/showcase/src/components/ProgressStripPreview.tsx
  - apps/showcase/src/components/TrustPanelPreview.tsx
  - apps/showcase/src/components/MapPinPreview.tsx
  - apps/showcase/src/components/TabBarPreview.tsx
  - apps/showcase/src/components/BottomCardPreview.tsx
autonomous: true
requirements: [DS-COMPONENTS-B]

must_haves:
  truths:
    - "ProgressStrip shows a ProgressBar with electric green fill color"
    - "TabBar uses BottomNavigation.Bar (not deprecated createMaterialBottomTabNavigator)"
    - "MapPin uses @expo/vector-icons Ionicons (bundled with Expo — no extra install)"
    - "BottomCard uses Surface with borderTopLeftRadius and borderTopRightRadius from tokens"
    - "Each file exports both a Preview component and a sourceCode string"
    - "No hardcoded hex values in any file"
  artifacts:
    - path: "apps/showcase/src/components/TabBarPreview.tsx"
      provides: "BottomNavigation.Bar from react-native-paper"
      exports: ["TabBarPreview", "TabBarSourceCode"]
    - path: "apps/showcase/src/components/ProgressStripPreview.tsx"
      provides: "ProgressBar with colorActionPrimary"
      exports: ["ProgressStripPreview", "ProgressStripSourceCode"]
  key_links:
    - from: "apps/showcase/src/components/MapPinPreview.tsx"
      to: "@expo/vector-icons"
      via: "Ionicons location icon"
---

<objective>
Implement the remaining 5 of 11 VoltVenture components as React Native Paper compositions. This plan runs in parallel with Plan 03 (Wave 3) since they write to different files.

Purpose: Completes the component section. Plan 07 will merge both batches into the final REGISTRY.
Output: 5 component preview files. Note: REGISTRY update for this batch is handled in Plan 07 to avoid conflict with Plan 03's registry writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md
@.planning/phases/04-rn-paper-showcase-app/04-02-SUMMARY.md
@stories/components/progress-strip.stories.js
@stories/components/trust-panel.stories.js
@stories/components/map-pin.stories.js
@stories/components/tab-bar.stories.js
@stories/components/bottom-card.stories.js
</context>

<interfaces>
From lib/voltventure_tokens.ts:
- colorActionPrimary = '#c6ff2d' (ProgressBar color prop)
- colorSurfaceBase = '#ffffff', colorSurfaceRaised = '#ffffff'
- colorTextPrimary = '#0f0f0f', colorTextSecondary = '#808080'
- space400 = 16, space500 = 20, space600 = 24, space1200 = 48
- radiusLg = 20, radiusXl = 28

react-native-paper APIs used:
- ProgressBar: props progress (0-1), color, style
- BottomNavigation.Bar: props navigationState ({ index, routes }), onTabPress, renderIcon, renderLabel, shifting=false
- Surface: prop elevation, style
- Card.Content, Text, Button from react-native-paper
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement ProgressStrip, TrustPanel, MapPin preview components</name>
  <files>
    apps/showcase/src/components/ProgressStripPreview.tsx,
    apps/showcase/src/components/TrustPanelPreview.tsx,
    apps/showcase/src/components/MapPinPreview.tsx
  </files>

  <read_first>
    - stories/components/progress-strip.stories.js — visual reference: step indicator, 1 of 3 progress
    - stories/components/trust-panel.stories.js — visual reference: surface card with bullet trust points
    - stories/components/map-pin.stories.js — visual reference: circular green pin with location icon
    - .planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md — Component → RN Paper Mapping (ProgressBar, Surface, MapPin custom View)
  </read_first>

  <action>
    ProgressStripPreview.tsx: Import ProgressBar, Text from react-native-paper. Preview renders a View with padding tokens.space400. Show a Text label "Step 1 of 3" above a ProgressBar with progress={0.33} and color={tokens.colorActionPrimary}. Wrap ProgressBar in a View with marginTop tokens.space200.

    TrustPanelPreview.tsx: Import Surface, Text from react-native-paper. Preview renders a Surface with elevation={1}, style borderRadius tokens.radiusLg, padding tokens.space500, margin tokens.space400. Inside: a Text heading (typeface Manjari_700Bold if loaded, fontSize tokens.typeHeadingSm.fontSize if that token exists — otherwise use fontSize 18) "Why VoltVenture?" and three trust bullet Text rows: "✓ Verified drivers with background checks", "✓ Real-time trip sharing", "✓ 24/7 rider support". Use colorTextPrimary for heading, colorTextSecondary for bullet rows.

    MapPinPreview.tsx: Import { Ionicons } from '@expo/vector-icons'. Preview renders a View centered horizontally (alignItems: 'center'). Inside: a View with width tokens.space1200, height tokens.space1200, borderRadius tokens.radiusFull, backgroundColor tokens.colorActionPrimary, alignItems 'center', justifyContent 'center', containing an Ionicons name="location" size={tokens.iconSizeLg} color={tokens.colorTextPrimary}. Below that pin, a Text label "Home" with colorTextPrimary and marginTop tokens.space100. No hardcoded hex values.
  </action>

  <verify>
    <automated>grep -c "ProgressBar" apps/showcase/src/components/ProgressStripPreview.tsx && grep -c "Surface" apps/showcase/src/components/TrustPanelPreview.tsx && grep -c "Ionicons" apps/showcase/src/components/MapPinPreview.tsx</automated>
  </verify>

  <acceptance_criteria>
    - ProgressStripPreview.tsx: uses ProgressBar with progress={0.33} and color={tokens.colorActionPrimary}; no hardcoded hex
    - TrustPanelPreview.tsx: uses Surface with elevation prop; has 3 trust bullet Text items; no hardcoded hex
    - MapPinPreview.tsx: uses Ionicons from @expo/vector-icons with name="location"; circular background uses tokens.colorActionPrimary; text uses tokens.colorTextPrimary; no hardcoded hex
    - All 3 files export both Preview and SourceCode
  </acceptance_criteria>

  <done>ProgressStrip, TrustPanel, MapPin preview components created</done>
</task>

<task type="auto">
  <name>Task 2: Implement TabBar and BottomCard preview components</name>
  <files>
    apps/showcase/src/components/TabBarPreview.tsx,
    apps/showcase/src/components/BottomCardPreview.tsx
  </files>

  <read_first>
    - stories/components/tab-bar.stories.js — visual reference: 3-tab bar (Home, Ride, Profile)
    - stories/components/bottom-card.stories.js — visual reference: rounded-top sheet with price and CTA
    - .planning/phases/04-rn-paper-showcase-app/04-RESEARCH.md — BottomNavigation.Bar guidance ("use BottomNavigation.Bar not BottomNavigation"); State of the Art table (deprecated createMaterialBottomTabNavigator); Component → RN Paper Mapping (TabBar → BottomNavigation.Bar, BottomCard → Surface)
  </read_first>

  <action>
    TabBarPreview.tsx: Import BottomNavigation from react-native-paper, Ionicons from @expo/vector-icons. Use React.useState for index (default 0). Define routes = [{ key: 'home', title: 'Home', focusedIcon: 'home', unfocusedIcon: 'home-outline' }, { key: 'ride', title: 'Ride', focusedIcon: 'bicycle', unfocusedIcon: 'bicycle-outline' }, { key: 'profile', title: 'Profile', focusedIcon: 'person', unfocusedIcon: 'person-outline' }]. Render BottomNavigation.Bar with navigationState={{ index, routes }}, onTabPress={({ route }) => setIndex(routes.indexOf(route))}, renderIcon={({ route, focused, color }) => <Ionicons name={focused ? route.focusedIcon : route.unfocusedIcon} size={tokens.iconSizeMd} color={color} />}, shifting={false}. Wrap in a View with paddingTop tokens.space400.

    BottomCardPreview.tsx: Import Surface, Text, Button from react-native-paper. Preview renders a Surface with elevation={4}, style: borderTopLeftRadius tokens.radiusXl, borderTopRightRadius tokens.radiusXl, padding tokens.space500, backgroundColor tokens.colorSurfaceBase. Inside: Text "Your ride" in heading style, a row with Text "Estimated fare" left and Text "12 IMP" right (using colorTextPrimary), and a Button mode="contained" labelStyle={{ color: tokens.colorTextPrimary }} marginTop tokens.space400 label "Confirm Booking". No hardcoded hex values.
  </action>

  <verify>
    <automated>grep -c "BottomNavigation.Bar" apps/showcase/src/components/TabBarPreview.tsx && grep -c "borderTopLeftRadius" apps/showcase/src/components/BottomCardPreview.tsx</automated>
  </verify>

  <acceptance_criteria>
    - TabBarPreview.tsx: uses BottomNavigation.Bar (not BottomNavigation wrapper); uses useState for index; shifting={false}; no hardcoded hex
    - BottomCardPreview.tsx: Surface has borderTopLeftRadius and borderTopRightRadius using tokens.radiusXl; Button contained has labelStyle={{ color: tokens.colorTextPrimary }}; no hardcoded hex
    - Both files export Preview and SourceCode
  </acceptance_criteria>

  <done>TabBar and BottomCard preview components created; all 5 component batch B files complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @expo/vector-icons | Used for MapPin and TabBar icons; bundled with Expo SDK, no extra install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | Static demo components | accept | All components are static visual demos; no real data or user interaction persisted |
</threat_model>

<verification>
From apps/showcase/:
- `grep -rn "#[0-9a-fA-F]\{6\}" src/components/TabBarPreview.tsx` — 0 matches
- `grep -rn "#[0-9a-fA-F]\{6\}" src/components/BottomCardPreview.tsx` — 0 matches
- `grep "BottomNavigation.Bar" src/components/TabBarPreview.tsx` — 1 match
- `grep "colorTextPrimary" src/components/BottomCardPreview.tsx` — at least 1 match on the contained button
</verification>

<success_criteria>
- 5 component preview files created: ProgressStrip, TrustPanel, MapPin, TabBar, BottomCard
- All use RN Paper primitives per the Component → RN Paper Mapping
- No hardcoded hex values in any file
- TabBar uses BottomNavigation.Bar with shifting={false}
- BottomCard and Button contain buttons have labelStyle={{ color: tokens.colorTextPrimary }}
</success_criteria>

<output>
Create `.planning/phases/04-rn-paper-showcase-app/04-04-SUMMARY.md` when done.
</output>
